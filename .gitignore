node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
# Templates versionados (contêm campos VAZIOS, nunca segredos):
!.env.example
!.env.local.example
!.env.staging.example

# Banco SQLite local (runtime) — não versionar
zappflow.db
zappflow.db-shm
zappflow.db-wal

# Sessão/credenciais do WhatsApp Web (NUNCA versionar)
.wwebjs_auth/
.wwebjs_cache/

# Mídias recebidas (runtime)
media/

# Scripts de teste/rascunho temporários (não versionar)
scratchpad/

# Segredo de JWT gerado em runtime quando JWT_SECRET não está na env (nunca versionar)
.jwt_secret

# Radar B2B — base gerada pelo ETL (não versionar)
data/radar_rio.db
tools/radar_etl/downloads/
