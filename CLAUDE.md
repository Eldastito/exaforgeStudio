# ExaforgeStudio — Contexto pra sessões Claude Code

Repositório monolítico (frontend React + backend Node/Express + SQLite via `better-sqlite3`) que roda 15+ módulos verticais isolados por `organization_id`.

## Como o trabalho é organizado

- **ADRs** em `docs/adr/ADR-XXX-*.md` — cada módulo grande tem um ADR próprio. Toda decisão arquitetural relevante vive lá (nunca no código nem em wiki externa).
- **Fatias** — unidade de PR. Cada ADR é fatiado em N incrementos numerados (Fatia 1..N). Cada fatia = 1 PR draft → CI verde → merge → next fatia. Testes por fatia em `scripts/test-*.ts` (rodam via `npm run test:<nome>`), matrix em `.github/workflows/ci.yml`.
- **Serviços** em `src/server/*Service.ts` — 1 arquivo por domínio, `static` methods, `orgId` sempre 1º arg (tenant isolation obrigatório).
- **Rotas** em `src/server/routes/*.ts` — Express, com `AuthRequest` + `requireRole` + `actor(req)`.
- **DB** em `src/server/db.ts` — CREATE-then-ALTER estrito (aditivos no fim; nunca reorderar). Colunas opt-in em `organization_settings`.

## Módulos em produção (por ADR)

| ADR | Módulo | Status |
| --- | --- | --- |
| ADR-080 | Clínica base (agenda + prontuário + docs + LGPD + PIN + snapshot + retenção + relatório mensal) | 33 fatias — em produção |
| **ADR-145** | **Clínica: Jornada de Tratamento multiespecialidade** | **FECHADO — 15 fatias (34–48) em produção** |
| ADR-144 | Escola — conector família + resumo diário | em produção |
| ADR-136 | Decision-Action Ledger (`business_signals`) | em produção — usado por Retail, Escola, Clínica (F47) |
| **ADR-136/156** | **Decision Intelligence (consolidação transversal)** | **FECHADO — DI-1..DI-4.5 + UI + ADR-156. Aditivo sobre ADR-135/136/152; sem menu novo pro lojista** |
| **ADR-161** | **Radar Empresarial (percepção transversal + sensibilidade de negócio)** | **FECHADO — PRD 2 em 12 fases (F1–F12) / 20 PRs. Aditivo sobre ADR-136/158; sem ledger/alerta paralelo (CA1). Três origens de percepção (humana/digital/externa) + correlação + anomalia + investigação + priorização goal/SLA/reversibilidade + roteamento + calibração + production readiness (health · budget por-detector · runbook). Runbook em `docs/runbook/radar-operacao.md`** |
| **ADR-162** | **Customer Recovery & Reputation Intelligence (PRD 5)** | **FECHADO — 15 fatias (F0–F14) em produção. Reclame AQUI como sensor externo → ciclo fechado sobre a espinha (detectar→identificar→classificar→investigar→recomendar→responder→resolver→réplica/fechar→prevenir→aprender→medir→endurecer). Aditivo puro (ADR-135/136/152/158/159); sem motor/policy/runtime/alerta paralelo; sem tabela `reputation_cases`. Guardrails RN-CRR (untrusted-data, alegação≠fato, grounding obrigatório, high-risk conservador, não inventa dinheiro, baseline antes de causa). Runbook em `docs/runbook/reputation-operacao.md`** |
| **ADR-163** | **Invisible UX & Zero-Training Experience (PRD 6)** | **FECHADO — F0–F16 (impl. + reminder + 3 docs). Runbook em `docs/runbook/ux-operacao.md`. Aditivo/reversível sobre a base existente; NÃO é redesign nem Home/engine concorrente (§115/CA17). Entregues: F2 `NavigationManifestService` (nav por necessidade) · F3 "Hoje" por exceção (ESTENDE `FalaTuHomeService`) · F4 `UxPresentationService` (Decision Card + humanState/humanError) · F5 `AdaptiveOnboardingService` (autodiscovery, nunca inventa) · F6 `InferredSettingsService` (sugere, nunca auto-aplica política — RN-UX-3) · F7 `ZeroTrainingHelpService` (Fala Tu ensine/mostre/faça/onde) · F8 `ExecutionResultsService` (Executando/Resultados) · F9 `ContextualUpgradeService` (upgrade situacional ∩ fora-do-plano, anti-catálogo) · F10 `UxTelemetryService`+`ux_telemetry_events` (LGPD-minimizada, opt-in) · F11 `MobileReadinessService` (fallback garantido, CA1) · F12 `LegacyReductionService` (gate de retirada ADVISÓRIO guiado por telemetria — nunca remove tela; §112) · F13 `UxPreferencesService` (quiet-hours + limiar de alerta persistidos — fecha o gap §53/§68 da F0; `FalaTuProactiveService` passa a honrar a preferência, default 0-regressão) · F14 (limiar de alerta TOMA EFEITO — `selectUrgent` filtra push de valor abaixo do limiar; nunca cala crítico/valor-desconhecido §45/D7) · F16 `LegacyReductionReminderService` (Scheduler semanal publica `business_signals` quando a telemetria prova substituição — F12 vira proativo; advisório, resolve quando não há candidato). Rotas em `/api/ux/*` + `/api/entitlements/navigation-manifest`. 5 flags opt-in + 1 tabela nova (F10) + 3 colunas de preferência (F13). Dinheiro role-gated (§73); IA de nav em `docs/ux/ADR-163-F1-information-architecture.md`** |
| **ADR-164** | **Platform Trust, Reliability & Capacity Intelligence (PRD 7)** | **FECHADO — F0–F14 + as 2 fatias de AMBIENTE em produção. As pendências de ambiente viraram scaffold CONFIGURÁVEL e honesto: F2-host `VpsSpecProfileService` (Admin Master registra vCPU/RAM/storage/limites → headroom usa `cpuBasis:"spec"` + `capacityContext` com uso absoluto; sem perfil, honesto) + F3.4-SLO `SloDefinitionService` (Admin Master define p95 alvo/erro → `OperationalHealthService` classifica a latência contra o SLO, antes só reportada; sem SLO, honesto). Ambos GLOBAL em `platform_settings`, rotas `GET/POST /api/admin/vps-spec-profile` e `/slo`, `test:vps-spec-profile` (13) + `test:slo-definition` (15). Motores (todos determinísticos/GLOBAL/Admin Master, sem prom-client/OTel): F1 `PlatformTelemetryService`+`PlatformTelemetryContract` (fachada provider-agnóstica, Null honesto) · F2 `NodeHostTelemetryProvider` (só o que o Node vê; host/infra `requires_host_provider`) · F3 `HttpMetricsCollector`+middleware (p50/p95/p99/rps/5xx por rota normalizada, em memória) · F4 `DependencyHealthService` (fila+banco+provider; não-instrumentado nunca "healthy") · F5 `OperationalHealthService` (estende Prontidão de Produção) · F6 `PlatformBaselineService`+tabela global `platform_health_snapshots` (baseline sazonal, anomalia=hipótese §35, `insufficient_history` §59) · F7 `CapacityHeadroomService` (folga+zona por recurso) · F8 `CapacityForecastService` (tendência+confiança, ETA ao crítico, agrega por dia p/ tirar sazonalidade) · F9 `PlatformRootCauseService` (correlação sintoma→causa como hipótese; deploy `not_available`) · F10 `CapacityRecommendationService` (advisória, explicável, NUNCA executa D6/CA16) · F11 `PlatformProtectionModeService` (postura NORMAL/CAUTIOUS/PROTECTED, SHADOW por default §102, Guard nunca sacrifica crítico CA23) · F12 `PlatformAlertService`+tabela global `platform_health_events` (Platform Health Event separado de `business_signals`; anti-spam por dedupe+janela CA17) · F13 `CapacityEnvelopeService`+harness `loadtest:capacity` (limite seguro de rps via teste de carga fora de prod; `awaiting_load_test` sem dados §59) · F14 endurecimento (`test:platform-hardening` codifica os RN-PRC como regressão) + runbook `docs/runbook/platform-operacao.md`. Baseline/forecast/envelope só produz resultado útil após dias acumulando (§103). Guardrails RN-PRC: raw fora do SQLite · recomendação≠execução · Admin Master only (nunca vaza infra pro tenant) · determinístico antes de LLM · telemetria ausente≠saúde · nunca perde operação crítica · Platform Health Event separado. Rotas em `/api/admin/*` (operational-health, platform-baseline/anomalies, capacity-headroom/forecast/recommendations/envelope, platform-root-cause, protection-mode[+enforce], platform-alerts[+refresh]). 2 tabelas globais + 1 flag global (`platform_settings`), 0 tabelas/flags per-tenant. Análise em `docs/prd/ANALISE-PRD7-vs-CODEBASE-E-INFRA.md`; runbook em `docs/runbook/platform-operacao.md`** |
| **ADR-165** | **Universal Closed Loop & Outcome Assurance (PRD 8)** | **FECHADO — F0–F13 em produção (14 fatias). `DONE ≠ RESULTADO` observável ponta-a-ponta; os 3 achados da auditoria fechados: (a) condições de processo inertes→F2, (b) measurement gap silencioso→F6, (c) dupla contagem→F5. Motores (determinísticos, read-only sobre a FSM, isolados por org): F1 `OutcomeAssuranceService` (escada planned→executed→effect_confirmed→impact_measured→assured por ação e correlação) · F2 `ProcessOutcomeContractService` (avalia success/failure_conditions de processo via `PlaybookEngine.evaluateCondition`) · F3/F4 `BusinessOutcomeResolverRegistry` (pergunta ao system-of-record: Cobrança `receivables`, Comercial `sales_recovery_attributions`, Reputação `business_signals.status='resolved'`, Varejo `retail_daily_closings.status`) · F5 anti-dupla-contagem (`event_key` + índice UNIQUE parcial + dedup no ledger) · F6 `OutcomeReconcilerService` (done-sem-outcome→`business_signals`/`attention`) · F7 `ConfirmationEngine.sweepTimeouts` publica `confirmation_timed_out` · F8 `ExecutionTraceService` estendido (execuções+confirmações) · F9 `ExecutionResultsService.executing` mostra `assurance` por objetivo (R$ role-gated) · F10 `OutcomeCorrectionService` (correção GOVERNADA, nunca executa) · F11 `OutcomeAssuranceMetricsService` (KPIs por query) · F12 runbook · F13 `test:outcome-assurance-hardening`. Guardrails RN-OA-1..9: DONE≠RESULTADO · null≠zero · read-only não muda FSM · fact/estimate/influenced nunca somados · sem dupla contagem · determinístico antes de LLM · untrusted external data · não inventa dinheiro · correção governada. Aditivo puro; NÃO cria Runtime/Policy/Confirmation/Impact-Ledger/Scheduler/tabela-de-alerta/learning-engine. Rotas `/api/decision-intelligence/assurance/*` (action/correlation/process/reconcile/correct/metrics) + `/trace`. Pré-requisito do PRD 9 (Enterprise Learning) atendido. Análise em `docs/prd/ANALISE-PRD8-vs-CODEBASE.md`; ADR em `docs/adr/ADR-165-universal-closed-loop-outcome-assurance.md`; runbook em `docs/runbook/outcome-assurance-operacao.md`. Detalhe por fatia: F0 = auditoria + matriz (doc-only). F1 = `OutcomeAssuranceService.assess()` read-only (estado DERIVADO executado→efeito→outcome→impacto, por ação e por `correlation_id`; escada planned→executed→effect_confirmed→impact_measured→assured; gaps done_without_outcome/confirmation_pending; business outcome fica `resolver_pending` até a F3; rotas `/api/decision-intelligence/assurance/*`; `test:outcome-assurance` 23 checks). F2 = `ProcessOutcomeContractService.evaluate()` — avalia `success/failure_conditions` de PROCESSO (antes inertes, achado (a)) via `PlaybookEngine.evaluateCondition` sobre contexto derivado da instância; `toCondition` normaliza formato nativo E clausal (Outcome Contract sem tabela nova); falha tem precedência, sem contrato→no_contract, read-only não muda FSM; rota `/assurance/process/:instanceId`; `test:process-outcome-contract` 13 checks. F3 = `BusinessOutcomeResolverRegistry` + `CollectionOutcomeResolver` — pergunta ao system-of-record (`receivables.status='received'`) se o problema se resolveu, nunca à IA (D3/RN-OA-6); registry substitui array hard-coded do `ConfirmationEngine`; integrado no `assessAction` (o `resolver_pending` da F1 agora resolve); sem prova→unknown (enviado≠pago); `test:business-outcome-resolver` 11 checks. F4 = resolvers dos outros 3 golden loops: Comercial (`sales_recovery_attributions` por action_id → ganho atribuído), Reputação (`business_signals.status='resolved'` → resolvido≠respondido), Varejo (`retail_daily_closings.status` reconciled/approved→confirmed, divergent→not_confirmed); `test:outcome-resolvers-golden` 13 checks. F5 = anti-dupla-contagem (achado (c)): coluna `event_key` + índice UNIQUE parcial `(org,event_key) WHERE event_key IS NOT NULL` em `action_outcomes` (aditivo, nunca falha em legado) + `OutcomeMeasurementService.record(eventKey)` idempotente + dedup por event_key no ledger; `test:outcome-dedup` 8 checks. F6 = `OutcomeReconcilerService` — varre `done`-sem-`action_outcome` (após janela de graça) e publica sinal `outcome_assurance/done_without_outcome` em `business_signals`→`attention()`, resolve quando a medição atrasada chega; fecha o achado (b) (catch vazio silencioso do `complete`); Scheduler pass horário per-org; rota `POST /assurance/reconcile`; `test:outcome-reconciler` 11 checks. F7 = `ConfirmationEngine.sweepTimeouts` passa a publicar `business_signal` (`outcome_assurance/confirmation_timed_out`, severity risk) quando o SLA de confirmação estoura → aparece em `attention()`; fecha o gap de integração (timeout era marcado mas não fluía pra atenção); best-effort/idempotente; `test:confirmation-timeout-signal` 11 checks. F8 = `ExecutionTraceService.trace` estendido com `executions` (`action_execution_log` por correlation_id) + `confirmations` (`action_confirmations` por action_id) — o fio sinal→decisão→execução→confirmação→outcome fica completo (antes pulava esses dois elos); `summary` conta os novos elos; `closedLoop` não regride; `test:execution-trace-fullcycle` 9 checks. F9 = superfície de garantia — `ExecutionResultsService.executing` mostra `assurance` por objetivo (`assured` × só `executed`/`planned`, derivado de `assessCorrelation`); fato sempre visível, só o R$ role-gated (§73); `test:ux-execution-results` estendido p/ 20 checks. F10 = `OutcomeCorrectionService` — propõe correções GOVERNADAS pros gaps abertos de `outcome_assurance` via `DecisionActionService.propose` (nasce `awaiting_approval`, nunca executa — RN-OA-9); `actionType` prefixado `outcome_correction:` faz o Reconciler F6 ignorá-la (anti-recursão); idempotente; rota `POST /assurance/correct`; `test:outcome-correction` 10 checks. F11 = `OutcomeAssuranceMetricsService` — KPIs de garantia (`outcomeCoveragePct`/`effectConfirmedPct`/`assuredPct`/`gapRatePct`/`openGaps`) derivados por query (RN-004); percentuais null sem done (não inventa 0%); rota `GET /assurance/metrics`; `test:outcome-assurance-metrics` 11 checks. F12 = runbook `docs/runbook/outcome-assurance-operacao.md` (mapa dos serviços, rotas `/assurance/*`, fluxo detectar→sinalizar→corrigir→resolver, guardrails RN-OA, como adicionar domínio ao resolver). Pré-condição atendida: metade de código do PRD 7 (ADR-164) encerrou. Tese: `DONE ≠ RESULTADO` — hoje "todo processo `completed` teve outcome de negócio confirmado" é verdade POR DOMÍNIO instrumentado (golden loops Cobrança/Comercial/Reputação/Varejo já distinguem executado×confirmado×medido), não POR invariante do sistema. Aditivo puro; NÃO cria Runtime/Policy/Confirmation/Impact-Ledger/Scheduler/tabela-de-alerta/learning-engine (mandato do operador). Achados F0: (a) `success/failure_conditions` de PROCESSO em `process_definitions` só armazenados, nunca avaliados (`ProcessRuntimeService.defineProcess:139-141`); só `successCondition` de STEP avaliada; (b) measurement gap — `DecisionActionService.complete:212` catch vazio engole falha de `record`, ação fica `done` sem `action_outcome`; (c) double-count — `action_outcomes` sem UNIQUE (`db.ts:6261-6273`) + `provenValue` somado Comigo+Retail no `UnifiedImpactLedgerService.assemble:194`. Reuso: `PlaybookEngine.evaluateCondition` (avaliador puro), colunas `success/failure_conditions_json` existentes (Outcome Contract, sem tabela nova), `OutcomeMeasurementService`/`UnifiedImpactLedgerService` (medição), `ExecutionTraceService` (trace por `correlation_id`, estender p/ confirmações+execuções), `BusinessSignalService`+`attention()` (exceção com dedupe), `DecisionAction→ApprovalPolicy→CommandExecutor` (correção governada). Novo mínimo: `OutcomeAssuranceService.assess()` read-only (estado DERIVADO, não muda FSM — RN-OA-3) + `BusinessOutcomeResolver` registry determinístico (pergunta ao system-of-record, não LLM) + Reconciler done-sem-outcome. Guardrails RN-OA: DONE≠RESULTADO · null≠zero · ausência≠falha · fact/estimate/influenced nunca somados · sem dupla contagem · determinístico antes de LLM · untrusted external data · não inventa dinheiro · correção governada. Plano F0–F13. Pré-requisito do PRD 9 (Enterprise Learning). Análise em `docs/prd/ANALISE-PRD8-vs-CODEBASE.md`; ADR em `docs/adr/ADR-165-universal-closed-loop-outcome-assurance.md`** |
| **ADR-166** | **Enterprise Learning & External Intelligence 2.0 (PRD 9)** | **FECHADO — F0–F14 em produção (15 fatias). O ciclo fecha ponta-a-ponta: outcome ASSEGURADO (PRD 8) → aprendizado forte → evidência histórica → prior assimétrico na decisão → decisão melhor → novo outcome; e a inteligência externa distingue síntese do modelo (`model_knowledge`) de fonte viva (`live`). Guardrails RN-EL-1..9 + RN-EI-1..7 em `test:enterprise-learning-hardening`; runbook `docs/runbook/learning-operacao.md`. Motor ÚNICO (`PatternMemoryService`) + pipeline ÚNICO; cross-tenant FORA (§79); nenhum engine paralelo (§184). Pré-requisito do PRD 10 atendido. Detalhe por fatia: F0 FECHADA (auditoria + matriz — doc-only). F1 FECHADA — primeira ligação real do ciclo: ledger `business_pattern_outcomes` + `event_key`/idempotência + `source` em `PatternMemoryService.recordOutcome` (fecha o achado (a): antes `acted+1` sem chave dobrava a contagem) + `PatternLearningFromAssuranceService` liga `OutcomeAssurance(assured)→PatternMemory` (só `assured` aprende forte — DONE ≠ exemplo, RN-EL-1; worked/backfired DETERMINÍSTICO do valor MEDIDO base `fact` — RN-EL-3/6; sweep idempotente per-org no Scheduler + rota `POST /assurance/learn`; `test:enterprise-learning-assured` 28). F2 FECHADA — `assuredEffectiveness` (achado (b)): `PatternMemoryService.assuredStats`/`allEffectiveness` DERIVAM por query do ledger o recorte só-`assured`, SEPARADO do `effectiveness` que mistura manual+assured (RN-EL-6); sem prova → `null`, nunca 0 (RN-EL-5); anexado ao `/api/insights/patterns`; `test:assured-effectiveness` 12. F3 FECHADA — `LearningEpisodeService`: read model DERIVADO (sem tabela nova, D5/RN-004) que amarra padrão→desfechos assured→`learningState` (`unproven`/`reinforced`/`weakened`/`contested`); `suggestedRefutation` endereça o achado (c) como EVIDÊNCIA (não escreve `refuted`); rotas `/learning/episodes` + `/learning/episode/:patternId`; `test:learning-episode` 17. F4 FECHADA — `EvidencePackageService.historicalEvidence[]` (antes `[]` sempre) agora carrega o aprendizado com prova ASSEGURADA (Learning Episodes `assured`) — o ponto onde o aprendizado entra no pacote de decisão como EVIDÊNCIA (nunca ordem); refutação-sugerida primeiro; vazio sem prova (0-regressão); `test:historical-evidence` 8. F5 FECHADA — `DecisionEngine.learningPrior` consome o `historicalEvidence` como prior ASSIMÉTRICO: só ADICIONA cautela (`proceed→proceed_with_caution` quando o aprendizado assegurado mostra padrões que enfraqueceram/contradisseram), NUNCA relaxa nem toca `hold_for_human` (RN-EL-2/8, CA11-14); pequeno/aditivo/explicável/reversível; `test:learning-prior` 14. Ciclo interno FECHADO (outcome assegurado→aprendizado→evidência→decisão melhor). F6 FECHADA — `statsWilson.wilsonInterval` (pura/determinística) dá banda de confiança honesta à taxa binária `worked/assured`: distingue "1/1" (banda larga) de "40/45" (estreita); `assuredStats` expõe `workedRate`/`interval`/`confidence`; n=0 → null (RN-EL-5); `test:effectiveness-interval` 16. **Metade INTERNA (F1–F6) FECHADA.** F7 FECHADA (abre a metade EXTERNA) — `ResearchResult` ganha `evidenceMode` (`model_knowledge`/`live`) + `sourceEvidence[]` (tier A/B/C + `retrievedAt`); Stub e LLM se rotulam `model_knowledge` (síntese paramétrica ≠ fonte viva §53/§54, RN-EI-1/6; fontes citadas → tier C, sem `retrievedAt`); `runResearch` persiste a procedência (observável); `test:research-provenance` 12. F8 FECHADA — `LiveSearchResearchProvider`: busca VIVA (`evidenceMode:'live'`, tier B + `retrievedAt`) atrás do MESMO contrato/registry (RN-EI-7); opt-in por `EXTERNAL_RESEARCH_SEARCH_URL`; SEM vendor → stub `model_knowledge` (honesto, não inventa live — RN-EI-6); budget/master-only via `runResearch`; `test:live-search-provider` 14. F9 FECHADA — gates de curadoria 2.0 no `assessQuality` (achado (e)): GROUNDING bloqueia `live` sem fonte (RN-EI-5); `model_knowledge` não exige fonte (0-regressão); avisos não-bloqueantes (`model_knowledge_only`/`low_source_diversity`/`stale_sources`/`high_churn_vs_prior` contradição-vs-base); `test:research-curation-gates` 11. F10 FECHADA — `tenantTerms` preenchido (achado (f)): `publish` colhe nome/CNPJ/telefone/e-mail do tenant de origem (`tenantTermsFor`) → `assertNoTenantData` LANÇA se o nome do tenant vazar pro compartilhado (§94/§129); sem org → `[]` (0-regressão); `test:anonymize-tenant-terms` 9. F11 FECHADA — `ResearchNeedService.detect`: temas ATIVOS do org (sinais abertos) × `vertical_intelligence` fresca → lacunas mapeadas na taxonomia `(vertical,topic,region,timeframe)`; prioriza por severidade; sem vertical → honesto; NÃO roda pesquisa (RN-EI-4); rota `GET /research-needs`; `test:research-need` 12. F12 FECHADA — `ContextualFusionService.fuse`: por tópico ALINHA interno + histórico assegurado + externo (com `evidenceMode`); força CATEGÓRICA strong/moderate/weak (assured+live→strong); NUNCA soma bases distintas (CA35/RN-EL-6); procedência + caveats; rota `GET /context-fusion`; `test:contextual-fusion` 14. F13 FECHADA — `LearningMetricsService.metrics`: KPIs DERIVADOS por query (status, procedência manual×assured, `assuredCoveragePct`, estados, `avgAssuredEffectiveness`, drift/dormância); null sem denominador (RN-EL-5); rota `GET /learning/metrics`; `test:learning-metrics` 11. F14 FECHADA — `test:enterprise-learning-hardening` (12) codifica RN-EL/RN-EI + runbook `docs/runbook/learning-operacao.md`; **FECHA o PRD 9**. Análise em `docs/prd/ANALISE-PRD9-vs-CODEBASE.md`; ADR em `docs/adr/ADR-166-enterprise-learning-external-intelligence.md`; runbook em `docs/runbook/learning-operacao.md`. Tese dupla: (1) o ZapFlow já APRENDE padrões (`PatternMemoryService`, genérico/determinístico) mas o aprendizado NÃO consome o resultado ASSEGURADO do PRD 8 — `recordOutcome:199` faz `acted+1` (`:207`) sem `event_key`, não-idempotente, e NENHUMA ligação `OutcomeAssurance→PatternMemory` existe (grep zero); (2) a inteligência externa (ADR-156/157 FECHADOS) já tem separação física compartilhado×por-org + anonimização + histórico + delta + agendamento, mas SINTETIZA PELO MODELO, não busca fonte viva (`LlmResearchProvider:90` via `chat()`; header `:11-19` confirma sem vendor de busca; `ResearchResult:30` sem `sourceEvidence`/`evidenceMode`/`retrievedAt`). O slot de conexão entre os dois mundos existe e está VAZIO: `EvidencePackageService.historicalEvidence[]` é `[]` sempre (`compose:114`) e o `DecisionEngine.analyze:57` não aplica `learningPrior`. Achados F0: (a) `recordOutcome` não-idempotente; (b) `assuredEffectiveness` NÃO EXISTE — `effectiveness=(worked*1+no_effect*0.5+backfired*0)/acted` (`:212`) conta qualquer `acted`, não só o assured; (c) estado `refuted` declarado (`db.ts:2110`) mas nunca atribuído; (d) research = conhecimento paramétrico; (e) `assessQuality:101` só barra vazio+baixa-confiança; (f) `sanitizeForShared` chamado com `tenantTerms=[]` (`VerticalIntelligenceService:115`). DONE≠EXEMPLO DE SUCESSO (§9/CA2): só `assured` (PRD 8) realimenta aprendizado forte. Aprendizado é EVIDÊNCIA, não ordem — entra como `learningPrior` pequeno/aditivo/explicável/reversível no pacote; o gate real segue RBAC/`ApprovalPolicyService`/Autonomy Contract. model_synthesis≠live (§53/§54): `evidenceMode` torna a distinção explícita. Cross-tenant learning FORA de escopo (§79) — tudo isolado por `organization_id`; única camada sem org é a inteligência de mercado anonimizada (ADR-156). Aditivo puro; NÃO cria EnterpriseMemoryEngine, segundo PatternMemory, segundo pipeline de pesquisa, novo Decision/Context Engine (§184/CA40). Reuso: `PatternMemoryService` (motor único), `OutcomeAssuranceService`(escada assured), `EvidencePackageService.historicalEvidence[]` (slot vazio pronto), `ResearchCuratorService.assessQuality` (estender gates), `researchAnonymize.assertNoTenantData` (barreira dura), `ExternalResearchProvider`/`REGISTRY` (busca viva atrás do mesmo contrato). Guardrails RN-EL-1..9 (DONE≠exemplo · evidência não ordem · determinístico antes de LLM · idempotência · null≠zero · fact/estimate/influenced nunca somados · isolamento/cross-tenant proibido · prior pequeno/reversível · motor único) + RN-EI-1..7 (model_synthesis≠live · query sem dado privado · camada compartilhada zero-org · provider só miss+opt-in+budget+L3+ · grounding obrigatório · não inventa fonte · pipeline único). Plano F0–F14 + 40 CAs. Pré-requisito do PRD 10 (Integração Final/Produção/Prova Comercial). Análise em `docs/prd/ANALISE-PRD9-vs-CODEBASE.md`; ADR em `docs/adr/ADR-166-enterprise-learning-external-intelligence.md`** |
| **ADR-167** | **Final Integration, Social Intelligence, Creative Execution & Commercial Proof (PRD 10)** | **FECHADO — F0–F18 em produção (19 fatias). Ciclo social ponta-a-ponta: informação externa→oportunidade contextualizada→conteúdo orientado+variantes A/B/C→publicação GOVERNADA→confirmação→resultado MEDIDO→garantia (PRD 8)→aprendizado forte (PRD 9)→superfície proativa. Aditivo puro (§42 — sem motor/scheduler/policy/confirmation/learning/pipeline paralelo, sem tabela de alerta paralela, sem 2ª tela de credenciais, sem 2º Estúdio/calendário). PUBLISHED≠RESULTADO. §47 provado ponta-a-ponta (Moda/Clínica/Restaurante) em `test:social-golden-paths`; guardrails RN-SI/RN-EI em `test:social-hardening`; production readiness em `test:social-intelligence-hardening`. Runbook em `docs/runbook/social-intelligence-operacao.md`. Detalhe por fatia: F0 (auditoria + matriz — doc-only). Tese: o "cérebro" do ZapFlow (percepção→contexto→evidência→decisão→governança→execução→confirmação→garantia→aprendizado, PRDs 0–9) e a infra de pesquisa externa / vertical intelligence / procedência / Scheduler / JobQueue / entitlements / RBAC / criptografia / OAuth / Admin Master JÁ EXISTEM e são reusáveis (~85% COMPOR/REUTILIZAR). O PRD 10 NÃO é motor de social media: é a camada final que LIGA essas peças às superfícies reais (Canais e IA + Estúdio) e preenche 3 lacunas de borda: (a) `SocialChannelProvider` unificado (contrato+registry+capabilities+OAuth seguro) espelhando `ReputationProvider.ts:134`; (b) Estúdio ORIENTADO POR INTELIGÊNCIA (Oportunidade→handoff→variantes A/B/C→calendário editorial→publicação governada); (c) closed-loop de conteúdo (analytics→Outcome Assurance→Creative Learning). Achados F0 (5 auditorias): Canais/WhatsApp/Instagram/OAuth/Encryption existem (só IG+WA publicam; sem FB-feed/TikTok/LinkedIn/YouTube/X→DEFERIR; sem ads→DEFERIR); Estúdio existe (`StudioService.ts:55`: marca→imagem/vídeo→legenda→publica IG) mas SEM variantes A/B/C, SEM calendário draft→approved, SEM best-time-to-post; toda a espinha inteligência/governança/execução (`BusinessSignalService`/`RadarService`/`ContextEngineService`/`EvidencePackageService`/`DecisionEngine`/`ApprovalPolicyService`(Autonomy Contract)/`CommandExecutorService`/`ConfirmationEngine`/`OutcomeAssuranceService`/`PatternMemoryService`/`ExecutionTraceService`) e a plataforma (`EntitlementService`/`PermissionService`/`platform_settings`/`OperationalHealthService`/`EncryptionService`/`auditLog`) EXISTEM. Publicação vira COMANDO GOVERNADO via `DecisionAction→ApprovalPolicy→CommandExecutor→ConfirmationEngine` (D4). Opportunity Matching publica em `business_signals` (D6, nunca tabela paralela). Inteligência social REUSA a pesquisa do PRD 9 (D5). `PUBLISHED ≠ RESULTADO` (RN-SI-03). Guardrails RN-SI-01..15. PROIBIDO (§42): Social Intelligence Engine paralelo · 2º Scheduler/JobQueue · 2º Decision/Learning/Approval Engine · tabela de alerta paralela (`social_alerts`) · 2º Estúdio · 2ª tela de credenciais · 2º calendário · integração frontend→API social · segredo OAuth no browser. Plano F0–F18 (fatia-por-PR; F3 escolhe UM provider real). F1 FECHADA — `SocialChannelProvider` (contrato+registry+capabilities DESCOBERTAS + estados de conexão §5 + `StubSocialChannelProvider` determinístico) espelhando `ReputationProvider`; degradação explícita (`manual_required`/`capability_unavailable`, RN-SI-06); publish/schedule idempotentes (RN-SI-08); analytics honestos (null≠0, RN-SI-12); sem db/rede/OAuth (F2); `test:social-channel-contract` 25. F2 FECHADA — `SocialConnectionService` (Social Connection Hub) estende Canais e IA com o ESTADO por-org da conexão de canal social (espelha `ReputationConnectorService`): tabela `social_connections` (aditiva, opt-in, UNIQUE(org,channel)) com credenciais CIFRADAS (`config_enc`/AES-GCM, RN-SI-05), estado observável §5 persistido do provider (token vencido nunca "connected"), capabilities DESCOBERTAS/cacheadas (RN-SI-06) + escopos; `status`/`list` REDIGIDOS (só `hasToken`/escopos/estado); `refreshHealth`/`providerFor`/`disconnect`; rotas `/api/social/connections*` (owner/admin); `test:social-connection-hub` 24. F3 FECHADA — `InstagramChannelProvider` (PRIMEIRO provider REAL) envolve o `InstagramService` já provado (Graph API); NÃO duplica OAuth/token (credencial fica no `channels`, sem 2ª tela §42); capability DESCOBERTA por estado de conexão (RN-SI-06 — desconectado→vazias+not_connected, determinístico em CI; conectado→getProfile/getPosts/publish + audience só com probe de insights); degrada honesto (publish/schedule sem capacidade→manual_required; getPostAnalytics DEFERIDO F4; ads DEFERIDO §4); publish idempotente in-memory (durável é da F11); Hub `providerFor` roteia opt-in (default stub, 0-regressão); `test:social-provider-instagram` 21. F4 FECHADA — `SocialAnalyticsService` (Social Analytics Ingestion) puxa posts próprios + analytics do provider e persiste snapshot por-org em `social_post_metrics` (aditiva, UNIQUE(org,channel,post)→upsert idempotente RN-SI-08); métrica ausente→NULL nunca 0 (RN-SI-12); `summary` por query com total null sem prova; `pass()` no `Scheduler.tick` horário (§42 sem 2º Scheduler), só conexões habilitadas de provider REAL; `InstagramChannelProvider.getPostAnalytics` real (media insights); rotas `/api/social/analytics/:channel[/sync]`; `test:social-analytics-sync` 18. F5 FECHADA — `CompetitiveIntelligenceProvider`+`CompetitiveIntelligenceService`: inteligência competitiva de fonte PÚBLICA/LEGAL (RN-SI-11) atrás do MESMO contrato `ExternalResearchProvider` do PRD 9 (D5/§42 — sem pipeline paralelo), registrado `competitive`; honesto (sem `COMPETITIVE_INTEL_SOURCE_URL`→model_knowledge, nunca inventa live RN-EI-6; grounding obrigatório RN-EI-5; query só da taxonomia); `gather()` delega ao `runResearch` (orçamento+anonimização+curadoria+persistência reusados)→`vertical_intelligence` com evidenceMode; rota master-only `POST /api/decision-intelligence/competitive-intelligence/gather`; `test:competitive-intelligence` 16. F6 FECHADA — `VerticalSocialIntelligenceService.assemble` CONSOLIDA read-only a inteligência social do nicho REUTILIZANDO (§42, RN-EI-4, sem motor/cache/budget/tabela novos): externo do pool COMPARTILHADO via `ResearchBrokerService` (opt-in+freshness+cache L2/L3, nunca pesquisa; inclui competitiva F5) com `evidenceMode` exposto + próprio via `SocialAnalyticsService` (F4); honesto (tópico sem fresca→available:false; sem analytics→own null; caveats explicam lacunas); rota `GET /api/social/vertical-intelligence`; `test:vertical-social-intelligence` 17. F7 FECHADA — `OpportunityMatchingService` (virada PERCEPÇÃO→AÇÃO) cruza F6 com o momento da org (vertical de `organization_settings` + desempenho próprio F4) e publica OPORTUNIDADE fresca na espinha (`business_signals`, D6/§42 — nunca tabela paralela); grounding (só inteligência fresca, não inventa), hipótese (basis, PUBLISHED≠RESULTADO), nunca inventa dinheiro (impact null), idempotente por dedupe_key `(vertical,topic,channel)`, TTL=validUntil; entra em `attention()`→decisão/governança; `pass()` no `Scheduler.tick`; rota `POST /api/social/opportunities/match`; `test:opportunity-matching` 17. F8 FECHADA — `StudioBriefService` (Studio Intelligence Handoff) transforma a OPORTUNIDADE (F7) num BRIEFING orientado pro Estúdio (nicho/tópico/ângulo/formato/procedência + `briefingText` pronto p/ `StudioService.generate`); ESTENDE o Estúdio (§42 — sem 2º); read-only/determinístico (sem LLM/tabela), grounded na evidência (não inventa), carrega `correlationId` (fio ADR-158), procedência `evidenceMode` + basis hipótese, marca opcional no tom, formato sugerido pelo desempenho próprio (F4); rotas `/api/social/studio/opportunities` e `/studio/brief/:signalId`; `test:studio-brief` 17. F9 FECHADA — `CreativeVariantService` deriva do briefing (F8) VARIANTES A/B/C (ângulos distintos: benefício/tendência/identidade), fecha o gap do Estúdio (1 arte→A/B/C); ESTENDE o Estúdio (§42 — sem 2º); read-only/determinístico (sem LLM/tabela), cada variante é um `briefingText` pronto p/ `generate`, grounded no brief; `variantKey` estável `{signalId}:{label}` + correlationId (fio ADR-158, escalona/atribui depois F10/F12); rota `GET /api/social/studio/variants/:signalId`; `test:creative-variants` 11. F10 FECHADA — `EditorialCalendarService` estende `scheduled_posts` (§42 — sem 2º calendário) com estágio editorial `draft`→`scheduled` (draft NÃO publica: o passe só pega `status='scheduled'`, 0-regressão) + best-time-to-post derivado do desempenho próprio (F4 `social_post_metrics`, agrupa por dow/hora, insufficient_data honesto sem ≥3 amostras); 3 colunas aditivas (channel/correlation_id/variant_key p/ atribuição F12); `draft`/`approve`/`cancel`/`calendar`/`bestTime`; rotas `/api/social/studio/calendar[/:id/approve]`+`/best-time`; `test:editorial-calendar` 16. F11 FECHADA — Governed Publishing: publicação vira COMANDO GOVERNADO (D4) via `DecisionAction→ApprovalPolicy(Autonomy Contract)→CommandExecutor→ConfirmationEngine` — nada publica direto; `SocialPublishCommandHandler` (commandType `social_publish`, registrado no MESMO registry, espelha `CollectionPlaybook` — §42 sem runtime paralelo) + `GovernedPublishService.propose` (semeia agent_policy sem ampliar autonomia; default policy 'single'=exige aprovação humana); IDEMPOTÊNCIA DURÁVEL (fecha gap F1/F3: idempotencyKey=action.id + guard action_already_executed); PUBLISHED≠RESULTADO arma `ConfirmationEngine.expect` (método social_publish→F12); degradação honesta (canal sem capacidade→LANÇA, nunca finge); rotas `POST /api/social/publish[/:actionId/execute]`; `test:governed-publishing` 16. F12 FECHADA — Analytics & Attribution FECHA o closed-loop (PUBLISHED→RESULTADO): `SocialAttributionService.resolvePending` casa a confirmação `social_publish` (F11) com o analytics do post (F4, por `external_ref`) e resolve via `ConfirmationEngine.confirm`→ação conclui+outcome→`OutcomeAssuranceService` enxerga a escada (§42/D6, sem medição paralela); honesto (sem analytics→pending; nunca inventa dinheiro, resultado=engajamento medido); `attribution()` liga variante→engajamento; `pass()` no Scheduler; rotas `POST /api/social/attribution/resolve`+`GET /attribution`; `test:social-attribution` 13. F13 FECHADA — Creative Learning FECHA percepção→…→APRENDIZADO: `CreativeLearningService.learnFromAction` realimenta o MOTOR ÚNICO (`PatternMemoryService`, §42/§184 — sem 2º motor) com a assinatura criativa (nicho×ângulo×canal×formato) da publicação ASSEGURADA (F12→Outcome Assurance); só `assured` ensina forte (RN-EL-1), desfecho determinístico do engajamento medido (RN-EL-3, nunca dinheiro), idempotente `creative:<actionId>` (RN-EL-4), opt-in `pattern_memory`; `ensurePattern` (upsert público mínimo) ancora o padrão, assinatura vira `pattern_type`→eficácia por combinação; `sweep`/`pass`(Scheduler)/`effectiveness`; rotas `POST /api/social/creative-learning/sweep`+`GET /creative-learning`; `test:creative-learning` 13. F14 FECHADA — Fala Tu + Radar + Proatividade: a fatia social entra nas superfícies proativas EXISTENTES (§42 — sem nova); oportunidade (F7) já flui pro `attention()`/Smart Inbox e publicação governada (F11) já aparece como aprovação pendente; `SocialProactivityService.digest` (read-only) COMPÕE oportunidades+aprovações pendentes+resultados medidos+o que funciona (F7/F11/F12/F13), humano; `shortSummary` lê `ev.note` p/ framing; quiet-hours/limiar seguem no `FalaTuProactiveService` (PRD 6); rota `GET /api/social/proactive`; `test:social-proactivity` 8. F15 FECHADA — Entitlements + Billing: gate de PLANO das capacidades sociais SERVER-SIDE (RN-SI-14 — esconder botão não é segurança); `SocialEntitlementService` compõe o `EntitlementService` canônico (§42 — sem plano paralelo) mapeando publish→módulo `estudio`; `assertAllowed` roda no CAMINHO real (rota `POST /api/social/publish` recusa 402+caminho de upgrade ANTES de propor — cliente que pule a UI é barrado); `status` carrega upgradeTargetPlan/addonPrice p/ CTA de billing; Master Admin bypassa; rota `GET /api/social/entitlement`; `test:social-entitlement` 10. F16 FECHADA — Reliability + Security Hardening: `test:social-hardening` (17) codifica os guardrails RN-SI/RN-EI como REGRESSÃO sobre os serviços reais F1–F15 (segredos cifrados/redigidos · capability descoberta/degrada · idempotência durável · competitiva não inventa live · grounding · null≠0 · PUBLISHED≠RESULTADO · gate server-side · §42 sem tabela/runtime paralelo + não inventa dinheiro · isolamento). F17 FECHADA — Commercial Proof: `test:social-golden-paths` (25) prova o §47 ponta-a-ponta pra Moda/Clínica/Restaurante (externo→oportunidade→conteúdo+variantes→publicação governada→confirmação→resultado medido→garantia assured→aprendizado forte→superfície proativa), compondo os serviços reais (§42 — nada novo). F18 FECHADA — Production Hardening + Runbook: runbook `docs/runbook/social-intelligence-operacao.md` (mapa dos serviços, rotas `/api/social/*`, fluxo, guardrails, conectar canal/adicionar provider, rollout OFF→shadow→single-tenant→approved→cohort→GA, troubleshooting) + `test:social-intelligence-hardening` (53 — production readiness: serviços importáveis, rota montada, passes no Scheduler, testes wired, runbook presente); FECHA o ADR-167 e o PRD 10. Critério de sucesso (§47): informação externa real → evidência → oportunidade contextualizada → conteúdo no Estúdio → aprovação → publicação em canal real → confirmação → resultado medido → aprendizado; 3 golden paths (Moda/Clínica/Restaurante). Rollout OFF→shadow→single-tenant→approved→cohort→GA; autonomous publishing nunca direto em GA. Análise em `docs/prd/ANALISE-PRD10-vs-CODEBASE.md`; ADR em `docs/adr/ADR-167-final-integration-social-intelligence.md`; runbook (F18) `docs/runbook/social-intelligence-operacao.md`** |
| **ADR-168** | **Content & Growth Intelligence Loop: Creative Intelligence, Experimentation & Business Attribution (PRD 11)** | **F0 ABERTA (doc-only) — auditoria + `docs/prd/ANALISE-PRD11-vs-CODEBASE.md` + ADR-168. Implementação (F1+) BLOQUEADA até F0 mergear (Regra Zero/§43). Tese: o PRD 10 já entregou o ciclo social ponta-a-ponta parando em ENGAJAMENTO; o PRD 11 aprofunda a INTELIGÊNCIA CRIATIVA (Brand DNA 2.0 estruturado, Hook, Roteiro, adaptação multicanal — lacunas de borda do PRD 10) e estende a ATRIBUIÇÃO além do like (conteúdo→lead→venda→receita→MARGEM) para a experimentação escolher o vencedor pelo RESULTADO DE NEGÓCIO. Regra fundante: `ENGAGEMENT ≠ BUSINESS VALUE` (como DONE≠RESULTADO/PUBLISHED≠RESULTADO). Achado F0 (2 auditorias): os DOIS motores de maior porte já existem e são generalizáveis por registry — (1) motor de experimento estatístico `ProspectResearchService`+`prospect_experiments*` (`twoProportionZ:135`, campeão/desafiante `:186`) + winner logic de `CollectionAbMeasurementService`/`SalesRecoveryAbMeasurementService`; (2) registry de atribuição a system-of-record `BusinessOutcomeResolver` (register-a-resolver `:143`, 4 resolvers SoR) + precedência de valor `SalesRecoveryAttributionService:15` (orders→quotes→avg, fact/estimate). ~80% COMPOR/REUTILIZAR. Lacunas reais: Brand DNA raso+2 stores soltos (`brand_profiles`+`brand_voice_context`), sem hook/roteiro/adaptação-canal, `SocialAttributionService` para em engajamento (`resultAmount:null` sempre), sem link conteúdo→produto, sem métrica de meta de crescimento (`BusinessGoalService` registry schema-free→+1 entrada). Produtos/estoque/custo existem (`products_services`/`inventory_items.avg_cost`); margem derivável (`price−avg_cost`). Guardrails RN-CG-01..12 (ENGAGEMENT≠BUSINESS VALUE · atribuição SoR nunca LLM · não inventa dinheiro · não plagia concorrente · isolamento cross-tenant · margem role-gated · winner exige min-sample · experimento não auto-executa · Brand DNA grounded · shadow-first · PUBLISHED≠RESULTADO · sem motor paralelo). PROIBIDO (§37): 2º Maestro/Scheduler/JobQueue/Decision/Learning/Approval/Confirmation/Outcome Engine · 2º motor de experimento · 2º mecanismo de atribuição · 2º Estúdio/calendário/CRM/meta · tabela de alerta paralela · 2ª tela de credenciais/OAuth · segredo no browser. Plano F0–F18 (F1 Brand DNA 2.0 · F2 Campaign Objective Contract · F3 Hook · F4 Script · F5 Channel Adaptation · F6 Creative Experiment Engine · F7 Content→Lead · F8 Lead→Sale→Revenue→Margin · F9 Objective-aware Winner · F10 Creative Learning 2.0 · F11 Product Opportunity · F12 Growth Goal · F13 Fala Tu Growth Brief · F14 +1 provider real · F15 Growth Autopilot shadow · F16 Governed optimization · F17 golden paths ENGAGEMENT≠BUSINESS VALUE · F18 hardening+runbook). F0 FECHADA (doc-only, PR #1073). F1 FECHADA — `BrandDnaService`: Brand DNA 2.0 (ESTENDER §37 — sem 2º store de marca). Identidade ESTRUTURADA (persona/público/posicionamento/proibições/do-don't) em colunas aditivas de `brand_profiles` + VERSIONAMENTO (`brand_dna_versions` + `restore` que aplica como NOVA versão, nunca rebobina o contador — convenção nº 9) + UNIFICAÇÃO da voz (a voz continua sendo `organization_settings.brand_voice_context`/ADR-155, fonte ÚNICA lida/escrita via `GrimoireService`, nunca duplicada) + GROUNDED (RN-CG-09: `save` só grava o patch passado, `get` devolve null/[] sem dado, `completeness` derivado RN-004). `StudioService.suggestCaption` passa a CONSUMIR o DNA unificado (voz+persona+posicionamento+proibições no prompt; 0-regressão sem dado). Rotas `/api/studio/brand-dna` (GET/PUT), `/brand-dna/versions[/:v]`, `POST /brand-dna/restore/:v`. `test:brand-dna` 40. F2 FECHADA — `CampaignObjectiveContractService`: Campaign Objective Contract (COMPOR §37 — sem 2º modelo de meta). Liga um OBJETIVO de campanha (`CAMPAIGN_OBJECTIVES` do Estúdio) a uma MÉTRICA DE META de negócio (`BusinessGoalService`: revenue/appointments) via `correlation_id` (`campaign:<id>`, fio ADR-158 que o conteúdo carrega → atribuição F9/F12). É AQUI que `ENGAGEMENT ≠ BUSINESS VALUE` (RN-CG-01) começa a valer: objetivos de VAIDADE (engajamento/alcance/educativo/data) ligam a `goalMetric=null` — o contrato é HONESTO que não há métrica de negócio direta, em vez de fingir que engajamento é resultado. `progress` compõe a distância-à-meta reusando `BusinessGoalService.progress`; GROUNDED (RN-CG-09 — métrica sem alvo definido pelo dono→`goalDefined:false`, nunca inventa alvo; objetivo/métrica desconhecidos rejeitados). Tabela aditiva `campaign_objective_contracts`. Rotas `/api/studio/campaign-objectives` + `/campaign-contracts[/:id[/progress|/cancel]]`. `test:campaign-objective` 24. F3 FECHADA — `HookIntelligenceService`: Hook Intelligence (ESTENDER §37 — sem 2º Estúdio). Gera GANCHOS de abertura (scroll-stopper) em 6 padrões distintos (pergunta/curiosidade/afirmação ousada/prova social/dor/identidade), grounded no TÓPICO + OBJETIVO de campanha (que ordena os padrões) + BRAND DNA da F1 (persona/público/voz). DETERMINÍSTICO (sem LLM, roda em CI — espelha `CreativeVariantService`). GROUNDED (RN-CG-09 — sem tópico erro; padrão identidade só sai com persona/público) + respeito à marca (RN-CG-04 — gancho contendo termo PROIBIDO do Brand DNA é filtrado, com caveat). Sem tabela nova. Rota `POST /api/studio/hooks`. `test:hook-intelligence` 17. F4 FECHADA — `ScriptIntelligenceService`: Script Intelligence (ESTENDER §37 — sem 2º Estúdio). Gera ROTEIRO/STORYBOARD de vídeo em 5 beats na ordem (Gancho→Contexto→Demonstração→Prova→CTA), cada beat com duração/visual/fala; o beat 1 REUSA o gancho da F3 (`HookIntelligenceService`, sem duplicar geração); a CTA vem do OBJETIVO de campanha; a duração de cada beat vem do FORMATO (reels/story/post). DETERMINÍSTICO (sem LLM, roda em CI). GROUNDED (RN-CG-09 — sem tópico erro; voz/persona/público entram só quando existem) + respeito à marca (RN-CG-04 — beat contendo termo PROIBIDO do Brand DNA é saneado + caveat). Sem tabela nova. Rota `POST /api/studio/script`. `test:script-intelligence` 22. F5 FECHADA — `ChannelAdaptationService`: Channel Adaptation (ESTENDER §37 — sem 2º Estúdio). O `SocialChannelProvider` (PRD 10) só abstrai TRANSPORTE; F5 acrescenta a camada de CONTEÚDO — adapta legenda (limite por canal), hashtags (faixa por canal), FORMATO (mapeia pro suportado), CTA idiomática e tom pros 6 canais (instagram/facebook/tiktok/linkedin/youtube/x). DETERMINÍSTICO (transform puro, sem LLM/DB). GROUNDED (RN-CG-09 — normas são tabela fixa/pública; canal desconhecido rejeitado; nunca inventa hashtag, só corta excesso). `changes[]` registra ajustes; `caveats[]` alerta (poucas hashtags, emoji fora de tom); `adaptMany` gera N canais + `skipped`. Sem tabela nova. Rotas `GET /api/studio/channels` + `POST /channel-adaptation`. `test:channel-adaptation` 19. F6 FECHADA — `CreativeExperimentService`: Creative Experiment Engine (COMPOR §37 — REUSA `ProspectResearchService.twoProportionZ`, NÃO cria 2º motor de experimento). Generaliza o motor de experimento de prospecção sobre VARIANTES DE CONTEÚDO: mede a taxa de ENGAJAMENTO de cada variante (`social_post_metrics` por `variant_key` aditivo) e decide o campeão determinístico (melhor × 2ª melhor, z de duas proporções ≥ confiança). RN-CG-07 (amostra mínima de impressões/variante; sem ela `insufficient_data` e mantém `running` — não decide no ruído); campeão/desafiante marca `is_champion`; RN-CG-08 (decidir NÃO executa — promover pra publicação é comando GOVERNADO F16, sem `decision_action` criada aqui). ATENÇÃO RN-CG-01: o vencedor AQUI é por ENGAJAMENTO (PROXY) — o vencedor por RESULTADO DE NEGÓCIO (lead/venda/receita/margem) é a F9, que se sobrepõe quando o desfecho existir. Tabelas aditivas `creative_experiments`/`creative_experiment_variants` + coluna `variant_key` em `social_post_metrics`. Rotas `/api/social/experiments` (POST/GET), `/:id`, `POST /:id/decide`. `test:creative-experiment` 21. F7 FECHADA — Content→Lead Attribution (1º elo do fio conteúdo→lead→venda→receita→margem). `ContentOutcomeResolver` (domínio `content`, `appliesTo` `social_publish`) entra no `BusinessOutcomeResolverRegistry` do PRD 8 (§37 — register-a-resolver, NÃO cria 2º mecanismo de atribuição) e pergunta ao system-of-record (`content_lead_attributions` por `correlation_id`, nunca ao LLM — RN-CG-02) se o conteúdo GEROU LEAD: ≥1 → `confirmed`, senão `not_confirmed` (RN-OA-2, não é falha), sem fio → `unknown`. `ContentLeadAttributionService.attribute` ESCREVE o vínculo (valida o contato existir na org — não inventa lead; idempotente por UNIQUE(org,correlation_id,contact_id) — RN-CG-03). Um lead é MAIS que engajamento (RN-CG-01) — o dinheiro (venda/receita/margem) é a F8. Tabela aditiva `content_lead_attributions`. Rotas `POST /api/social/attribution/lead` + `GET /attribution/leads`. `test:content-lead-attribution` 18. F8 FECHADA — Lead→Sale→Revenue→Margin: `ContentRevenueAttributionService` estende o fio até o DINHEIRO (completa `ENGAGEMENT ≠ BUSINESS VALUE` do like à margem). Pra cada lead do conteúdo resolve o valor da venda por PRECEDÊNCIA (espelha `SalesRecoveryAttributionService`, §37 — mesmo modelo, sem 2º mecanismo): `orders` pago→`fact` > `quotes` aceito→`estimate` > `contacts.avg_ticket`→`estimate` > nenhum→NÃO atribui (RN-CG-03: sem prova, sem valor). Margem = Σ(unit_price−unit_cost)·qty dos `order_items`; só `fact` quando TODO custo é conhecido (unit_cost>0), senão `null` (não inventa lucro). `revenue_basis`/`margin_basis` separam `fact` de `estimate` — NUNCA somados (reportados à parte). O `ContentOutcomeResolver` (F7) sobe pro estágio `sale` com `revenueFact`/`revenueEstimate` quando a venda existe. Dinheiro role-gated na rota (RN-CG-06). Tabela aditiva `content_sale_attributions` (UNIQUE(org,corr,contact) dedupe). Rotas `POST/GET /api/social/attribution/revenue`. `test:content-revenue-attribution` 17. F9 FECHADA — Objective-aware Winner: `CreativeExperimentService.decide` passa a PREFERIR o RESULTADO DE NEGÓCIO (receita `fact` via F8, senão leads via F7) ao engajamento quando ele EXISTE — é AQUI que `ENGAGEMENT ≠ BUSINESS VALUE` (RN-CG-01) vira operacional na DECISÃO, não só na medição. Cada variante ganha `correlation_id` (coluna aditiva em `creative_experiment_variants`) que liga às atribuições F7/F8; `outcomeFor` deriva receita fact/estimate + leads por variante. Ranqueia por (receita fact desc, leads desc) — dinheiro provado antes de lead; empate no negócio → `inconclusive` (não decide no ruído); SEM desfecho de negócio → cai pro ENGAJAMENTO (F6, 0-regressão — `test:creative-experiment` segue 21/21). `basis` (`business_outcome`|`engagement`) explícito no retorno + audit. Dinheiro role-gated. Rota `GET /api/social/experiments/:id/outcome`. `test:objective-aware-winner` 11. F10 FECHADA — Creative Learning 2.0 (ESTENDER §184 — motor único). `CreativeLearningService.learnFromAction` (PRD 10 F13) passa a CLASSIFICAR o desfecho (worked/no_effect) pelo RESULTADO DE NEGÓCIO — receita `fact` via F8, senão leads via F7, pelo `correlation_id` da ação — em vez do engajamento quando ele existe (RN-CG-01). Assim o MOTOR ÚNICO (`PatternMemoryService`) passa a aprender "que assinatura criativa VENDE/gera lead pra qual nicho", não "que assinatura engaja". Sem desfecho de negócio ainda → cai pro engajamento (PROXY, 0-regressão do F13 — `test:creative-learning` segue 13/13). `realizedImpact` segue = ENGAJAMENTO (unidade documentada; NUNCA mistura R$ com contagem — RN-CG-03); a ponderação por negócio vive na CLASSIFICAÇÃO, não no valor. `businessBasis` (`revenue`|`leads`|`engagement`) + `businessValue` no retorno + note. `test:creative-learning-2` 10. F11 FECHADA — Inventory/Product Opportunity (COMPOR §37 — sem tabela de alerta paralela). `ProductOpportunityService` cruza `products_services` × `inventory_items.avg_cost` × desempenho (`order_items`) pra achar o produto que MERECE conteúdo: em ESTOQUE, ALTA MARGEM (`price−avg_cost`), VENDENDO POUCO. Publica `product_opportunity` na espinha canônica (`business_signals`, D7 — NUNCA tabela paralela), de onde flui pro Estúdio (brief F8) e atenção. Dinheiro role-gated (RN-CG-06): `candidates()` (rota owner/admin) traz margem/custo ABSOLUTOS; o SINAL só carrega `marginBand` QUALITATIVO (high/medium) — nunca R$. `impactAmount=null` (RN-CG-03 — não promete receita); `basis='hypothesis'` (PUBLISHED≠RESULTADO); grounding (RN-CG-09 — sem custo conhecido/`avg_cost<=0` ou sem estoque → não vira oportunidade); idempotente por `dedupe_key` do produto. `pass()` no `Scheduler.tick` (orgs com estoque). Rotas `GET /api/social/product-opportunities` + `POST /product-opportunities/match`. `test:product-opportunity` 15. F12 FECHADA — Growth Goal metric + goal↔content (ESTENDER §37 — sem 2º modelo de meta). `BusinessGoalService.METRICS` (registro schema-free — o ponto de extensão que a auditoria F0 apontou) ganha `content_revenue` (BRL — só `fact` do `content_sale_attributions` no mês corrente, RN-CG-03 não mistura estimate) e `content_leads` (count do `content_lead_attributions` no mês), ambos DERIVADOS por query (RN-004). +1 entrada no registro faz a meta de negócio poder ser "o que o CONTEÚDO gerou" (receita/leads atribuídos) — a ponte goal↔content. O Campaign Objective Contract (F2) liga um objetivo a essas métricas e `progress` rastreia a distância-à-meta (fecha o fio objetivo→conteúdo→meta). Dinheiro role-gated (mesma gating de `revenue`). `test:growth-goal` 12 (regressão `test:business-goals` 22 + `test:campaign-objective` 24 verdes). Análise em `docs/prd/ANALISE-PRD11-vs-CODEBASE.md`; ADR em `docs/adr/ADR-168-content-growth-intelligence-loop.md`** |
| ADR-132 | Sinais finos | em produção |
| ADR-081 | Conectores TISS (roadmap TISS XML/WS) | roadmap — molde documentado; aditivo à ADR-145 |
| ADR-151 | FalaTu — captura multimodal "Fala → Faz → Confere" (porte do repo `Eldastito/FalaTu`) | Núcleo fechado (5 fatias) + F6 aditivo em produção: multi-tenant opt-in + WhatsApp no canal interno + compras com conferência + memória com desambiguação ativa ("qual Carlos?") + briefing diário proativo via `business_signals` + entrega do briefing por WhatsApp (digest, molde ADR-144) |
| **ADR-160** | **Onda A — Percepção & Contexto (ZapFlow Execution Intelligence)** | **FECHADO — 10 fatias (F1–F10) em produção. Aditivo sobre a Onda 0 (ADR-158 espinha única + ADR-159 governança). Quatro threads entregues: percepção transversal (F1 `attention`) · Context Engine (F2 snapshot como leitura default · F3 convergir Context+V2) · metas/distância à meta (F4) · Autonomy Contract (via ADR-159) · porta I/O (D5, F5–F9: Fala Tu vira porta pro domínio canônico — tarefas→`TaskService` · eventos→agenda `AppointmentService` com contato · listas→requisição `PurchaseRequisitionService` casada ao catálogo · paridade WhatsApp · dedup de RAG em `vectorSimilarity`) + reconciliação do dual-write + backfill (F10 `FalaTuBridgeReconService`). Todos opt-in por flag, reversíveis, atômicos com o silo, nunca inventam (RN-151); silos preservados em dual-write — aposentadoria futura quando a reconciliação reportar `ready`** |

## ADR-145 — CLÍNICA JORNADA DE TRATAMENTO (fechado 2026-08-01)

Cliente: clínica multiespecialidade com salas compartilhadas, terapia em grupo, ciclos renováveis de 10 sessões, alta pelo médico, guia emitida pela recepção. Antes da ADR-145 o cliente operava com gambiarras (`force=true` pra grupo, "esquecer" paciente pra fingir alta, recadastrar pra outra especialidade). Depois da ADR-145 tudo é primeira classe.

**5 fases entregues em 15 fatias / 15 PRs / 0 breaking changes:**

- **Fase 1 — Fundação normalizada** (F35–F36): `clinic_specialties` + N:N `clinic_professional_specialties` + `clinic_care_episodes` longitudinal com unique parcial (1 episódio ativo por paciente/especialidade).
- **Fase 2 — Ciclos + PIN** (F37–F40): aditivos em `appointments` (care_episode_id, treatment_cycle_id, specialty_id, schedule_session_id); `clinic_treatment_cycles` renováveis com **saldo derivado por query (RN-004, nunca contador mutável)**; alta com PIN do médico (RN-007, reusa Fase 28); métricas + fila operacional.
- **Fase 3 — Grupo primeira classe** (F41–F43): `clinic_schedule_sessions`; RN-006 (5 pacientes em grupo = 1 ocupação do profissional, não 5); portal do paciente sem vazar outros participantes (RN-013 §3).
- **Fase 4 — Guia da recepção** (F44–F46): `clinical_guides` polimorfo (3 tipos: TISS + encaminhamento + pedido médico) com numeração série própria + snapshot canônico Fase 29 + PDF por tipo + envio HMAC + LGPD + integração bidirecional guide↔cycle (RN-005 §8: ciclo pending_authorization até guia emitida).
- **Fase 5 — IA operacional** (F47–F48): `ClinicScheduleSessionService.availability` (sugere 3 horários do MESMO profissional); `ClinicRenewalTaskService` publica sinais no `business_signals`; `ClinicGuideService.draft` pré-preenche rascunho de guia com o que existe e marca `missing:true` no que falta.

**Guardrails RN-014 duros** (documentados no header dos services + testados):

A IA operacional **nunca**:
- Sugere outro profissional (RN-003 profissional fixo).
- Renova ciclo (recepção decide + humano confirma).
- Dá alta (RN-007, alta é do médico com PIN).
- Emite guia sem PIN (F44/46).
- Inventa TUSS, carteirinha, `authorizationNumber` ou `validUntil` (F48).
- Herda `referralReason` de encaminhamento anterior (motivo é sempre novo).
- Fabrica lista de itens em pedido médico (F48).

**Retrocompatibilidade 100%** — appointments legados sem episódio/ciclo/sessão continuam operando como consulta avulsa. Migração puramente aditiva.

**Números:** 6 tabelas novas + 6 aditivos + 7 services novos + ~35 rotas + 13 scripts de teste (todos PASS 100% na CI matrix).

## Decision Intelligence — consolidação transversal (fechado 2026-08-08)

Origem: PRD "ZapFlow Decision Intelligence Fabric 2.0". A Fase 0 (análise comparativa, `docs/decision-intelligence/ANALISE-COMPARATIVA-PRD-vs-REPO.md`) provou que ~85% já existia — então a consolidação é **aditiva sobre ADR-135 (Snapshot/Evidence) + ADR-136 (Decision & Action Ledger) + ADR-152 (Runtime)**, **sem** abrir módulo/motor paralelo e **sem** duplicar alertas/tarefas/scheduler/memória. Plano e status em `docs/decision-intelligence/PLANO-E-FATIAS.md`.

**Fatias entregues (todas determinísticas — rodam em CI sem chave de IA):**

- **DI-1** — Classificação de impacto **L0–L4** + perfil de análise (roteador de profundidade) em `ImpactPrioritizationService.levelFor` (aditivo, campos extras no Pareto); **Evidence Package v1** (`EvidencePackageService`) — embrulha o Snapshot V2 num pacote cacheável (`evidence_packages`, opt-in `evidence_layer_enabled`) com freshness/confidence/sources.
- **DI-2** — `DecisionEngine.analyze({ mode })`: **Pre-Mortem / Red Team / Advocate como MODOS** (não agentes, não motor novo — PRD §13/§37), + banda de cenários (`DecisionSimulatorService.scenarios`) + `decision_risks`/`DecisionRiskService` (riscos previstos publicam em `business_signals`, convenção nº 12). Síntese **advisória** — o gate real segue no RBAC/`ApprovalPolicyService` (RN §35).
- **DI-3** — `DecisionMetricsService`: loop fechado com **valor protegido** (PRD §36), acurácia de previsão, materialização de risco, aceitação de recomendações e cache hit-rate (`evidence_cache_events`) — tudo **derivado por query** (RN-004).
- **DI-4 (External Intelligence, ADR-156)** — inteligência de vertical **compartilhada e anonimizada**. Duas camadas fisicamente separadas: `vertical_intelligence` (COMPARTILHADA, **sem `organization_id`**, só pesquisa do mundo externo) vs `organization_contextualization` (POR-ORG, isolada). Escrita **só pelo admin master** (D5); tenant é **read-only** (`ResearchBrokerService`, nunca chama provider). Filtro de anonimização (`researchAnonymize`) barra PII/id de tenant antes de gravar. Provider **manual** (`runManual` — admin cola; `ExternalResearchProvider` fica pronto p/ web-search futuro). Orçamento de pesquisa de **plataforma** (`ResearchBudgetService`, `research_usage_log`). Lembrete **semanal** (`VerticalIntelligenceReminderService` no Scheduler → `business_signals`, nunca roda pesquisa sozinho).
- **UI** — sem inflar o menu do lojista (PRD §31): painel master **"Inteligência de Nicho"** (`NicheIntelligenceView`), card **"Valor protegido"** na Central de Saúde e aba **"Analisar decisão"** no Diretor IA (`ExecutiveView`).

**Guardrails duros (RN-156, no header dos services + testados):** camada compartilhada carrega **zero** dado por-org/pessoal; query externa deriva só de `(vertical, topic, region, timeframe)`; provider só no miss + opt-in + budget + L3+; ADR-156 supersede a ADR-079 D4 **apenas** para inteligência externa de mercado (`prospect_learning_memory` segue por-tenant).

**Rotas:** `/api/decision-intelligence/*` (evidence, priorities, analyze, risks, metrics, vertical-intelligence[/manual/run], external-evidence, research-budget, research-refresh-due). Testes: `scripts/test-decision-intelligence-*.ts` (di1..di4-reminder).

## Convenções críticas (não regredir)

1. **Isolamento multi-tenant** — TODA query filtra `organization_id`. Toda função service recebe `orgId` como 1º arg. Cross-tenant é bug de segurança.
2. **CREATE-then-ALTER estrito em `db.ts`** — bug histórico das Fases L/T/U. Nunca reorderar; sempre append no fim.
3. **Snapshot canônico + `computeDocumentHash`** — docs emitidos (prescrição/atestado/recibo/guia) congelam snapshot (Fase 29); renomear paciente/negócio depois NÃO altera doc. Hash recursivo canonicaliza JSON.
4. **HMAC signed URLs** — segredo derivado `sha256(JWT_SECRET:{scope}_v1)`, TTL 15min, `timingSafeEqual`, basename-only, `X-Content-Type-Options: nosniff`. Padrão Fases K/18/33/45.
5. **PIN com lockout** — `verifyPin` reusa `timingSafeEqual` + 5 tentativas / 15min. Fase 28.
6. **LGPD** — `dados_sensiveis` (Art.11) pra leitura clínica + `comunicacoes` (Art.7) pra envio. `maskIdentifier` em audit metadata (Fase 32).
7. **Best-effort services** (deliveries, notifications) — nunca throw pro caller; dedup via unique index + try/catch `SQLITE_CONSTRAINT_UNIQUE`.
8. **Race conditions** — transação atômica com SELECT COUNT dentro da tx antes do INSERT (padrão AC-012 da Fatia 41).
9. **Retenção CFM 20 anos** — nunca `DELETE`. Cancelamento é UPDATE status='cancelled' (preserva histórico).
10. **Feature flags opt-in** — `organization_settings.{modulo}_{feature}_enabled INTEGER DEFAULT 0`.
11. **Import dinâmico** pra quebrar ciclos entre services: `import("./X.js").then(m => m.X.method(...))`.
12. **BusinessSignal** (ADR-136) — quando um detector precisa alertar a operação, publica em `business_signals` com `dedupe_key` — nunca cria tabela própria de "alertas".

## Fluxo padrão de uma fatia

1. Ler ADR do módulo (`docs/adr/ADR-XXX.md`).
2. Implementar service em `src/server/`. Comentários em PT-BR no header explicando decisões e regras (RN-xxx).
3. Rota em `src/server/routes/`. `AuthRequest`, `requireRole` quando aplicável, `actor(req)` no audit.
4. Teste em `scripts/test-<nome>.ts` — sempre com `tmpDir` isolado, `check(name, ok)` helper, cobre happy path + edge cases + audit + isolamento multi-tenant.
5. Wiring: `package.json` scripts, `.github/workflows/ci.yml` matrix, atualizar seção "Status" do ADR marcando fatia como MERGED.
6. `git commit` com corpo detalhado (a auditoria de features roda no `git log`), push, PR draft.
7. Subscribe PR + esperar CI verde + merge.
8. Reset --hard origin/main + próxima fatia.

## O que evitar

- Comentários que só descrevem o `WHAT` do código — só documente `WHY` não óbvio (invariante, workaround, restrição regulatória).
- Contador mutável no lugar de query derivada. Se surgir necessidade, reveja se é mesmo derivável (RN-004 sempre foi).
- Backward-compat "shims" quando dá pra só editar o código. Aditivo em DB é diferente — é obrigatório (compat com dados existentes).
- Colocar validação em ambos service + rota. Rota valida forma (schema); service valida invariante de negócio.
- Reusar `store_id` como `id`. Sempre `randomUUID()`.
