# ADR-080 — Módulo Clínica + Quick-Start Saúde / Clínica 2.0

**Status:** Aceito (implementação em fases A–E, ver plano abaixo).

**Origem:** PRD "Quick-Start Saúde / Clínica 2.0 / 2.1" (Agenda Clínica Inteligente + Convênios + Autorização de Procedimentos), a partir de dores reais de uma clínica: sistema de agenda que apaga o paciente quando excede o tempo, ausência de aviso "o paciente continuará?", impossibilidade de editar o plano sem apagar e recriar o paciente, e a necessidade de operar com muitos profissionais (agenda por link, impressão manual). O PRD propõe transformar o Quick-Start Saúde num setup operacional de clínica.

---

## Contexto

### O que o inventário mostrou (antes de escrever código)

O PRD assume que "a fundação técnica já existe" e trata a evolução como `ALTER TABLE` + um retoque no pack Saúde. O código conta outra história:

- **Quick-Start / `OnboardingTemplateService`**: só sabe semear **áreas, cadências, automações (flags em `organization_settings`) e FAQ→RAG**, idempotente por nome. Não cria telas, ficha de paciente nem fluxo de autorização. A rota `/api/quickstart/apply` **não tem RBAC** — qualquer usuário autenticado aplica um pack. O pack `saude` hoje: 2 áreas (Recepção, Pós-consulta), 2 cadências (`agendado`, `entregue_concluido`), automações genéricas, 1 FAQ.
- **Agenda**: é uma **lista simples** (`AgendaView.tsx`), não um agendador. Sem grade dia/semana, sem colunas por profissional, sem filtros, sem impressão. `appointments.assigned_to` **está morto** (nunca é escrito; o `PATCH` nem o expõe). **Não existe "sala"**. Duração é **global da org** (`agenda_slot_minutes`), não por consulta. Não há check-in/início/fim. Conflito só por **capacidade agregada da org** (`AppointmentService.isFree`), e a criação nem valida.
- **Cadências**: `trigger_stage` é uma **string acoplada ao `stage` do ticket**. Os gatilhos clínicos do PRD (`autorizacao_pendente`, `autorizacao_aprovada`, `documentacao_pendente`, `retorno_recomendado`) não são estágios de ticket — cadastrar a cadência **não a faz disparar** sem código no ponto de transição.
- **Infra transversal pronta e reaproveitável**: `EncryptionService.encrypt/decrypt/hash` (ADR-054) para credenciais de operadora; `logAuthEvent` → `auth_audit_logs` para auditoria; padrão de portal público por token aleatório + SHA-256 + expiração (`RadarPublicService`) para o portal do profissional; `ReportPdfService` (pdfkit) para impressão; **socket.io** (salas `org:`) para tempo real.
- **TISS / TUSS / convênio / autorização de procedimento**: **greenfield total** — nada existe.
- **Roles**: `owner`/`admin`/`agent`. Não existe tipo "profissional". A agenda está atrás do módulo `agenda`, que a vertical `saude` já habilita.

### Restrição de produto

Clínica lida com **dado sensível de saúde** (LGPD Art. 11): CPF, carteirinha, plano, procedimento. Isso eleva o rigor de isolamento, criptografia, auditoria de leitura/exportação e acesso por token sem dado em URL.

## Decisão

### D1. Um módulo `clinica` novo; o Quick-Start é o interruptor + a semente, não o dono da funcionalidade

O `OnboardingTemplateService` **não** vira o lar da agenda/ficha/autorização. Criamos um **módulo opcional `clinica`** (registrado em `OPTIONAL_MODULES`, `ModuleService.MODULE_BY_ROUTE`, e no preset da vertical `saude`), gated como os demais. O **Quick-Start Saúde 2.0** passa a **ativar o módulo e semear o conteúdo inicial** (áreas, cadências, FAQ, flags `clinic_*`). A funcionalidade (agenda clínica, ficha, autorização) vive no módulo, com suas tabelas, rotas e telas.

### D2. Profissional é entidade própria (`clinic_professionals`), desacoplada de login

Clínica tem muitos profissionais, a maioria **sem** conta no painel. Criamos `clinic_professionals` (nome, especialidade, cor, ativo) **desacoplada de `users`**, com **link opcional** para um `user` quando o profissional precisar do portal. Não reusar `users` (evita gastar `users_limit` do plano e misturar quem opera o sistema com quem só aparece na agenda). `appointments.assigned_to` (morto) é substituído por `professional_id` referenciando essa tabela, com snapshot de nome.

### D3. Nunca apagar por tempo; alerta de permanência derivado no cliente

O ZappFlow **já** é seguro quanto a "apagar paciente por tempo excedido" — nenhum job toca o ciclo do agendamento por tempo. O trabalho é **duração por consulta** (coluna nova, sem teto de 150 min) + status `over_time` (nunca exclusão). O **alerta visual** ("faltam 15 min / excedeu") é **calculado no navegador** a partir de `scheduled_end` num timer local — **sem job no Scheduler**. O socket.io só empurra mudanças quando outra recepção faz check-in/estende. A cadência de WhatsApp de autorização é assunto separado.

### D4. Autorização assistida primeiro; TISS depois, em ADR próprio

O MVP de convênios é **autorização assistida/manual**: `procedure_authorization_requests` (registro + máquina de status + checklist de documentos + acompanhamento de protocolo), operadoras e procedimentos/TUSS cadastrados manualmente. **Sem** XML/WebService/API no começo. Credenciais de operadora cifradas com `EncryptionService`. Conectores TISS (XML/WebService/API por operadora, com certificado digital A1/A3) ficam para um ADR futuro — é o Nível 2/3 do gateway do PRD, com peso de segurança próprio. Promessa comercial honesta: "centralizamos e preparamos a autorização; onde houver integração, enviamos; onde não, deixamos pronto para envio manual".

### D5. Gatilhos clínicos precisam de ponto de disparo, não só cadastro

As cadências novas (`documentacao_pendente`, `autorizacao_pendente`, `autorizacao_aprovada`, `autorizacao_negada`, `retorno_recomendado`) exigem **código** que, na transição de status da autorização/documentação, chame `CadenceService.startForTicket(...)` com o gatilho. O dropdown `STAGE_LABELS` (`CadencesView.tsx`), hoje dessincronizado dos estágios reais, é atualizado para expor os novos gatilhos.

### D6. Snapshot de plano é imutável

`appointments.patient_plan_snapshot` **congela** o plano no momento da autorização (auditoria do que foi cobrado). A troca de plano atualiza a **ficha** (`patient_profiles`, fonte da verdade) e registra `patient_plan_history`; agendamentos futuros leem o plano atual até serem autorizados, quando congelam. O snapshot nunca "muta" retroativamente.

### D7. Guardrails de IA e dado sensível

A IA pode coletar dados, listar documentos necessários, identificar pendências, resumir status e preparar a solicitação para revisão. A IA **não** dá diagnóstico, não interpreta exame, não promete cobertura/autorização, não inventa TUSS e **não envia autorização sem revisão humana no MVP**. Ficha em tabela satélite; credenciais cifradas; portal por token (sem dado sensível em URL); auditoria em criação/edição/troca de plano/autorização/check-in/extensão/finalização/impressão-exportação.

### D8. RBAC no Quick-Start (corrige bug pré-existente)

`/api/quickstart/apply` passa a exigir `requireRole("owner","admin")` — aplicar um setup clínico (que sobrescreve automações e semeia áreas) não pode ficar aberto a qualquer `agent`.

### D9. Plano de implementação em fases (substitui o "tudo de uma vez" do PRD)

- **Fase A — Pack Saúde 2.0 (pequena, valor imediato):** 4–5 áreas com persona (Recepção Clínica, Convênios e Autorizações, Pós-consulta/Retorno, Financeiro/Particular, Coordenação de Agenda), 7 cadências (textos), FAQ ampliado, flags `clinic_*` em `organization_settings`, RBAC do Quick-Start, sincronização do `STAGE_LABELS`. Idempotência mantida (não duplica por nome; automações sobrescrevem flags, nunca dados clínicos).
- **Fase B — Ficha do Paciente:** `patient_profiles` + `patient_plan_history`; editar plano/convênio sem apagar contato nem agendamento; histórico e auditoria. Mata a dor mais citada.
- **Fase C — Agenda Clínica (o item grande):** `clinic_professionals`; duração por consulta (sem teto, opção "sem previsão"); check-in/início/`over_time` (nunca excluir); alerta de permanência client-side; conflito por profissional/sala.
- **Fase D — Multi-profissional e operação:** grade/colunas por profissional, filtros (profissional, sala, status, plano, data), impressão/PDF (via `ReportPdfService`), portal do profissional por token (molde `RadarPublicService`).
- **Fase E — Autorização assistida:** operadoras, procedimentos/TUSS (cadastro manual), `procedure_authorization_requests` com status/checklist, dispatch das cadências clínicas, guardrails de IA.
- **Fase F (futuro, ADR próprio):** conectores TISS XML/WebService/API por operadora.
- **Fase G — Prontuário/SOAP (extensão 2026-07):** `clinical_encounters` (uma linha por consulta, `UNIQUE(org, appointment_id)`) com campos SOAP explícitos (Subjetivo/Objetivo/Avaliação/Plano) + `form_data JSON` extensível (Fatia 1b vai definir schemas por especialidade a partir de fichas em papel enviadas por foto); versionamento em `clinical_encounter_history` (diff campo-a-campo, padrão `product_edit_history`); LGPD Art.11 obrigatório (`hasConsent('dados_sensiveis')` bloqueia `open()`/`update()` com código `LGPD_CONSENT_REQUIRED`); `startCare()` da agenda tenta abrir encounter *best-effort* (sem travar o atendimento se faltar consentimento); `finalize()` congela em `signed` — updates novos são bloqueados no service (addendum fica pra próxima fatia). Rotas: `GET/POST /appointments/:id/encounter`, `PATCH /encounters/:id`, `POST /encounters/:id/finalize`, `GET /encounters/:id/history`, `GET /patients/:contactId/encounters`, `POST /patients/:contactId/consent/sensitive`. UI: botão "Prontuário" no card do appointment (durante/após atendimento); modal SOAP com 4 abas + área de campos extensíveis (Ficha). Teste: `test:clinic-encounter` (26 verificações — LGPD block, open idempotente, SOAP + form_data, history diff, finalize idempotente, update bloqueado após signed, histórico do paciente ordenado, isolamento, auditoria).
- **Fase 23 — Catálogo CID-10 com busca (extensão 2026-07):** fecha o buraco de qualidade do campo `cid` no atestado (Fase H) — hoje é texto livre, então a mesma condição vira "H10.9", "H10", "H109", "conjuntivite" — impossível auditar/agregar. Nova tabela GLOBAL (não por org, CID-10 é padrão OMS/DATASUS mesmo pra todas as clínicas): `cid10_codes` (code PRIMARY KEY, description, chapter, created_at) + índice de descrição. `Cid10Service`: `seed()` idempotente lazy (chamado no 1º `get`/`search`, flag booleana evita re-execução) com ~50 códigos comuns curados de atestado geral (viroses J00/J06/J20, gastro A09/K52, cefaleia G43/R51, ortopedia M25/M54/S93, oftalmo H10/H52, otorrino H65/J30, dermato L20/L23/B02, gineco N39/N76/N94, cardio I10, saúde mental F32/F41/F43/G47, endócrino E03/E11, sintomas R50/R53, Z76.3 acompanhamento). `normalizeCode(input)` aceita variações do usuário ("h109", "H10.9", "  h 10.9  ") e devolve formato canônico ("H10.9") — usa PRIMARY KEY, garantindo lookup determinístico. `get(code)` normaliza + SELECT; `search(query, limit)` faz UMA query com CASE de precedência: (0) exact-code match, (1) prefix-code, (2) description contains — ordena por rank ASC + code ASC, limit default 20 / faixa 1..100. Query vazia devolve `[]`. **Integração `ClinicDocumentsService`**: `createCertificate` normaliza `cid` do input pro formato canônico; se `cidDescription` NÃO veio explícita e `cid` bate no catálogo, auto-preenche `cid_description` do catálogo (fonte única). Descrição explícita do usuário SEMPRE vence (permite override pra clínicas com nomenclatura própria). CID fora do catálogo passa livre — **não é policy**, é AJUDA. Mesmo tratamento em `updateCertificate`: patch `cid` sem `cidDescription` puxa do catálogo; patch `cidDescription` explícito respeitado. Rotas: `GET /clinic/cid10/search?q=&limit=` (autenticado — user autenticado do org, sem RBAC especial), `GET /clinic/cid10/:code` (404 se desconhecido). Sem alteração no PDF do atestado (Fase H) — snapshot já usa `cid_description` gravado, agora só melhor preenchido. Teste: `test:clinic-cid10` (36 verificações — normalizeCode 5 variações, seed idempotente, `get` normalizando ("h109" == "H10.9"), search empty/exact/prefix/description case-insensitive, ordenação exact>prefix, limit clip + clamp máx 100, integração createCertificate 4 casos (CID conhecido auto-preenche; explícito sobrepõe; desconhecido preserva null; sem CID null), updateCertificate 3 casos, isolamento clínico intacto). Regressão OK: `clinic-encounter` (26/26), `clinic-documents` (31/31), `clinic-signature-visible` (31/31), `clinic-signature-pin` (29/29), `clinic-consent-reads` (46/46), `clinic-timeline` (39/39), `clinic-absence` (35/35). CI matrix ganhou `clinic-cid10`.
- **Fase 22 — Bloqueio de agenda por indisponibilidade do profissional (extensão 2026-07):** profissional marca férias/congresso/atestado/outro; `ClinicAgendaService.createAppointment` recusa slot que se sobrepõe. Antes desta fatia a recepção só sabia da ausência oralmente (WhatsApp da secretária), agendava por engano dentro do período, e o paciente descobria no dia. Nova tabela `clinic_professional_absences` (id, org, professional_id, starts_at, ends_at, reason `vacation|conference|sick_leave|other`, notes, created_by, created_at) + índice `(org, professional_id, starts_at, ends_at)`. `ClinicProfessionalAbsenceService`: `create(orgId, professionalId, {startsAt, endsAt, reason, notes})` — valida `endsAt > startsAt` (`ABSENCE_INVALID_RANGE`), reason ∈ whitelist (`ABSENCE_INVALID_REASON`), profissional existe no org, notes trim + clip 500 chars; `list(orgId, {professionalId?, activeAt?, from?, to?, limit?})` — ordenado `starts_at DESC`, `activeAt` filtra ativas no timestamp (starts <= t < ends, borda direita aberta), `from`/`to` filtra sobreposição com a janela pedida, `limit` default 100 max 500; `overlaps(orgId, professionalId, startMs, endMs)` — devolve a Absence sobreposta ou null (regra `abs.starts < endMs AND abs.ends > startMs`, janela aberta na direita — appt que começa exatamente no `ends` da ausência PASSA); `remove(orgId, absenceId)` — DELETE row (não é dado clínico, retenção não se aplica). Múltiplas ausências parciais podem coexistir (manhã em congresso + tarde em folga no mesmo dia). Timezone-agnóstico: comparação em ms via `Date.parse` (ISO 8601 preserva ordem). **Gate em `ClinicAgendaService.createAppointment`**: se `professionalId` está no input E não é `force:true`, chama `overlaps`; se casa, lança `PROFESSIONAL_UNAVAILABLE` com payload `absence` (permite UI mostrar "Dra. Ana em férias 10/09-20/09"); mesmo padrão do CONFLICT — `force:true` bypassa. Sem `professionalId` NÃO bloqueia (ausência é por profissional). Rotas: `GET /clinic/professionals/:id/absences?activeAt=&from=&to=&limit=` (autenticado); `POST /clinic/professionals/:id/absences` (owner|admin) com `{startsAt, endsAt, reason, notes?}` → 400 nos códigos de validação; `DELETE /clinic/absences/:id` (owner|admin); rota `POST /clinic/appointments` ganhou tratamento de 409 `PROFESSIONAL_UNAVAILABLE` (paralelo a `CONFLICT`) com `{error, code, absence}`. **Nunca APAGA appointments existentes** — se recepção cria ausência com consultas já agendadas, elas permanecem; gestor decide caso a caso (cancel/reschedule via fluxos existentes). Auditoria `CLINIC_ABSENCE_CREATED`/`CLINIC_ABSENCE_REMOVED` com `{absenceId, professionalId, reason, startsAt, endsAt}`. Teste: `test:clinic-absence` (35 verificações — create OK 4 reasons, validações (range/reason/prof-inexistente/notes trim+clip 500), overlaps 6 cenários (before-only, inside, wrap, after-only, borda em ends, outro prof), createAppointment dentro → 409 PROFESSIONAL_UNAVAILABLE com payload absence, force:true bypassa, sem professionalId não bloqueia, outro profissional na mesma janela OK, fora da janela OK, remove libera imediato, appts pré-existentes preservados após criar ausência (integridade), list com filtros professionalId/activeAt/from-to/limit, isolamento multi-tenant, auditoria com metadata). Regressão OK: `clinic-encounter` (26/26), `clinic-addendum` (27/27), `clinic-followup` (27/27), `clinic-reschedule` (42/42), `clinic-vacancy` (30/30), `clinic-timeline` (39/39). CI matrix ganhou `clinic-absence`.
- **Fase 21 — Timeline unificada do paciente (extensão 2026-07):** fecha o lado "gestor consulta rápida" — hoje pra reconstruir a história de um paciente o profissional precisa abrir 4 abas (encounters, receitas, atestados, anexos, envios), cruzar timestamps na cabeça, e ainda perde o rastro de addendums pós-assinatura (Fase 20). A timeline consolida tudo numa cronologia DESC única. **Sem tabela nova** — read-only sobre `appointments`, `clinical_encounters`, `clinical_encounter_addendums`, `clinical_prescriptions`, `clinical_medical_certificates`, `clinical_encounter_attachments`, `clinical_document_deliveries`. `ClinicPatientTimelineService.getTimeline(orgId, contactId, {from?, to?, limit?, kinds?})` roda 7 SELECTs isolados por (org, contact), mescla em memória, filtra janela + kinds, ordena DESC por `at` com tiebreaker narrativo (ordem de KIND_ORDER: `appointment_scheduled` < `encounter_opened` < `prescription_issued` < `certificate_issued` < `attachment_added` < `document_sent` < `encounter_signed` < `encounter_addendum` — dentro do mesmo timestamp, o evento "mais tardio na narrativa" vem primeiro no DESC), clipa em `limit` (default 100, faixa 1..500) e devolve `{contactId, count, totalBeforeLimit, items[]}`. Cada `TimelineItem` carrega `{kind, at, encounterId, appointmentId, refId, actorId, actorName, summary}` — `refId` é id do doc/addendum/anexo/delivery quando aplicável (permite drill-down na UI); `summary` é linha curta em pt-BR pronta pra render ("Receita emitida (assinada com PIN)", "Anexo: raio-X.png", "Adendo: Resultado do hemograma...", "Consulta inicial (cancelado)"); `at` sempre ISO string do timestamp original. **Semântica de docs**: só `status='issued'` entram (rascunho é privado do profissional). **Semântica de deliveries**: só `status='sent'` — falhas vivem no endpoint dedicado. **Semântica de anexos purgados** (Fase U): row aparece com sufixo `[arquivo purgado]` no summary — auditoria diz que existiu. LGPD Art.11 obrigatório (`LGPD_CONSENT_REQUIRED`): `requireConsent(orgId, contactId)` antes de qualquer SELECT — mesmo comportamento de `listByPatient` da Fase 19 (contact inexistente/cross-tenant → hasConsent devolve false → 403, consistente com "nada a expor sobre id que não existe"). Rota: `GET /api/clinic/patients/:contactId/timeline?from=&to=&limit=&kinds=` (owner|admin|agent — profissional autenticado do org consulta timeline dos pacientes que atende; RBAC granular por paciente/profissional foge do escopo da fatia); erros 403 `LGPD_CONSENT_REQUIRED`, 400 nas demais. Sem tabela = sem migração; sem alteração em service existente = zero risco de regressão. Teste: `test:clinic-timeline` (39 verificações — cenário rico com 10 eventos misturados em 1 paciente, todos 8 kinds presentes, ordenação DESC (item mais recente = apt agosto, mais antigo = apt maio cancelado), payload rico (refId/encounterId/actorName preenchidos, summary com preview de nota longa truncada em 60 chars + "...", sufixo "(cancelado)" em appt cancelled), LGPD block após revoke + re-grant restaura, contact inexistente → LGPD_CONSENT_REQUIRED, filtro `from`/`to` janela junho exclui apt maio/agosto, filtro `kinds` restringe pra 2 tipos, `limit=3` clipa + `totalBeforeLimit=10` reporta pré-clipping, paciente sem eventos → `count:0` `items:[]`, tiebreaker narrativo signed>opened>appt no mesmo timestamp exato, isolamento multi-tenant org B com mesmo contactId não vê eventos de A). Regressão OK: `clinic-encounter` (26/26), `clinic-addendum` (27/27), `clinic-documents` (31/31), `clinic-attachments` (24/24), `clinic-doc-send` (30/30), `clinic-consent-reads` (46/46). CI matrix ganhou `clinic-timeline`.
- **Fase 20 — Addendum ao prontuário assinado (extensão 2026-07 · CFM 1.821/2007):** fecha a dívida técnica declarada da Fase G ("`finalize()` congela em `signed` — updates novos são bloqueados no service; addendum fica pra próxima fatia"). CFM 1.821/2007 exige que o prontuário original NÃO seja modificado após finalizado, mas o profissional precisa poder acrescentar informação relevante que apareceu depois (resultado de exame que voltou, correção de erro material, evolução tardia). Sem addendum, o profissional ou joga fora a info (perde continuidade clínica) ou tenta editar o encounter assinado (bloqueado pelo service, quebra confiança no produto). Nova tabela `clinical_encounter_addendums` (id, org, encounter_id, contact_id, author_id, author_name_snapshot, note, signed_with_pin, created_at) — APPEND-ONLY por design (nunca UPDATE nem DELETE, cada addendum é uma foto imutável com autoria + timestamp próprios). `ClinicEncounterService.addAddendum(orgId, encounterId, actorId, {note, actorName?, pin?})`: exige encounter `signed` (código `ENCOUNTER_NOT_SIGNED` em rascunho — em draft o fluxo é `update` que cai no history normal); LGPD Art.11 obrigatório (código `LGPD_CONSENT_REQUIRED`); note valida 1..4000 chars (códigos `ADDENDUM_EMPTY`/`ADDENDUM_TOO_LONG`); reusa `verifyPin` da Fase T (agora exportado) — profissional SEM PIN cadastrado assina sem PIN (compat legado, `signed_with_pin=0`), profissional COM PIN exige (`PIN_REQUIRED`/`PIN_INVALID`), quando ok grava `signed_with_pin=1`. `listAddendums(orgId, encounterId)`: consent gate LGPD, ordenado `created_at DESC` (mais recente primeiro), encounter inexistente devolve `[]` sem gatear consent (mesma semântica do `history` da Fase 19 — nada a esconder). Rotas: `POST /encounters/:id/addendums` (body `{note, pin?}`, sem RBAC especial — qualquer usuário autenticado do org pode adicionar; autoria fica gravada), `GET /encounters/:id/addendums`. Códigos HTTP: 403 `LGPD_CONSENT_REQUIRED`, 409 `ENCOUNTER_NOT_SIGNED`, 401 `PIN_REQUIRED`/`PIN_INVALID`, 400 `ADDENDUM_EMPTY`/`ADDENDUM_TOO_LONG`. Auditoria `CLINIC_ENCOUNTER_ADDENDUM_ADDED` com `{encounterId, addendumId, signedWithPin, length}` — permite auditor rastrear volume e uso de PIN. Teste: `test:clinic-addendum` (27 verificações — draft rejeita, signed aceita append-only, note preservada, ordenação DESC, empty/too-long rejeitados, LGPD block write + read + re-grant restaura, encounter inexistente devolve `[]` sem gate, PIN opcional (sem PIN emite, com PIN configurado exige, PIN errado falha), isolamento multi-tenant, auditoria com metadata completo). Regressão OK: `clinic-encounter` (26/26), `clinic-consent-reads` (46/46), `clinic-documents` (31/31), `clinic-signature-pin` (29/29). CI matrix ganhou `clinic-addendum`. ADR-080 Fase G atualizado — dívida "addendum fica pra próxima fatia" fechada.
- **Fase 19 — Consent LGPD Art.11 em READS (extensão 2026-07):** completa o guardrail que a Fase 1 abriu só nas writes. Antes, o consent `dados_sensiveis` era exigido em `open`/`update`/`create`/`add` — mas `get*`/`list*`/`render*Pdf`/`read` continuavam devolvendo dado clínico mesmo quando o paciente havia REVOGADO o consentimento. Impacto regulatório: LGPD Art.8 §5 (revogação facilitada) e Art.11 II f violados — o titular tirava o consent e o profissional/gestor seguia lendo prontuário, receitas, atestados e anexos até a retenção CFM passar (20 anos). Fix cirúrgico em cada service: `ClinicEncounterService.getByAppointment/get/listByPatient/history` gata via `requireConsent`; helpers privados `getRaw`/`getByAppointmentRaw` mantêm callers internos (`update`/`finalize`/`setFollowUpRecommendation`/retorno do `open`) sem duplicar checagem — o `update` já valida consent explicitamente e o `finalize` continua write-only (não gate por design, alinhado ao comportamento histórico). `ClinicDocumentsService.getPrescription/getCertificate` gatam em cada leitura; `listByEncounter` deriva `contact_id` do encounter dono e gata antes de qualquer row; `renderPrescriptionPdf`/`renderCertificatePdf` gatam por consequência (chamam `getPrescription`/`getCertificate`); helpers `getPrescriptionRaw`/`getCertificateRaw` cobrem callers internos (`updatePrescription`/`issuePrescription`/`updateCertificate`/`issueCertificate` — todos já têm `requireConsent` explícito). `ClinicAttachmentService.list` deriva `contact_id` do encounter; `get` e `read` gatam via row do próprio anexo; `getRaw` privado cobre `add`/`remove`/`setSharedWithPatient` (todos com consent gate explícito ou visibilidade decidida pelo gestor sem depender do consent do paciente — o Portal do Paciente da Fase 18 já revalida em cada acesso). **Row inexistente NÃO gata** em nenhum método — retorno `null`/`[]` é consistente com "nada a expor". `ClinicRescheduleService.createOffer` ganha gate `comunicacoes` — se o paciente revogou comms, offer NÃO é criada (retorna `null` + audit `CLINIC_RESCHEDULE_SKIPPED_NO_CONSENT`); `ClinicVacancyService.tryOfferOnCancel` já checava consent no `pickCandidate` (Fase Q linha 101). Rotas HTTP afetadas (`GET /appointments/:id/encounter`, `GET /encounters/:id/history`, `GET /patients/:contactId/encounters`, `GET /encounters/:id/documents`, `GET /prescriptions/:id/pdf`, `GET /certificates/:id/pdf`, `GET /encounters/:id/attachments`, `GET /attachments/:id/download`) devolvem **403** com `code: "LGPD_CONSENT_REQUIRED"` em vez de 400/404 — permite UI diferenciar "sem permissão" de "não existe" e disparar fluxo "solicitar novo consentimento ao paciente". Teste: `test:clinic-consent-reads` (46 verificações — regressão do gate da Fase 1, sanity de todos os reads com consent, cada método bloqueando após revoke com code correto, row inexistente NÃO gata e devolve null/array vazio, re-grant restaura reads, reschedule sem comms retorna null + audit, revoke em B não afeta reads em A). Regressão: 11 suítes verdes (`clinic-encounter` 26/26, `clinic-documents` 31/31, `clinic-attachments` 24/24, `clinic-doc-send` 30/30, `clinic-patient-portal` 35/35, `clinic-signature-visible` 31/31, `clinic-signature-pin` 29/29, `clinic-reschedule` 42/42, `clinic-vacancy` 30/30, `clinic-followup` 27/27, `clinic-emergency` 35/35). CI matrix ganhou `clinic-consent-reads`.
- **Fase 18 — Emergencial: cross-tenant + RBAC + secrets (extensão 2026-07):** varredura de segurança/governança/UX em cima da Agenda Clínica pegou 5 achados críticos que exigem fix antes das próximas features. **(1) Retention cross-tenant** (`ClinicRetentionService.runForOrg` linhas 74-87, versão Fase U): a varredura por `mtime` lia a RAIZ de `CLINIC_DOCS_DIR` sem distinguir a qual org o PDF pertencia — org A com `deliveryDays=7` apagava PDFs de org B com `deliveryDays=730`, destruição silenciosa de dado clínico entre clínicas violando CFM 1.821 (guarda de 20 anos). Fix: `ClinicDocumentDeliveryService.send` grava em `CLINIC_DOCS_DIR/{orgId}/{uuid}.pdf`; `storage_key` passa a ser `{orgId}/{uuid}.pdf`; `safeStorageKey` aceita exatamente uma barra separando 2 segmentos válidos (`^[a-zA-Z0-9._-]+$` cada), recusa `..`, barras extras ou basename escondido; `signedUrl` monta rota `/api/public/clinic/documents/:orgId/:file`; `resolveSignedFile` valida HMAC do path completo; `runForOrg` só varre `CLINIC_DOCS_DIR/{orgId}/`. PDFs pré-Fatia-18 soltos na raiz (sem mapeamento arquivo→org, `storage_key` nunca era persistido) são movidos por `ClinicRetentionService.migrateLegacyPdfs()` pra `_legacy_orphans/` — fora do alcance de qualquer `runForOrg`, gestor limpa manualmente. Idempotente e best-effort. **(2) HMAC de URL colapsava com `JWT_SECRET` vazio** (`ClinicDocumentDeliveryService.ts:39`): `process.env.JWT_SECRET || ""` produzia segredo derivado `sha256(":clinical_document_v1")` — publicamente reproduzível, permitindo forjar URL de qualquer PDF clínico se o atacante descobrisse um `{orgId}/{uuid}.pdf`. Fix: importa `JWT_SECRET` de `./config/secret.js` (bootstrap já garante segredo ≥32 chars via env → persistido em disco → aleatório efêmero, nunca vazio). **(3) RBAC ausente em rotas de alto blast-radius**: `POST /prescriptions/:id/issue`, `POST /certificates/:id/issue`, `GET /professionals/:id/pin-status`, `POST /patients/:contactId/portal-tokens`, `DELETE /patients/portal-tokens/:tokenId`, `PUT /lgpd/settings`, `POST /lgpd/contact/:id/forget`, `PUT /lgpd/consent-config` — todas aceitavam qualquer usuário autenticado. Sem PIN configurado no profissional, um `agent` podia emitir receita em nome do médico (`verifyPin` degrada pra `false` no legado sem PIN); gerar token do portal expõe receitas/atestados/anexos por 30 dias. Fix: `requireRole("owner", "admin")` em todas. **(4) Portal do paciente ignorava revoke de consent** (`ClinicPatientPortalService.resolveToken`): validava só `active + expires_at`, sem re-checar `hasConsent(SENSITIVE|COMMS)`. Paciente que revogava LGPD Art.11 continuava exposto pelos links já entregues (WhatsApp cacheia URL no celular) por até 30 dias. Fix duplo: `resolveToken` revalida ambos os consents em cada acesso (defesa 1); `LgpdService.revokeConsent` faz cascade pra `patient_portal_tokens.active = 0` quando `consentType ∈ {dados_sensiveis, comunicacoes}` (defesa 2, imediata) e grava audit `CLINIC_PATIENT_PORTAL_REVOKED_CASCADE`. Consent NÃO listado (ex: `marketing`) NÃO cascata. Isolamento cross-org preservado — revoke em A não afeta tokens de B. Teste: `test:clinic-emergency` (35 verificações — retention: cross-tenant purge respeita subpasta, PDF de A não é tocado por retention de B, idempotência, dispatch multi-org; legacy orphans: migrateLegacyPdfs move da raiz pra `_legacy_orphans/`, retention ignora arquivos ali, idempotente; JWT: `JWT_SECRET` do bootstrap ≥32 chars, HMAC atual ≠ secret público forjado com `sha256(":clinical_document_v1")`, `resolveSignedFile` recusa sig forjada; RBAC: `requireRole` bloqueia agent com 403, libera owner/admin, bloqueia request sem user; portal: resolve com consent OK, revoke sensitive → null + cascade marca `active=0` + audit gravado, novo grant + novo token resolve normal, revoke comms também cascata, revoke marketing NÃO cascata, cross-org isolamento). Regressão OK: `clinic-doc-send` (30/30 — signedUrl embute `{orgId}/{file}`, recusa key legada sem barra, recusa key com barras extras), `clinic-retention` (23/23 — teste atualizado pra escrever PDFs em `CLINIC_DOCS_DIR/{orgId}/`), `clinic-patient-portal` (35/35), `clinic-signature-visible` (31/31), `clinic-signature-pin` (29/29), `clinic-monthly-report` (47/47), `clinic-documents` (31/31). Rota pública ganhou `X-Content-Type-Options: nosniff` e sanitização de `filename` no `Content-Disposition` (CRLF-safe). CI matrix ganhou `clinic-emergency`.
- **Fase 17 — Relatório mensal em PDF (extensão 2026-07):** fecha o loop de valor pro gestor — as Fases M–U produzem eventos e as Fases O/R já expõem dashboards ao vivo, mas ninguém arquiva/imprime/manda pro contador ou pra decisão de sócios. Consolida o mês inteiro num PDF único. **Sem tabela nova** — reusa `ClinicMetricsService.overview` (mesma matemática do dashboard, uma verdade só) com janela fechada `[dia 1 00:00 UTC, último dia 23:59:59.999 UTC]` do mês pedido. `normalizeMonth(input?, nowMs?)` aceita `YYYY-MM` válido; sem input, cai no **MÊS ANTERIOR** ao `nowMs` (relatório é retrospectivo por natureza — mês corrente é sempre parcial, geraria número enganoso). `monthWindow(month)` devolve `{fromISO, toISO, label}` com nome do mês em pt-BR ("julho de 2026"). `ClinicMonthlyReportService.buildPayload(orgId, month?, nowMs?)` monta `{month, monthLabel, windowFromISO, windowToISO, businessName, metrics}`; `renderPdf(orgId, month?)` gera o `Buffer` via pdfkit (padrão da Fase H — cabeçalho verde teal, blocos com título sublinhado, tabela de profissionais). Blocos do PDF: (1) **Atendimentos** — total/realizados/no-show(%)/cancelados/confirmados-pelo-paciente(%); (2) **Cancelamentos por origem** — paciente(%)/staff/system; (3) **Lembretes WhatsApp** — enviados/falhas/confirmação(%)/cancelamento(%); (4) **Automações WhatsApp** — vagas oferecidas/aceitas + **horário recuperado** (soma da duração das vagas aceitas, formatado como "1,5 h" — "receita salva") + reagendamentos ofertados/escolhidos; (5) **Documentos emitidos** — receitas/atestados/enviados por canal; (6) **Retornos** — recomendados/agendados/pendentes; (7) **Ocupação por profissional** — tabela com consultas/realizadas/canceladas/ocupação em horas, cabeçalho separador linha, sort desc por consultas (herdado de `overview`). Bloco vazio ("Sem atendimentos no período.") em vez de crash quando o mês tem 0 consultas. Rota: `GET /api/clinic/reports/monthly.pdf?month=YYYY-MM` — **owner/admin only** (relatório expõe agregados de negócio), responde `Content-Type: application/pdf` + `Content-Disposition: inline; filename="relatorio-mensal-YYYY-MM.pdf"`, auditoria `CLINIC_MONTHLY_REPORT_GENERATED` com `{month, totalAppointments, recoveredMinutes}` (permite auditor rastrear quem/quando/pra qual mês). Teste: `test:clinic-monthly-report` (47 verificações — `normalizeMonth` aceita válido e cai em mês anterior no default, `monthWindow` UTC fecha primeiro/último dia inclusive fevereiro, `buildPayload` casa com seed (5 completed + 3 no-show + 2 cancelled + 4 confirmed + 2 rx + 1 cert + vaga aceita 60min → recoveredMinutes=60), PDF %PDF- magic, `pdf-parse.PDFParse.getText()` extrai literal do nome do negócio + label do mês + títulos dos 7 blocos + nome dos profissionais + "1 h" (60min) + rodapé "ADR-080 Fase 17", isolamento multi-tenant (seed em B não afeta payload de A, professionals de B não aparecem em A), mês vazio renderiza PDF válido com "Sem atendimentos no período.", lembrete recém-enviado aparece no próximo build). Regressão OK em `clinic-metrics` (35/35), `clinic-signature-visible` (31/31), `clinic-retention` (23/23) — reuso puro do `overview`, zero mudança nas fases anteriores.
- **Fase 16 — Assinatura visível no PDF (extensão 2026-07):** fecha o loop VISUAL da Fase T — o PIN prova autoria no banco; sem esta fase, paciente/fiscalização não vê. Aditivos em `clinical_prescriptions` e `clinical_medical_certificates`: `signature_hash TEXT` (SHA-256 hex do conteúdo canônico) + `signature_timestamp DATETIME` (ISO UTC do momento do issue). Hash cobre payload ordenado por chave (`JSON.stringify(payload, Object.keys(payload).sort())`) contendo `kind`, `id`, `orgId`, `contactId`, snapshots do profissional (nome/registro/conselho), itens/campos clínicos e o próprio `signedAt` — timestamp entra pra impedir colisão entre docs idênticos emitidos em momentos distintos. Determinístico: mesma entrada → mesmo hash (permite conferência offline). Só gera quando `signedWithPin === true`; sem PIN, ambos ficam `null` e o rodapé não é renderizado (compat backward). `drawElectronicSignatureFooter(doc, hash, timestamp)` desenha rodapé com linha tracejada + "Assinado eletronicamente com PIN pessoal (ADR-080 · LGPD Art. 11)" + "Emitido em: DD/MM/AAAA HH:MM UTC" + `Hash SHA-256: <64hex>` em Courier — só é chamado quando ambos existem. Auditoria `CLINIC_PRESCRIPTION_ISSUED`/`CLINIC_CERTIFICATE_ISSUED` ganha `signatureHash` no metadata (permite auditor cruzar hash do PDF impresso com log). Reemissão de doc já `issued` é no-op (o `issue*` retorna cedo se já `issued`) — hash/timestamp preservados. Teste: `test:clinic-signature-visible` (31 verificações — sem PIN campos null + PDF sem rodapé, com PIN hash 64 hex + timestamp ISO + PDF contém "Assinado eletronicamente"/"ADR-080"/"LGPD Art. 11"/"Hash SHA-256"/hash literal/"Emitido em"/"UTC", PDF assinado > PDF sem, determinismo, 2 docs emitidos em ms diferentes têm hashes diferentes, certificate segue mesma regra, prescription hash ≠ certificate hash, isolamento multi-tenant, auditoria carrega `signatureHash`, reemissão preserva). Extração via `pdf-parse` (`PDFParse.getText()`) — texto do rodapé sai literal no output do pdfjs, permitindo asserção precisa. Nenhum ALTER foi ANTES do CREATE dessa vez (padrão das Fases L/T/U seguido).
- **Fase U — Retenção LGPD / purge automático (extensão 2026-07):** LGPD Art.16 exige eliminar dado quando cumprida a finalidade — aplicado com CAUTELA no contexto clínico. **PRESERVAMOS** row de prontuário/receita/atestado (Resolução CFM 1.821/2007 exige 20 anos de guarda). **APAGAMOS** apenas ARQUIVOS derivados: (1) PDFs gerados pra envio de deliveries por WhatsApp — cache, o paciente já tem cópia no celular (default 30 dias); (2) anexos ao prontuário (imagens, exames) após N anos (default 730 = 2 anos). Row do anexo fica com `purged_at` pra rastro; delivery fica com `file_purged_at`. Config por org: `clinic_retention_enabled` (default 1), `clinic_retention_days_deliveries` (default 30, faixa 7..3650), `clinic_retention_days_attachments` (default 730, faixa 7..7300 = até 20 anos). `ClinicRetentionService.runForOrg(orgId)` best-effort (nunca lança), retorna `{deliveriesPurged, attachmentsPurged, errors}`; guardrail `MIN_DAYS=7` no config (defende contra 0/negativo); deleta PDFs em `CLINIC_DOCS_DIR` por mtime (fonte de verdade do FS); anexos por `uploaded_at < cutoff AND purged_at IS NULL`; **path traversal protegido**: `storage_key` valida com `basename` + regex — key sujo (`../etc/passwd`) conta como erro, jamais deleta fora. `dispatch()` percorre orgs com módulo `clinica`. Novo pass `clinicRetentionPass` no Scheduler (loop principal). Rotas: `GET /clinic/settings/retention` e `PUT /clinic/settings/retention` (owner/admin, patches parciais dos 3 campos); `POST /clinic/settings/retention/run` pra manual ("limpar agora"). Auditoria `CLINIC_RETENTION_PURGE` **só** quando algo foi apagado (não polui log). Teste: `test:clinic-retention` (23 verificações — anexo recente NÃO apaga, anexo velho apaga + purged_at + row preservada, PDF velho apaga por mtime, PDF novo mantém, delivery `sent` velha ganha `file_purged_at` sem virar deletada, idempotência (2ª rodada zerada), `enabled=false` skip completo, isolamento multi-tenant cross-org, storage_key com traversal conta como erro sem apagar nada fora, auditoria só quando purgou). Bug pego pelo teste: mesmo padrão de ordem ALTER/CREATE das Fases L/T — `ALTER TABLE clinical_document_deliveries ADD COLUMN file_purged_at` e `clinical_encounter_attachments ADD COLUMN purged_at` estavam ANTES dos respectivos CREATEs; movidos pra depois.
- **Fase T — Assinatura eletrônica com PIN (extensão 2026-07):** protege autoria clínica sem virar barreira burocrática. **NÃO é ICP-Brasil** (não vale como assinatura digital jurídica), mas é PROVA DE AUTORIA INTERNA: sem o PIN do profissional, ninguém emite receita/atestado em nome dele. Aditivos em `clinic_professionals`: `pin_salt` (UUID), `pin_hash` (SHA-256 de `salt+pin`, 4-8 dígitos), `pin_updated_at`. Aditivos em `clinical_prescriptions` e `clinical_medical_certificates`: `signed_with_pin` (default 0). `ClinicAgendaService`: `setProfessionalPin(orgId, id, pin)` (aceita `null` pra limpar, `PIN_INVALID_FORMAT` pra fora do regex `/^\d{4,8}$/`, salt NOVO a cada set — invalida hashes anteriores mesmo com o mesmo dígito); `hasProfessionalPin(orgId, id)` boolean (não expõe hash). `ClinicDocumentsService.verifyPin(orgId, profId, pin)`: se profissional NÃO tem PIN, retorna `false` (compat backward — clínicas legadas emitem sem PIN); se tem PIN mas nada foi fornecido → `PIN_REQUIRED`; se PIN errado → `PIN_INVALID`; se bate → `true`. `issuePrescription`/`issueCertificate` aceitam `{pin?}`, chamam `verifyPin` antes de virar `issued`, gravam `signed_with_pin` e incluem o boolean no `signedWithPin` do metadata da auditoria. Rotas: `PUT /clinic/professionals/:id/pin` (owner/admin) com body `{pin: string|null}`; `GET /clinic/professionals/:id/pin-status` devolve `{hasPin}`. Erros `PIN_REQUIRED`/`PIN_INVALID` respondem **401** (não 400); `PIN_INVALID_FORMAT` responde 400. UI: `EncounterModal` → função `issue()` tenta emitir sem PIN primeiro; se 401 + code `PIN_REQUIRED`|`PIN_INVALID`, `window.prompt` loop até acertar ou cancelar; toast diferencia "Emitido" vs "Emitido e assinado com PIN". Teste: `test:clinic-signature-pin` (29 verificações — set PIN 4-8 dígitos, `PIN_INVALID_FORMAT` pra formato ruim, salt novo a cada set gera hash diferente mesmo com mesmo PIN, `hasProfessionalPin` reflete estado, compat sem PIN emite normal + `signed_with_pin=0`, `PIN_REQUIRED` sem PIN, `PIN_INVALID` errado, PIN certo emite + `signed_with_pin=1`, mesma regra pra Certificate, `setPin(null)` limpa + volta pra compat, cross-org protegido, auditoria `PIN_SET`/`PIN_CLEARED` + `ISSUED` com `signedWithPin` no metadata). Bug pego pelo teste: ALTER TABLE ADD COLUMN `signed_with_pin` estava ANTES dos CREATEs de `clinical_prescriptions`/`clinical_medical_certificates` no `db.ts`. Em banco novo o CREATE ainda não tinha rodado, ALTER falhava silenciosamente e a coluna não existia. Fix: mover ALTER pra imediatamente depois dos CREATEs (mesmo bug pego na Fase L — padrão já documentado).
- **Fase S — Segundo lembrete H-2 + escalada H-1 (extensão 2026-07):** reduz mais no-show usando a mesma infra da Fase M. Novos campos em `organization_settings`: `clinic_second_reminder_hours` (default 2, faixa 1..12) e `clinic_second_reminder_enabled` (default 1). `ClinicReminderService`: `ReminderTemplateKey` ganha `"2h"`; `renderMessage(templateKey)` produz texto urgente e curto ("Ainda vamos te ver hoje às HH:MM?"); **guard**: `sendForAppointment(templateKey='2h')` retorna `null` sem enviar se `patient_confirmed_at IS NOT NULL` (paciente odiaria receber "confirma?" logo depois de confirmar); `force: true` bypassa. `dispatch()` refatorado em `dispatchWindow(orgId, nowMs, hoursBefore, templateKey)` — roda 2 janelas por org (24h + 2h). Dedup segue independente por `template_key` — 24h e 2h coexistem como rows distintas. Nova coluna `appointments.needs_manual_confirmation` (default 0). **Escalada H-1** em `escalateNearAppointments`: `UPDATE ... SET needs_manual_confirmation=1 WHERE start > now AND start <= now+1h AND patient_confirmed_at IS NULL AND needs_manual_confirmation=0` — idempotente, best-effort. Rota `PUT /clinic/settings/reminders` aceita agora `{hoursBefore?, secondHoursBefore?, secondEnabled?}` (patches por campo, validação por range). UI: badge amarelo **"aguarda confirmação"** (ícone `AlertTriangle`) no card do appointment quando `needs_manual_confirmation=1 AND !patient_confirmed_at AND status ativo` — recepção sabe ligar ou liberar vaga. Teste: `test:clinic-second-reminder` (17 verificações — template 24h vs 2h diferentes, guard de confirmed, `force` bypassa, dedup independente por template, dispatch 2 janelas envia 2 mensagens, `secondEnabled=0` pula 2ª janela, `secondHours=3` respeitado, escalada H-1 seta flag em <1h não confirmadas + NÃO seta em confirmadas + idempotente, isolamento, auditoria SENT com `template_key='2h'`). Fix de compatibilidade: `test:clinic-reminder` (Fase M) começou a falhar porque a org de teste caía no default `secondEnabled=1` e o novo `dispatch` mandava 2 lembretes em vez de 1 — atualizado o `seedOrg` do teste antigo pra explicitamente setar `clinic_second_reminder_enabled=0`.
- **Fase R — Visibilidade das automações WhatsApp no painel do gestor (extensão 2026-07):** sem esta fase o que as Fases M–Q fazem sozinhas era caixa-preta pro gestor — sem visibilidade, sem confiança, sem adoção. `ClinicMetricsService.overview` ganha `automations.reschedule` (offered/chosen/abandoned/expired) e `automations.vacancy` (offered/accepted/declined/expired + **`recoveredMinutes`** = soma da duração das vagas aceitas — "receita salva"). Nova rota `GET /clinic/vacancies?limit=` devolve últimas N ofertas com nomes hidratados (candidato, profissional, paciente da vaga original). `ClinicVacancyService.recent(orgId, limit)` faz o JOIN com `contacts` + `clinic_professionals` + subquery pro paciente do source appt. UI: 2 mudanças no painel colapsável de indicadores: seção nova "Automações WhatsApp" com 4 cards (vagas oferecidas, horário recuperado em horas, reagendamentos ofertados, ofertas abandonadas/expiradas) — `recoveredMinutes > 0` fica tone `good`. Novo painel colapsável **"Ofertas de vaga (automáticas)"** ao lado da Fila de retornos: mostra últimas 20 offers com nome do convidado + status colorido (aguardando/aceita/recusada/expirada/substituída) + slot + quem abriu a vaga. Load on-demand (só ao expandir). Teste: `test:clinic-automation-visibility` (15 verificações — vacancy.offered/accepted/declined contam corretos, `recoveredMinutes=60` pra vaga 1h aceita, `reschedule.offered=0` sem ofertas, `recent()` hidrata candidateName/professionalName/sourcePatientName e ordena desc por created_at, isolamento multi-tenant, filtro from/to respeitado).
- **Fase Q — Aviso de vaga na fila de retornos (extensão 2026-07):** fecha o triângulo cancelamento→vaga→paciente-da-fila. Quando um appt é cancelled (`patient|staff`) numa consulta FUTURA (start > now + 6h) que tinha profissional, o sistema oferece a vaga pro paciente MAIS ANTIGO com follow-up recomendado pendente do MESMO profissional. Melhor UX pra paciente ("temos vaga amanhã, quer?") e pra clínica (horário que ia ficar em branco vira consulta; ocupação máxima). Nova tabela `clinical_vacancy_offers` (id, org, source_appointment_id, candidate_contact_id, candidate_encounter_id, professional_id, slot_start, slot_duration_minutes, status pending|accepted|declined|expired|superseded, new_appointment_id, provider_message_id, expires_at, resolved_at). Uma oferta ativa por vez POR VAGA — não bombardeia N pacientes simultâneos. `ClinicVacancyService`: `tryOfferOnCancel(orgId, apptId, {sender?})` **best-effort** (nunca lança pra não bloquear cancel); `pickCandidate` faz SELECT com `NOT EXISTS` pra pular quem já tem retorno agendado, quem tem outra pending, e quem já declined/expired ESTA mesma vaga (`superseded` OK, permite superar sem re-oferecer); **itera** com `excludeContactIds` até achar candidato com LGPD `comunicacoes` + canal ativo + identifier (sem loop, um único sem consent bloqueava a vaga inteira). `handleReply(orgId, contactId, accept)` — SIM cria retorno via `scheduleFollowUp` com **parent = APPT ORIGINAL DO CANDIDATO** (não o source, que é de outro paciente!), `confirmByPatient(new)`, marca offer `accepted`, superseda outras `pending` pra mesma vaga (proteção anti-double-book). NÃO marca `declined` + agenda próxima tentativa via microtask. Housekeeping em `pendingOfferFor` expira offers vencidas (TTL 2h) antes de retornar. Integração: `ClinicReminderReplyService.tryHandle` ganha PRIORIDADE 0.5 entre reschedule offer (Fase P) e reminder normal (Fase N) — se contato tem vacancy pending, SIM/NÃO age nela (evento raro tem prioridade). Cancel via NÃO (Fase N) e via reschedule (Fase P) chamam `tryOfferOnCancel` em `Promise.resolve().then(...)` pra não bloquear resposta ao paciente que cancelou. Teste: `test:clinic-vacancy` (30 verificações — cancel <6h/sem prof → sem oferta, escolha do mais antigo com consent (pula quem falta), sender chamado 1× com texto + canal certos, `pendingOfferFor`, SIM cria retorno com parent do candidato, offer `accepted`+`new_appointment_id`, mesma vaga NÃO re-oferece pra quem `declined`, próxima tentativa oferece pro próximo candidato, NÃO `declined` + reply amigável, housekeeping expira antes de retornar, integração Fatia 8/10, isolamento multi-tenant, auditoria `OFFERED`/`ACCEPTED`/`DECLINED`). Bug pego pelo teste: primeira versão de `tryOfferOnCancel` só chamava `pickCandidate` UMA vez; se o mais antigo faltasse LGPD `comunicacoes` ou canal, devolvia `null` sem tentar o próximo — vaga ficava sem oferta. Fix: loop com `excludeContactIds` (até 20 iterações) até achar candidato elegível.
- **Fase P — Reagendamento em 1 clique via WhatsApp (extensão 2026-07):** fecha o loop da Fase N — paciente responde `REMARCAR` ao lembrete, sistema oferece 3 slots do mesmo profissional, paciente responde `1`/`2`/`3`, tudo dentro do WhatsApp sem ligar. Melhor UX pra paciente (fluxo completo no chat) E clínica (retém receita que ia embora como cancelamento; reduz ligação à recepção). Nova tabela `clinical_reschedule_offers` (id, org, contact, source_appointment, offered_slots_json [{startISO, durationMinutes}], status pending|chosen|expired|abandoned, chosen_index, new_appointment_id, expires_at, resolved_at) — guarda os slots entre as duas mensagens; sem isso o `1` fica ambíguo. `ClinicRescheduleService`: `findSlots(orgId, apptId, count)` prefere mesmo horário do dia da consulta original, varre `DAYS_TO_SCAN=14` dias à frente em passos de 30 min dentro da faixa útil (`WORKDAY_START_HOUR=8`, `WORKDAY_END_HOUR=18`), pula qualquer dia com conflito com outro appt ATIVO do profissional (`ClinicAgendaService.findConflicts`); um slot por dia; nunca oferece nas próximas 1h. `createOffer` abandona offers anteriores do mesmo paciente (garante 1 ativa por vez), retorna `{offer, message}` com texto numerado "1) qui 07/08 às 10:00\n2) sex 08/08 às 10:00\n3) seg 11/08 às 10:00" + instrução "responda o número ou X pra cancelar". `handleChoice` aceita `1..N`, `X`/`cancelar`/`desisto`; se número válido: reusa `scheduleFollowUp` (parent_appointment_id, herança de prof/duração, conflito), cancela original com `cancelledBy='patient', reason='rescheduled'`, chama `confirmByPatient(new)`; se conflito no meio (race), abandona a offer e sugere REMARCAR de novo; se slot escolhido virou passado (raro), diz "responda REMARCAR de novo". Novo intent `reschedule_offered` em `ClinicReminderReplyService.parseIntent` — `REMARCAR/REAGENDAR/MUDAR/OUTRO HORARIO/OUTRO DIA` (tolerante a acento/caixa). `tryHandle` ganha PRIORIDADE (0) antes de tudo: se há offer pendente do paciente, tenta parsear texto como escolha `1/2/3/X` — se casa, executa e responde; se não casa, cai no fluxo normal (permite SIM/NÃO com offer pendente). `NO_PATTERNS` da Fase N teve `remarcar/remarca` REMOVIDOS pra não colidir. Teste: `test:clinic-reschedule` (42 verificações). Bug pego pelo teste: primeira chamada de `createOffer(orgId, contactId, apt.id)` tinha argumentos invertidos — assinatura era `(orgId, sourceAppointmentId, contactId)`. Fix trocou a ordem.
- **Fase O — Dashboard/insights clínico (extensão 2026-07):** faz o valor dos slices anteriores virar visível — no-show rate, taxa de confirmação SIM, cancelamento por origem, ocupação por profissional, funil de retornos, docs emitidos. Sem tabela nova (só GROUP BY em cima do que G–N produzem). `ClinicMetricsService.overview(orgId, {from?, to?})` devolve `{window, appointments, reminders, cancellations, documents, followUps, professionals[]}` em uma call. Window default cobre **`[now-30d, now+30d]`** (60 dias). Taxas com **denominador correto**. Rota: `GET /clinic/metrics?from=&to=`. Teste: `test:clinic-metrics` (35 verificações). Bug pego pelo teste: window default `[now-30d, now]` excluía appointments futuros — fix `[now-30d, now+30d]`.
- **Fase N — Confirmação SIM/NÃO no WhatsApp (extensão 2026-07):** fecha o loop da Fatia M — o lembrete pede SIM/NÃO, agora o sistema escuta. Parser interceptador no `webhookProcessor.processIncomingMessage` (padrão dos interceptadores `SupplierQuoteService`, `SatisfactionService`, `BusinessTutorService`): entre CSAT e o call da IA, chama `ClinicReminderReplyService.tryHandle(orgId, contactId, text)` que devolve `{handled, action, appointmentId, reply}`. Se `handled`, envia ACK via `deliverBotMessage` e RETORNA antes da IA processar. Aditivos em `appointments`: `patient_confirmed_at`, `cancelled_at`, `cancelled_by` (`patient|staff|system`), `cancellation_reason`. Novos métodos `ClinicAgendaService.cancel(orgId, id, {reason?, cancelledBy?})` e `confirmByPatient(orgId, id)` — ambos idempotentes; `cancel` nunca DELETE (mesma filosofia do módulo). `ClinicReminderReplyService`: `parseIntent` (regex tolerante a acento/caixa/pontuação/emoji — SIM/S/CONFIRMA/OK/YES/👍 vs NÃO/N/NAO/CANCELA/CANCELAR/NO/👎; ambíguo → `null`, deixa IA cuidar); `findPendingReminder` (JOIN em `clinical_appointment_reminders` `sent` nas últimas 26h com `ORDER BY sent_at DESC, rowid DESC` — tiebreaker padrão do repo pra CURRENT_TIMESTAMP com resolução de segundo); `tryHandle` (SIM chama `confirmByPatient` + reply "Perfeito! Consulta confirmada"; NÃO chama `cancel({cancelledBy:'patient', reason:'patient_reply'})` + reply "Consulta cancelada. Obrigado por avisar!"; SIM sobre já-cancelado responde amigável sem "reverter"; 2ª SIM/NÃO responde amigável sem duplicar). Não expõe rota HTTP nova — o hook é interno do webhook. Teste: `test:clinic-reminder-reply` (37 verificações — 17 variantes de intent inclusive emoji, sem lembrete → not_handled, fora da janela 30h → not_handled, SIM confirma e é idempotente, NÃO cancela com metadata correta e é idempotente, cancelar libera horário pra novo appt, SIM sobre cancelado responde amigável sem ação, isolamento multi-tenant, auditoria `CONFIRMED_BY_PATIENT`/`CANCELLED`). Bug pego pelo teste: `ORDER BY sent_at DESC` sem tiebreaker era indeterminado quando 2 lembretes caíam no mesmo segundo — fix `rowid DESC` (padrão do projeto, mesmo dos edit histories).
- **Fase M — Lembrete automático de consulta (extensão 2026-07):** reduz no-show — problema #1 da clínica no BR — sem depender de secretária ou profissional lembrar. Scheduler roda a cada tick do loop (`clinicReminderPass`); percorre orgs com o módulo `clinica` ativo e appointments futuros em status `confirmed`/`arrived` na janela `[hoursBefore - 1h, hoursBefore + 1h]` (default 24h; configurável por org via `organization_settings.clinic_reminder_hours`, faixa 1..168h). Nova tabela `clinical_appointment_reminders` (id, org, appointment, contact, channel, to_identifier, template_key `24h`, status queued|sent|failed, provider_message_id, error, sent_at). Dedup por (org, appt, template, status IN ('sent','queued')) — mesmo lembrete só sai UMA vez, mesmo com o Scheduler rodando várias vezes na janela; `failed` NÃO conta como dedup (permite retry automático); `force: true` bypassa (manual "reenviar"). `ClinicReminderService`: `sendForAppointment` (LGPD `comunicacoes` obrigatório → LGPD_COMMS_CONSENT_REQUIRED; APPT_NOT_ACTIVE se cancelled/no_show/completed; sem canal ativo devolve `null` — skip silencioso; sem identifier erro claro; render template PT-BR com nome+data em `America/Sao_Paulo`+profissional+instrução SIM/NÃO); `dispatch` (percorre orgs, respeita `hoursBefore` por org, catch por-appt evita travar o pass). `sender` injetável facilita teste. Rotas: `GET /appointments/:id/reminders` (histórico), `POST /appointments/:id/remind` (manual, aceita `{force?: true}`), `GET/PUT /clinic/settings/reminders` (config `hoursBefore`, PUT gate `owner|admin`). Teste: `test:clinic-reminder` (27 verificações — LGPD block, APPT_NOT_ACTIVE, envio OK com template válido, dedup real (sender NÃO chamado 2×), `force=true` cria row novo, provider falha grava failed sem relançar, failed permite reenvio, sem identifier erro claro, sem canal null-skip, janela de 24h vs 48h fora, `hoursBefore=2` respeitado, isolamento multi-tenant, auditoria `CLINIC_REMINDER_SENT`/`_FAILED`).
- **Fase L — Portal do Paciente (extensão 2026-07):** paciente recebe link seguro por WhatsApp/e-mail e acessa direto do celular — próximas consultas, receitas + atestados EMITIDOS (rascunho não vaza), anexos que o profissional marcou como compartilháveis. **Não expõe SOAP** (achado clínico é do profissional) nem financeiro. Molde do `ClinicPortalService` (Fase D — Portal do Profissional): token opaco de 32 bytes, no banco fica só o hash SHA-256 + expiração + `active` flag. Nova tabela `patient_portal_tokens` (id, org, contact_id, token_hash, active, expires_at, last_access_at, created_by/at) — múltiplos tokens ativos coexistem (celular + tablet do familiar); revogação por id OU em massa por paciente. `clinical_encounter_attachments` ganha `share_with_patient` (default `0` — anexo é privado até o profissional marcar; evita foto de tratamento interno virar visível por acidente). `ClinicPatientPortalService`: `generateToken` exige LGPD `dados_sensiveis` + `comunicacoes` (LGPD_CONSENT_REQUIRED / LGPD_COMMS_CONSENT_REQUIRED); TTL default 30d, máx 90d; devolve token cru **uma vez**. `resolveToken` retorna `{orgId, contactId}` ou `null` (rejeita expirado/revogado/tampered), atualiza `last_access_at`. `getPortalData(orgId, contactId)` monta o snapshot com `upcoming` (exclui cancelled/no_show) + `past` + `prescriptions` (só issued) + `certificates` (só issued) + `attachments` (só `share_with_patient=1`). `assertOwns*` bloqueia acesso cross-patient e cross-org antes de servir binários. `ClinicAttachmentService.setSharedWithPatient` alterna o flag (não bloqueada por signed — visibilidade não é achado clínico). Rotas autenticadas: `POST/GET /patients/:contactId/portal-tokens`, `DELETE /patients/portal-tokens/:tokenId`, `PATCH /attachments/:id/share`. Rotas públicas (sem auth) em `clinicPublic.ts`: `GET /patient/:token` (dados), `GET /patient/:token/prescriptions|certificates/:id/pdf`, `GET /patient/:token/attachments/:id/download` — cada uma resolve token + `assertOwns*` antes de qualquer dado (path traversal em ids é impossível). Teste: `test:clinic-patient-portal` (35 verificações — LGPD dupla, token cru só uma vez + hash no DB, múltiplos ativos, revoke por id + revokeAll, token expirado/revogado/tampered → null, `getPortalData` só issued/só compartilhados/exclui cancelled, `assertOwns*` cross-patient + cross-org, `setSharedWithPatient` roundtrip, auditoria ISSUED/REVOKED/SHARE_CHANGED). Bug pego durante o teste: `ALTER TABLE clinical_encounter_attachments ADD COLUMN share_with_patient` estava ANTES do `CREATE TABLE` da Fatia J no `db.ts` — em banco novo, ALTER falhava silenciosamente e a coluna não existia. Fix: mover ALTER pra depois do CREATE.
- **Fase K — Envio de receita/atestado por WhatsApp (extensão 2026-07):** fecha o loop pra o paciente — profissional emite → clica "Enviar" → PDF chega no celular. Nova tabela `clinical_document_deliveries` (id, org, doc_kind prescription|certificate, doc_id, contact_id, channel_id, to_identifier, status queued|sent|failed, provider_message_id, error, sent_by, sent_at) — cada tentativa vira row (2ª via = 2 rows), histórico completo. `ClinicDocumentDeliveryService.send(orgId, kind, docId, actorId, {sender?})`: exige doc `issued` (`DOCUMENT_NOT_ISSUED`) + LGPD `dados_sensiveis` **E** `comunicacoes` (`LGPD_COMMS_CONSENT_REQUIRED` — envio por canal exige consentimento separado), resolve canal (do contato → fallback pelo primeiro conectado; padrão `RadarService.sendReport`), renderiza PDF via `ClinicDocumentsService.render*Pdf`, salva em `PRIVATE_MEDIA_DIR/clinical_docs/{uuid}.pdf`, gera URL assinada HMAC (`sig + exp`, TTL 15 min; secret derivado de `JWT_SECRET`, padrão `FashionAvatarService.signedUrl`), chama `MessageProviderService.sendDocument` (padrão `whatsapp_cloud` `type=document`/Evolution `send/media`), grava sent+`provider_message_id` OU failed+error — nunca relança (histórico é o retorno pra UI). `sender` injetável facilita teste sem chamada de rede. Rota pública (sem auth) `GET /api/public/clinic/documents/:key?exp=&sig=` monta o download com `resolveSignedFile` (HMAC + `timingSafeEqual` + basename-only pra bloquear path traversal) e faz `fs.createReadStream(...).pipe(res)`. Rotas autenticadas: `POST /prescriptions/:id/send`, `POST /certificates/:id/send`, `GET /documents/:kind/:id/deliveries`. UI: botão **"WhatsApp"** na aba Docs do `EncounterModal` (só quando `status=issued`), lista de envios em pequena timeline com badge `sent`/`failed`, botão "Reenviar" ao lado de cada. Teste: `test:clinic-doc-send` (26 verificações — rascunho bloqueia, LGPD comms bloqueia, envio OK grava sent+message_id, URL assinada absoluta com APP_URL, PDF `%PDF-` no disco privado, HMAC roundtrip + recusa sig/exp/path-traversal inválidos, provider falha grava failed sem relançar, paciente sem identifier erro claro, fallback de canal quando o do contato está disconnected, sem canal ativo erro, histórico ordenado desc, isolamento multi-tenant, auditoria `CLINIC_DOCUMENT_SENT`/`SEND_FAILED`).
- **Fase J — Anexos ao prontuário (extensão 2026-07):** foto de exame, PDF de laudo, imagem antes/depois grudados no encounter (crítico pra fisio/dermato/orto — comparação visual é insumo do próprio SOAP). Nova tabela `clinical_encounter_attachments` (id, org, encounter, appointment, contact, label, kind image|pdf|other, mime_type, original_filename, storage_key, size_bytes, uploaded_by, uploaded_at). Arquivo físico em **`PRIVATE_MEDIA_DIR/clinical/{orgId}/{encounterId}/`** (fora do `/media` estático — dado LGPD Art.11), nome `{uuid}{ext}` (nunca `originalname` — evita colisão e path traversal). `ClinicAttachmentService.add|read|list|remove`. Whitelist: `image/png`, `image/jpeg`, `image/webp`, `application/pdf`. Limite 15 MB (padrão `radar.ts`). LGPD Art.11 obrigatório em `add`/`remove`. `remove()` BLOQUEADO após `signed` (`ATTACHMENT_FROZEN`) — mesma integridade dos docs emitidos; `add()` FUNCIONA pós-signed (paciente pode trazer exame depois). Não espelha em S3 nesta fatia — política de espelhamento pra dado sensível precisa de decisão dedicada. Rotas: `POST/GET /encounters/:id/attachments`, `GET /attachments/:id/download` (autenticada, streaming; `inline` pra image, `attachment` pra PDF), `DELETE /attachments/:id`. Upload multipart via `multer.memoryStorage()` + `fileFilter` (mesmo padrão `radar.ts:24-31`). UI: nova aba **"Anexos"** no `EncounterModal` com input file, preview inline pra imagens, ícone pra PDF, botão remover (só draft). Teste: `test:clinic-attachments` (24 verificações — LGPD block, whitelist mime, tamanho, arquivo em disco com nome uuid+ext, `read` roundtrip, `list` ordenado, `remove` apaga row+arquivo, `remove` bloqueado pós-signed, `add` OK pós-signed, isolamento, auditoria).
- **Fase I — Retorno em 1 clique + fila (extensão 2026-07):** fecha o loop `agenda → prontuário → docs → retorno → agenda`. Aditivos no DB: `appointments.parent_appointment_id` (rastreia a série de consultas do mesmo caso) + índice `(org, parent)`; `clinical_encounters.follow_up_recommended_days` (intenção clínica marcada pelo profissional no plano). Nenhuma tabela nova — retorno é `appointment` normal, reusando `createAppointment` (mesma validação de conflito por profissional/sala, mesmo `force`). `ClinicAgendaService.scheduleFollowUp(orgId, sourceApptId, {inDays|atISO, durationMinutes?, force?})`: herda paciente/profissional/duração da consulta de origem, resolve data (`atISO` > `inDays`), reusa `createAppointment` e depois marca `parent_appointment_id`; **idempotente** — se já existe retorno futuro NÃO cancelado com o mesmo parent, devolve o existente (evita duas secretárias criando dois retornos); retorno cancelado LIBERA re-agendamento. `ClinicAgendaService.followUpQueue(orgId)`: lista encounters `signed` com `follow_up_recommended_days` sem retorno pendente, com nome do paciente + profissional + `suggestedAt` (data sugerida = origem + N dias). `ClinicEncounterService.setFollowUpRecommendation(orgId, encounterId, days|null)`: NÃO bloqueada por `signed` (intenção agendada pode ser ajustada pós-assinatura sem virar addendum); rejeita `days=0` (`Math.floor` + `throw`, sem `Math.max` autocorretor). Rotas: `POST /appointments/:id/follow-up`, `GET /follow-up-queue`, `PATCH /encounters/:id/follow-up-recommendation`. Teste: `test:clinic-followup` (27 verificações — herança prof/paciente/duração, `parent_appointment_id`, idempotência, cancelar libera, `atISO` > `inDays`, `CONFLICT` propaga e `force` bypass, fila entra/sai conforme estado, isolamento, auditoria `CLINIC_FOLLOWUP_SCHEDULED` + `CLINIC_ENCOUNTER_FOLLOWUP_SET`).
- **Fase H — Receita + Atestado (extensão 2026-07):** `clinical_prescriptions` (items JSON — droga/dose/quantidade/instruções/tarja; `repeats_allowed`; `valid_until`) e `clinical_medical_certificates` (CID-10 opcional, `days`, `purpose` rest|comparecimento|other, `notes`). Ciclo `draft` → `issued` (imutável após `issued` — service lança `DOCUMENT_ISSUED`, DB não impõe pra não engessar cancelamento futuro). Snapshot próprio do profissional (`professional_name_snapshot`, `professional_registration_snapshot`, `professional_council_snapshot`) é gravado no momento do `issue` — alterar o cadastro depois **não** afeta doc emitido. `clinic_professionals` ganha `registration_number` + `council` (aditivos, opcionais). LGPD Art.11 obrigatório (mesmo guardrail do encounter: `LGPD_CONSENT_REQUIRED` em create/update). PDF é `Buffer` via `pdfkit` (padrão `ReportPdfService.generateGovernancePdf`): cabeçalho com nome do negócio, corpo estruturado, bloco de assinatura com nome + `CRM/UF número`; rascunho ganha faixa vermelha "RASCUNHO — não vale como emitido". Rotas: `GET /encounters/:id/documents`, `POST /encounters/:id/prescriptions|certificates`, `PATCH /prescriptions|certificates/:id`, `POST /:id/issue`, `GET /:id/pdf`. UI: nova aba **"Docs"** no `EncounterModal` com listagem + botões "Nova receita" / "Novo atestado"; modais dedicados com itens dinâmicos (receita) e CID + dias + motivo (atestado); "Emitir" congela e libera "Baixar PDF" (blob download, padrão `ExportAuditButton`). Teste: `test:clinic-documents` (31 verificações — LGPD block, receita exige item, update bloqueado após issued, `issue` idempotente com snapshot próprio que não muda quando cadastro do profissional muda depois, atestado exige `days ≥ 1`, PDF gera `%PDF-` válido pra emitido e rascunho, isolamento, auditoria).

A dependência é estrita: a Agenda Clínica (C) é pré-requisito das visões multi-profissional e da autorização vinculada a consulta. A ordem começa pelo que entrega valor com menor risco (A, B) antes do subsistema pesado (C).

## Consequências

**Positivas:**
- O grosso do valor imediato (pack + ficha) sai cedo e barato, sem depender do subsistema pesado de agenda.
- A funcionalidade clínica nasce como módulo gated — sem inchar quem não é clínica, respeitando planos.
- Zero risco jurídico novo de integração: TISS fica para ADR próprio, com o tempo de tratar certificado digital com seriedade.
- Reaproveita criptografia, auditoria, portal por token, PDF e socket.io existentes — pouca infra nova.

**Trade-offs aceitos:**
- A Agenda Clínica (Fase C) é praticamente um agendador novo — é o maior item e o maior risco; assumido conscientemente e isolado numa fase própria.
- `clinic_professionals` desacoplada de `users` significa um segundo cadastro de pessoas no sistema (profissional ≠ usuário) — aceito em troca de não gastar licença e não misturar operação com quem só aparece na agenda.
- Autorização manual no MVP não "envia sozinha" — comunicado como promessa honesta, não como limitação escondida.
- Gatilhos clínicos custam código de disparo (não só cadastro) — reconhecido em D5.

## Testes

- Fase A: teste do pack `saude` 2.0 (idempotência por nome, flags aplicadas, RBAC bloqueando `agent`).
- Fase B: `patient_profiles`/`patient_plan_history` — editar plano preserva contato/agendamentos e registra histórico; isolamento multi-tenant; auditoria.
- Fase C: duração por consulta sem teto; transição de status nunca exclui; conflito por profissional; cálculo de `over_time` determinístico.
- Fase D/E: token do portal resolve só a agenda do próprio profissional (nunca de outro/tenant); autorização respeita guardrails de IA e audita cada transição.
- Regressão: suítes existentes de isolamento/RBAC/auditoria não regridem.
