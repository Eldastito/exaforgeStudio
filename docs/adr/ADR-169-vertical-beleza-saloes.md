# ADR-169 — Vertical Beleza & Salões (PRD 12)

**Programa:** ZapFlow Distribuição por Vertical (segunda onda)
**Estado:** **F0 MERGEADA (PR #1121)** · **F1 MERGEADA (PR #1122)** · **F2 MERGEADA (PR #1123)** · **F3 MERGEADA (PR #1124)** · **F4 MERGEADA (PR #1125)** · **F5 MERGEADA (PR #1126)** · **F6 MERGEADA (PR #1127)** · **F7 MERGEADA (PR #1128)** · **F8 MERGEADA (PR #1129)** · **F9 MERGEADA (PR #1130)** · **F10 MERGEADA (PR #1131)** · **F5-transversal-A MERGEADA (PR #1132)** · **F5-transversal-B MERGEADA (PR #1133)** · **F5-transversal-C MERGEADA (PR #1134)** — **F5-transversal completa (3/3 fatias): consent LGPD + quiet-hours + frequency-cap** — · **F11 MERGEADA (PR #1135)** · **F12 em andamento** — `BeautyMaintenanceDetector` (~200L, DETERMINÍSTICO — 0 LLM): segundo tijolo do Beauty Autopilot em SHADOW (o primeiro foi F11). Publica sinal `beauty:maintenance_due:{contactId}:{serviceId}` na espinha canônica pra pares (contato, serviço) cuja janela `products_services.maintenance_days` já venceu SEM appointment futuro do mesmo par agendado. Regra: "coloração feita há 60d, coloração tem manutenção sugerida em 30d → hora de oferecer retorno". **1 coluna aditiva** em `products_services` (`maintenance_days INTEGER` — opt-in por SERVIÇO; NULL = sem manutenção, 0-regressão dura pra catálogos legados) + **1 flag opt-in** por org (`beauty_maintenance_detector_enabled`, default 0). **Query única com NOT EXISTS** garante "só o mais recente por (contato, serviço)" — evita publicar N sinais pro mesmo par. **Ignora `cancelled`/`no_show`** (falta não conta como "última visita"). **§42/D6** — SEM tabela paralela: sinal vive em `business_signals` com dedupe. **§84 CANONICAL_LOOP** — DETECTAR só; não escreve `decision_actions`; não muda status de appointment. A ação (`beauty_maintenance_offer` handler) é fatia futura F11-B (Autopilot em SHADOW), passará pelos 3 gates da F5-transversal. **RN-BS-07** (cross-tenant duro em 2 cenários testados); **RN-BS-11** (só publica pra serviço com `maintenance_days>0` DE FATO configurado — nunca "chuta" janela); **RN-BS-12** (só sinaliza, nunca envia); **RN-BS-04** (`evidence.contactName` só com consent `comunicacoes` — leitura live). Padrão canônico espelha F11: `sweep(orgId, now?)` + `pass()` best-effort no Scheduler + shape de retorno pra observability + `isEnabled`/`setEnabled`. Sinais têm `severity=attention`, `basis=fact`, `subjectType=contact`, `sourceEntityType=appointment` (aponta pro último appt do par). Registrado no `Scheduler.tick`. `test:beauty-maintenance-detector` 29/29 (flag default OFF + sweep zero, publish com TODOS os campos corretos incluindo dedupe key + evidence + daysSinceLast + maintenanceDays, contactName null → 'Ana' após consent, dedupe idempotente, appointment FUTURO do mesmo par suprime, serviço sem maintenance_days NULL não publica, dentro da janela não publica, cancelled/no_show ignorados, cross-tenant duro em 2 cenários, pass() ignora flag OFF, zero hardcoded). Regressão: F11 44/44 + 3 guards F5-transversal + look-to-appointment + service-duration + professional-services + clinic-agenda + clinic-episode-appointment todos verdes, tsc limpo. — `AbandonedBeautySimulationDetector` (~200L, DETERMINÍSTICO — 0 LLM): primeiro tijolo do Beauty Autopilot em SHADOW. Detecta consultas em `beauty_visual_consultations.status='ready'` que têm ≥1 simulação SUCCEEDED mas `selected_at IS NULL` há MAIS de X horas (default 24h) e publica sinal na ESPINHA CANÔNICA (`business_signals` com dedupe `beauty:abandoned_simulation:{consultationId}` — D6, sem tabela paralela). A cliente carregou foto, gerou visual, viu — mas não avançou; essa lacuna é a oportunidade que a fatia futura Autopilot transforma em follow-up governado (via `DecisionAction→ApprovalPolicy→CommandExecutor` + os 3 gates da F5-transversal). **2 colunas aditivas** em `organization_settings`: `beauty_abandoned_detector_enabled` (flag opt-in, default 0 = 0-regressão) + `beauty_abandoned_after_hours` (INTEGER ≥1, NULL = default 24). **Registrado no Scheduler.tick** — passe horário per-org, best-effort (falha per-org não interrompe demais). **RN-BS-11 (leitura live)**: só publica se consent `hair_simulation` continua ATIVO — se paciente já revogou, não vazamos oportunidade obsoleta. **RN-BS-04**: sinal NÃO carrega foto/base64/prompt — só ids; `contactName` só aparece se consent `comunicacoes` ativo (senão null). **RN-BS-12**: NÃO envia mensagem — só sinaliza; a ação (follow-up) é fatia futura em SHADOW. **RN-BS-07**: TODAS as queries filtram `organization_id`; sweep de orgA NÃO cria sinal em orgB (testado). **§84 CANONICAL_LOOP DETECTAR**: só detecta; não escreve `decision_actions`; não muda status da consulta. **§42/D6**: sem TABELA paralela de alerta — vive em `business_signals` com dedupe. **`sweep(orgId, now?)`** aceita `now` opcional pra teste determinístico + retorna `{detected, deduped, skipped_consent_revoked, publishedSignalIds}` pra observability. **`pass(now?)`** varre todas orgs habilitadas. **`setAfterHours`** valida `≥1`, aceita null (undo). Sinais têm `severity=attention`, `basis=fact`, `subjectType=contact`, `subjectId={contactId}`, `sourceEntityType=beauty_visual_consultation`. `test:beauty-abandoned-simulation` 44/44 (constantes, flag default OFF + sweep zero, publish com todos os campos corretos incluindo dedupe key, evidence populada com hoursSinceCreation calculado, contactName null sem consent → 'Ana' após consent comunicacoes, dedupe idempotente 2 sweeps → 1 sinal, consulta recente <24h NÃO detecta, sim PROCESSING/FAILED_FINAL NÃO detecta, selected/scheduled NÃO detecta, consent hair_simulation revogado skipped_consent_revoked=1, cross-tenant duro em 3 cenários, pass() ignora flag OFF, custom afterHours 72 respeitado, null volta ao default, validação ≥1, zero hardcoded). Regressão: 3 guards F5-transversal + 4 testes beauty verdes, tsc limpo. **Aditivo puro, opt-in, reversível** (§4/§37 canônico). Habilita o próximo passo: F11-B Beauty Autopilot em SHADOW (fatia futura) — propor follow-up via `DecisionActionService.propose` freado pelos 3 gates da F5-transversal. — `ClientFrequencyCapGuardService` (~230L, DETERMINÍSTICO — 0 LLM): 3º e ÚLTIMO gate transversal da F5-transversal no MESMO sink canônico `MessageProviderService.sendMessage`, depois de consent (F5-A) e quiet-hours (F5-B). Compõem por E lógico: consent > quiet > cap (não pode falar > pode mas não agora > pode agora mas já falei demais). Freio anti-spam: se um contato já recebeu N mensagens do sistema na última janela de H horas, o próximo envio é RECUSADO — resolve "várias regras disparando pra mesma pessoa em sequência" que é o problema clássico do autopilot F11+ acumular follow-ups. **3 colunas aditivas** em `organization_settings`: `client_frequency_cap_enforced` (flag opt-in, default 0 = 0-regressão), `client_frequency_cap_max_per_window` (INTEGER ≥1, NULL = default 3), `client_frequency_cap_window_hours` (INTEGER ≥1, NULL = default 24). **1 tabela nova** `outbound_send_log(id, organization_id, contact_id, sent_at)` com índice `(org, contact_id, sent_at)` — **STORE ISOLADO** dos `messages` (que depende do padrão de escrita dos callers). O guard escreve APENAS quando ALLOWS + só ela lê — fonte única, determinística. **Defaults**: 3 mensagens / 24h (razoável pra follow-up de salão). **Two-phase**: `evaluate` decide; se allow, sink fetches; se fetch OK, `record` grava (best-effort — nunca lança pro caller — rede lenta > falso pico no cap). **`evaluate(orgId, identifier, now?)`** aceita `now` opcional pra teste — permite simular janelas com timestamps determinísticos sem esperar 24h de verdade. **`setParams`** valida `≥1`, aceita `null` pra voltar ao default. **RN-BS-11**: sem custom, usa defaults explícitos; nunca "chuta" cap por vertical. **RN-BS-12**: freio anti-spam automático pros follow-ups do autopilot. **RN-BS-07**: log gravado/lido na MESMA org do canal (cross-tenant testado — orgB com 5 envios NÃO conta pra orgA que vê `unknown_contact`). **Unknown contact** (identifier sem contato) → PERMITE + NÃO REGISTRA (comunicação de sistema; cap não se aplica). **NÃO é rate-limit de infra** — é FREQUÊNCIA PERCEBIDA pela cliente. **Efeito no sink**: bloqueio lança `OutboundFrequencyCapError` com `code=outbound_blocked:frequency_cap`, `name=OutboundFrequencyCapError`, `used`/`cap`/`windowHours` + `contactId`/`contactName` populados pra caller marcar delivery como `blocked_frequency_cap` e (se quiser) reagendar pós-janela. `test:beauty-frequency-cap-transversal` 35/35 (constantes, flag default OFF + record no-op, dentro do cap default 3/24h — 3 envios permitem/4º bloqueia, envio antigo (25h atrás) NÃO conta, mix antigo+recente conta só recentes, custom `max=1, windowHours=1` respeitado, `null` volta ao default, validação `≥1` lança em 0 e -5, unknown_contact permite+não-registra, cross-tenant duro em 2 cenários, `OutboundFrequencyCapError` shape completo, integração REAL com sendMessage — bloqueia após cap com pre-seeded log / flag OFF passa direto, zero hardcoded). Regressão: 3 guards F5-transversal verdes + 5 callers reais (`test:clinic-followup` 27/27, `test:clinic-reminder` 27/27, `test:clinic-vacancy` 30/30, `test:clinic-followup-notice` 53/53, `test:radar-send` 10/10) + `test:beauty-look-to-appointment` 46/46, tsc limpo. **FECHA a F5-transversal completa** (3/3 fatias). Autopilot F11+ liberado com 3 freios transversais: consent (LGPD), quiet-hours (descanso), frequency-cap (anti-spam). — `ClientQuietHoursGuardService` (~190L, DETERMINÍSTICO — 0 LLM): 2º gate transversal no SINK canônico `MessageProviderService.sendMessage`, imediatamente depois do consent guard (F5-A). Compõem por E: consent vem primeiro ("não pode falar" é mais duro que "pode mas não agora"). Recusa envio em hora SP dentro da janela silenciosa configurada — default **22h→8h** (madrugada), cobre a intuição "não me incomode de madrugada". **3 colunas aditivas** em `organization_settings`: `client_quiet_hours_enforced` (flag opt-in, default 0 = 0-regressão), `client_quiet_hours_start_hour` (INTEGER 0-23, NULL = default 22), `client_quiet_hours_end_hour` (INTEGER 0-23, NULL = default 8). **NÃO CONFUNDIR com `proactive_awake_start/end`** (UxPreferences ADR-163 F13, ADR-163) — aquele é a janela do DONO pra receber PUSH; este é a janela em que o SISTEMA silencia mensagens pra CLIENTE. Data diferente, política diferente, coluna diferente. **Semântica `[start, end)`** com virada de meia-noite: `start > end` (ex.: 22→8) é caso normal — quiet cruza a meia-noite; `start < end` (ex.: 8→22) é o oposto — quiet DURANTE o dia (SPA fechado, raro mas suportado); `start == end` (ex.: 10→10) = **24h silêncio total** — pausa efetivamente todos os envios (o dono pode usar como "estou em férias"). Mesma matemática que `UxPreferencesService.isAwake`, invertida. **`evaluate(orgId, now?)`** aceita `now` opcional pra testes determinísticos (fixa a hora sem esperar meia-noite passar). **`setWindow`** valida hora 0-23 e aceita `null` explícito pra voltar ao default (undo idempotente). **Efeito no sink**: bloqueio lança `OutboundQuietHoursError` com `code=outbound_blocked:quiet_hours`, `name=OutboundQuietHoursError`, `hourSP` + `startHour` + `endHour` populados pra caller marcar delivery como `blocked_quiet_hours` (não como `failed`). **RN-BS-11 (nunca infere)**: sem custom, usa default explícito (22, 8) — nunca "chuta" janela por vertical. **RN-BS-12 (autopilot não vai direto pra GA)**: é o freio automático pros follow-ups da F11+ — autopilot que respeita quiet-hours é mais aceito pelas clientes. **RN-BS-07**: flag e horas lidas da MESMA org do canal — cross-tenant testado (silêncio total da orgB não afeta orgA às 12h). **NÃO SILENCIA CRÍTICO**: o guard vive no sink OUTBOUND; caller crítico legítimo (ex.: "sua reserva foi CANCELADA agora") deve usar outro canal (email/push interno) — bipass de código violaria "sink único = política única". `test:beauty-quiet-hours-transversal` 48/48 (constantes, flag default OFF + 0-regressão, dentro de 22→8 bloqueia 7 horas testadas, fora permite 6 horas testadas, payload de bloqueio inclui hourSP + janela, janela DIURNA 8→22 inverte, `start==end` = 24h silêncio testado em 6 horas, `setWindow(null,null)` volta ao default, custom parcial válido, validação de hora 0-23 lança em 25 e -1, cross-tenant duro em 2 cenários, OutboundQuietHoursError shape completo, integração REAL com sendMessage — bloqueia dentro / passa fora / flag OFF em outra org passa direto, read-only comprovado, zero hardcoded). Regressão: 6 testes beauty verdes + 5 callers de sendMessage (`test:clinic-followup` 27/27, `test:clinic-reminder` 27/27, `test:clinic-vacancy` 30/30, `test:clinic-followup-notice` 53/53, `test:radar-send` 10/10) — 0-regressão comprovada. tsc limpo. **Aditivo puro, opt-in, reversível** (§4/§37 canônico). Beneficia TODAS as verticais. Próxima: F5-transversal-C (frequency-cap por contato). — `OutboundConsentGuardService` (~180L, DETERMINÍSTICO — 0 LLM): **gate LGPD transversal** de comunicação outbound no SINK canônico. Fatia RECOMENDADA antes de F11+ pra que o Beauty Autopilot (e todo caller de `sendMessage` no repo — Cadence/Playbook/Reminder/Follow-Up/Radar/Coordenador/Vacancy/Retail/Escola/Clínica/Comigo/Prospect/Growth, ~30 callers) NÃO atropele o "não quero mais mensagens" da cliente. **1 flag opt-in** aditiva em `organization_settings.outbound_consent_required` (default 0 = 0-regressão dura). **1 ponto de guarda** — `MessageProviderService.sendMessage` faz lookup por `identifier` na org do canal e consulta `LgpdService.hasConsent(orgId, contactId, 'comunicacoes')` ANTES de qualquer efeito (rede/Radar/log). **RN-BS-04 (escopos separados)**: consent `marketing` NÃO libera `comunicacoes` (comprovado no teste — `evaluate()` retorna `consent_missing` mesmo com marketing ativo). **RN-BS-07 (cross-tenant)**: consent lido da MESMA org do canal; contato da orgB não vaza pra orgA (mesmo identifier). **RN-BS-11 (nunca infere)**: sem prova → recusa; ausência ≠ permissão. **Identifier sem contato** (broadcast/sistema/teste) → PERMITE (`unknown_contact`) — não bloqueia caminho operacional interno. **OutboundBlockedError** com `code=outbound_blocked:consent_missing` + `name=OutboundBlockedError` pra caller detectar (`err?.name === 'OutboundBlockedError'`) e degradar gracefully (marcar delivery como `blocked_consent` em vez de `failed`). **Leitura LIVE** — revogar consent volta a bloquear imediatamente (sem cache). **Read-only puro** — guard NÃO escreve consent/signal/log (deixamos audit pro caller). `test:beauty-outbound-consent-transversal` 25/25 (constantes, flag default OFF + 0-regressão, flag ON + sem consent bloqueia, escopos separados, revoke live, unknown_contact permite, cross-tenant duro em 3 cenários, OutboundBlockedError shape, integração REAL com MessageProviderService.sendMessage — bloqueia ANTES do fetch pro provider, orgC com flag OFF passa direto, read-only, zero hardcoded). Regressão: 6 testes beauty verdes + 5 callers de sendMessage verdes (`test:clinic-followup` 27/27, `test:clinic-reminder` 27/27, `test:clinic-vacancy` 30/30, `test:clinic-followup-notice` 53/53, `test:radar-send` 10/10 — 0-regressão pra pipelines existentes de envio), tsc limpo. **Aditivo puro, opt-in, reversível** (§4/§37 canônico). Beneficia TODAS as verticais (não só beleza). Próximas fatias da F5-transversal: B `client_quiet_hours_enforced` (F5-transversal-B) e C `client_frequency_cap_enforced` (F5-transversal-C) — cada uma numa PR própria pra manter fatias pequenas. — `BeautyLookToAppointmentService` (~430L, DETERMINÍSTICO — 0 LLM): **FECHA O CICLO** da Beauty AI. A simulação escolhida (F5-F8) e o serviço recomendado (F9) viram um HORÁRIO RESERVADO na agenda canônica, com a profissional certa. Sem esta fatia, a Beauty AI é uma "brincadeira" — com ela, é uma virada operacional (§7/§46 do PRD). **Composição PURA** — nenhum motor novo (§4/§37 CANÔNICO): reusa `ProfessionalServiceService.listProfessionalsFor` (F4 — quem sabe fazer o serviço), `ClinicScheduleSessionService.availability` (ADR-145 F41 — próximos horários livres do profissional, respeitando conflito + ausência + capacidade de sala; 14 dias max; mesma tabela `clinic_professionals` que beauty usa como roster), `AppointmentService.create` (F4/ADR-160 F6 — porta canônica de criação com duração pela `products_services.duration_minutes` do F4), `beauty_visual_consultations.scheduled_appointment_id` (F5 — coluna JÁ existente, agora finalmente populada; fecha o fio consulta→appt SEM tabela nova). **Snapshot visual DERIVÁVEL** (RN-004 — nunca duplica): `consultationForAppointment` recupera a sim escolhida via FK `scheduled_appointment_id`+`selected_simulation_id` — quando a retenção purga a foto (F5 `purgeExpired`), o snapshot some naturalmente com ela (LGPD). **Guardrails RN-BS ATIVOS**: RN-BS-07 (cross-tenant duro em 4 cenários — consulta/pro/serviço de outra org retorna `not_found` silencioso, não vaza existência); RN-BS-11 (nunca infere capacidade — `book` REJEITA profissional sem link em `professional_services`, retorna `professional_not_capable`); RN-BS-12 (nunca agenda por IA — a decisão é HUMANA; a automação de follow-up é F11+ em SHADOW via `DecisionAction→ApprovalPolicy→CommandExecutor`); RN-BS-03 (0 LLM — serviço só orquestra agenda). **2 métodos públicos + 1 auxiliar**: `availability(orgId, consultationId, {serviceId, fromMs?, days?, roomId?})` — retorna `[{professional, slots[]}, ...]` com `is_primary` primeiro; `book(orgId, consultationId, {serviceId, professionalId, startISO}, actorId)` — cria appointment + amarra ao snapshot; `consultationForAppointment(orgId, apptId)` — lookup derivado da FK. **7 erros tipados** (nunca 500 opaco): `consultation_not_found` · `consultation_not_selected` · `service_not_found` · `service_missing_duration` · `no_capable_professional` · `professional_not_capable` · `slot_conflict` · `invalid_start`. **Idempotência real**: consulta já `scheduled` retorna o mesmo `appointmentId` (não cria appointment duplicado — `test:beauty-look-to-appointment` prova `apptCount===1` após 2 bookings idênticos). **Guard cross-service**: mesmo `AppointmentService.create` sendo a porta canônica, ele NÃO conhece per-professional conflict; o service faz check EXPLÍCITO em `appointments.professional_id` antes do INSERT (retorna `slot_conflict` HTTP 409). **Snapshot do nome do pro**: popula `professional_name_snapshot` (aditivo do F4) — preserva "Maria" mesmo se a Maria for depois renomeada. 2 rotas HTTP novas: `GET /api/beauty/consultations/:id/availability?serviceId=&days=&fromMs=&roomId=` (retorna `[{professional, slots[]}]`), `POST /api/beauty/consultations/:id/book` (body `{serviceId, professionalId, startISO}` → 200 com appointmentId | 400 tipado | 404 not_found | 409 conflict). `test:beauty-look-to-appointment` 46/46 (constantes/vocabs, availability happy path com primary primeiro, 5 erros tipados de availability, book happy path com professional_id+snapshot+product_service_id populados, appointment gravado no DB com todos os campos, consulta pra `scheduled` com FK populada, idempotência sem duplicação, 5 erros tipados de book incluindo `slot_conflict` real com pro sobrepondo, `consultationForAppointment` derivando pela FK, cross-tenant DURO em 4 cenários — consulta/pro/serviço de outra org → not_found, zero hardcoded Studio Márcia). Regressão: `test:beauty-registry` 42/42, `test:beauty-blueprint-piloto` 54/54, `test:beauty-visual-consultation` 71/71, `test:beauty-hair-simulation` 61/61, `test:beauty-harmony-analysis` 61/61, `test:beauty-routes` 40/40, `test:beauty-look-to-services` 51/51, `test:beauty-service-duration` 25/25, `test:beauty-professional-services` 52/52, `test:clinic-agenda` 27/27 (agenda canônica intacta), `test:clinic-episode-appointment` 34/34, `test:falatu-bridge-recon` 15/15, tsc limpo. — `LookServiceRecommendationService` (~330L, DETERMINÍSTICO — 0 LLM): a virada da Beauty AI num agendamento potencial (elo comercial do PRD §16/§46). Recomenda serviços do CATÁLOGO REAL do tenant (`products_services WHERE type='service' AND active=1` — RN-BS-11: nunca inventa serviço fora do catálogo), grounded em `beauty_visual_simulations.params_json` (color/cut) e/ou `beauty_reference_looks.suggested_services_json` (curadoria do salão). **3 níveis de relevância** (`primary` = curado pelo salão via `suggested_services_json`, `matched` = LIKE em vocab fechado, `generic` = reservado pra fatia futura). **Vocabulários FECHADOS** exportados (`KEYWORDS_COLOR` 16 termos: colora/mecha/balayage/luzes/ombre/reflexo/matiz/etc, `KEYWORDS_CUT` 7 termos: corte/escova/chapinha/finaliza/penteado/franja/cortar) — a intenção da sim (`params_json.color_target`/`cut_target`) casa por LIKE sobre `LOWER(name)`. **Precedência**: (1) ids curados em `suggested_services_json` do referenceLook vêm PRIMEIRO na ordem preservada, (2) matches por keyword complementam (dedupe por id). **Read-only puro** — 0 INSERT em `beauty_visual_simulations`/`beauty_visual_analyses` (comprovado no teste). **Cross-tenant duro** — `referenceLookId` de outra org é IGNORADO silenciosamente (não vaza existência), `getSimulation`/consulta de outra org retorna `not_found`. **Determinismo** — 2 chamadas com mesmo state retornam MESMA lista (nada de random/timestamp). Catálogo vazio → `insufficient_catalog` com `activeCatalogCount=0` e mensagem instrutiva ("Cadastre serviços em Configurações › Catálogo"). Sim/consulta sem parâmetros comerciais → `sem_parametros`. Preserva `price`/`duration` do catálogo (não recalcula). 2 métodos públicos (`recommendForSimulation({simulationId, referenceLookId?})`, `recommendForConsultation({consultationId, referenceLookId?, goalOverride?})` — `goalOverride` mapeia texto livre pra intenção via mesmos vocabs) + `vocabulary()`. 3 rotas HTTP novas: `GET /vocabulary/recommendations`, `GET /simulations/:id/recommendations?referenceLookId=`, `GET /consultations/:id/recommendations?referenceLookId=&goal=`. `test:beauty-look-to-services` 51/51 (constantes, catálogo vazio → insufficient_catalog, sim color/cut/combined casando com catálogo, NÃO recomenda produto físico nem serviço inativo nem serviço de nome não-batendo, `matched` tem matchReason não-vazio, referenceLook → 2 `primary` na ordem de curadoria, ids inexistentes silenciosamente ignorados, `sem_parametros`, consultation com goal="cor"/"escova"/"transformação completa", goal fora do vocab, cross-tenant DURO em 4 cenários, determinismo, read-only comprovado, zero hardcoded Studio Márcia). Regressão: `test:beauty-registry` 42/42, `test:beauty-blueprint-piloto` 54/54, `test:beauty-visual-consultation` 71/71, `test:beauty-hair-simulation` 61/61, `test:beauty-harmony-analysis` 61/61, `test:beauty-routes` 40/40, tsc limpo. — Análise de Harmonia Visual. **Regra fundante RN-BS-03 codificada como código**: IA NUNCA julga aparência. Nova entrada `estetica_appearance_advice` em `AiGovernanceService.PEOPLE_AFFECTING` — sugestão estética passa a exigir ATOR HUMANO + MOTIVO (senão lança `human_decision_required`; ADR-130). Nova tabela aditiva `beauty_visual_analyses` (1 linha por análise — histórico completo por consulta, LGPD auditável). `BeautyHarmonyAnalysisService` (365L, DETERMINÍSTICO — 0 LLM): 5 dimensões técnicas em vocab fechado (`contraste`/`equilibrio`/`destaque`/`volume`/`intensidade`), derivadas de goal/intensity da consulta + parâmetros da simulação via mapas (`COR_ESCURA`/`COR_MEDIA`/`COR_CLARA` + `CORTE_LEVE`/`CORTE_MEDIO`/`CORTE_PRONUNCIADO`). Narrativa gerada por template — descreve o EFEITO da mudança com verbos ("cria", "acentua", "confere"), nunca ADJETIVOS DA PESSOA. **Validador HARD** rejeita 20+ palavras proibidas (`bonito`/`feio`/`atraente`/`lindo`/`nota`/`score`/`rank`/`melhor`/`pior`/`envelhec`/`rejuvenesc`/`afin`/`emagrec`/`embel` — inclui a raiz + `\w*`); se qualquer aparecer na narrativa gerada, **service LANÇA** (proteção contra regressão futura). Disclaimer obrigatório sempre embutido (PRD §31 — "nunca um julgamento sobre a pessoa"). `analyze` valida pré-condições em cascata: `guardApplied` (actor+reason) > consulta na org > consent `hair_simulation` ativo > (se `simulationId`) pertence à consulta. Cada análise gera 1 linha em `ai_decisions` (auditoria ADR-130). 3 rotas HTTP novas: `GET /vocabulary/harmony`, `POST /consultations/:id/analysis` (retorna 400 com `human_decision_required` quando falta actor/reason — semanticamente correto), `GET /consultations/:id/analyses` + `GET /analyses/:id`. **Determinismo comprovado**: 2 análises com mesmos inputs geram MESMA narrativa + MESMAS dimensões (ids diferentes — histórico completo LGPD). `test:beauty-harmony-analysis` 61/61 (PEOPLE_AFFECTING+guardApplied, vocab 5 dimensões, todas as pré-condições, 20+ palavras proibidas ausentes na narrativa, disclaimer sempre presente, determinismo, listForConsultation ordenado com `rowid DESC` como tiebreaker, cross-tenant duro, LGPD auditoria em `ai_decisions`, zero hardcoded Studio Márcia). Regressão: `test:ai-governance` 33/33 (guardrail canônico intacto), 7 beauty verdes, tsc limpo. — superfície HTTP da Beauty AI. Backend: `MODULE_BY_ROUTE[beauty]="estudio"` (F7 amarra Beauty AI ao módulo estúdio já no preset da vertical — sem inventar módulo novo); `routes/beauty.ts` (10 endpoints autenticados: `/vocabulary`, `POST|DELETE /consents`, `POST /consultations`, `GET /consultations/:id` que retorna consulta+assets+simulations, `POST /consultations/:id/upload` com multer 15MB whitelist mimetype, `POST /assets/:id/approve|reject`, `POST /consultations/:id/simulate`, `GET /simulations/:id`, `POST /simulations/:id/cancel`, `POST /consultations/:id/select`); `routes/beautyPublic.ts` (rota pública `/api/public/beauty/media/:key` que resolve URL assinada HMAC do F5 com `X-Content-Type-Options: nosniff` + `Cache-Control: private,max-age=900` — TTL coincide com a assinatura). Gates duros em CADA handler: `requireBeauty` (404 sem `vertical=beleza` — não vaza) + `requireSimulator` (403 sem `beauty_hair_simulator_enabled=1`). `test:beauty-routes` 40/40 (E2E HTTP real via Express minimal + fetch: gate vertical/simulator, todos os path 200/400/404/403, upload multipart real, URL assinada válida/sig-alterada/exp-expirado/traversal, multi-tenant duro, select avança consulta pra 'selected', cancel de QUEUED com raça do JobQueue neutralizada, DELETE consent apaga assets). Regressão: 7 testes irmãos beauty verdes, tsc limpo. **UI frontend DEFERIDA** (view `BeautyView.tsx` no App.tsx+useStore+Sidebar fica pra fatia futura F7b — o backend já pode ser consumido por qualquer cliente HTTP hoje; a fatia ficaria grande demais misturando backend+frontend). — Simulador de Cabelo (o coração da Beauty AI). Nova tabela `beauty_visual_simulations` (aditiva, mesma família de `fashion_tryon_jobs`) + `BeautyHairSimulationService` (~410 linhas) que espelha `FashionTryOnService`: contrato `HairSimulationProvider` plugável, `StubHairSimulationProvider` SEMPRE DISPONÍVEL (determinístico — gera PNG 1x1 assinado pelo hash pra CI/fallback), `GoogleGeminiHairSimulationProvider` (produção, `editImagesGoogleB64` mesma API do Fashion, ativo se `GOOGLE_AI_API_KEY`/`GEMINI_API_KEY`), seleção por env `BEAUTY_HAIR_SIMULATION_PROVIDER` ou preferência automática. **Prompt `SAFETY_PROMPT_HAIR` invertido do Fashion** (RN-BS-04): "preserve rosto/expressão/tom de pele/corpo IDÊNTICOS + PROIBIDO embelezar/afinar/emagrecer/rejuvenescer/envelhecer + aplique a mudança de cabelo (cor e/ou corte)"; RN-BS-03 explícito (nunca julga aparência); RN-BS-01 (nunca adiciona pessoas). **Vocabulário fechado** (`COLOR_VOCAB` + `CUT_VOCAB` — 18 cores + 15 cortes conhecidos): input fora do vocab vira `null` no params (RN-BS-11 — nunca texto arbitrário no prompt). **Idempotência real** por `input_hash = sha256(assetId:stableParams:providerKey)` — retry devolve o job anterior sem tocar no provider. Fila `beauty_hair_simulation` no `JobQueueService` com `maxAttempts:1` (retry caro = decisão humana). `processJob` público (padrão do Fashion — CI-friendly). Belt-and-suspenders: revalida consent `hair_simulation` mesmo com foto já approved (fecha race admin-revoga-entre-upload-e-simulação). `referenceLookId` de OUTRA org é IGNORADO silenciosamente (RN-BS-02/07 — não vaza existência). Output em `private_media/beauty/{uuid}.png`, mesma URL assinada `fileSigning` escopo `beauty_private_media_v1` do F5. `cancelSimulation` só QUEUED. `purgeExpired` usa mesma janela dos avatares (`beauty_avatar_retention_days`). `test:beauty-hair-simulation` 61/61 (constantes, prompt invertido validado textualmente, vocab, provider ativo, todas as pré-condições, happy path com Stub → SUCCEEDED + PNG signature verificada no disco, idempotência real, vocab fora do set vira null, consent revogado após upload recusa, cross-tenant duro, referenceLookId cross-tenant ignorado, cancel só de QUEUED, purga por retenção, RN-BS-01 (consulta NÃO avança pra scheduled), zero hardcoded Studio Márcia). Regressão: `test:fashion-tryon` 20/20 (padrão canônico intacto), `test:beauty-visual-consultation` 71/71, `test:beauty-registry` 42/42, `test:beauty-blueprint-piloto` 54/54, `test:beauty-profiles` 75/75, `test:beauty-service-duration` 25/25, `test:beauty-professional-services` 52/52, tsc limpo. — fundação da Beauty AI. 4 tabelas novas aditivas (`beauty_consents`, `beauty_visual_consultations`, `beauty_avatar_assets`, `beauty_reference_looks`) + 2 colunas opt-in em `organization_settings` (`beauty_hair_simulator_enabled`, `beauty_avatar_retention_days`). `BeautyVisualConsultationService` (376 linhas) espelha `FashionAvatarService` (`contact_id` no lugar de `customer_id`; `contacts` é o CRM canônico do ZapFlow): consent tipado por 4 escopos SEPARADOS (`hair_simulation` ≠ `use_in_marketing` — RN-BS-04); consulta em `draft`→`ready`→`selected`→`scheduled`; upload com quarentena obrigatória + EXIF strip via `sharp` (`Software`/`Make`/`Model` REMOVIDOS do arquivo final — RN-BS-04); URL assinada via `fileSigning` canônico com escopo próprio `beauty_private_media_v1` TTL 15min; `timingSafeEqual` + `safeStorageKey` anti path traversal + isolamento de escopo (assinatura fashion não vale aqui); retenção configurável (default 30d, clamp 1..365) + purga preguiçosa na leitura + `purgeExpired` pra Scheduler futuro; revogação de `hair_simulation` APAGA assets do contato imediatamente (LGPD Art.18). `safety_report_json` só flags booleanas (RN-BS-05 — nunca foto/base64/prompt no log). Aprovação manual em F5 (validação por IA vem em F6+ com `validateGuidedPhoto`). `test:beauty-visual-consultation` 71/71 (constantes exportadas, grantConsent idempotente + escopo inválido lança, escopos SEPARADOS, startConsultation com validação de contato + cross-tenant, upload com pré-condições, EXIF STRIP real, quarentena obrigatória, aprovação/rejeição, URL assinada + timing safe + escopo isolado + expirado + traversal, retenção configurável, `purgeExpired`, revoke consent apaga assets, isolamento cross-tenant, zero hardcoded Studio Márcia). Regressão: `test:fashion-avatar` 44/44 (padrão canônico intacto), `test:beauty-registry` 42/42, `test:beauty-blueprint-piloto` 54/54, `test:beauty-profiles` 75/75, `test:beauty-service-duration` 25/25, `test:beauty-professional-services` 52/52, tsc limpo. Rota `/api/public/beauty/media` referenciada mas ainda não montada — F6 monta junto com a UI de simulação. — catálogo real na agenda + N:N profissional↔serviço. **Parte 1:** `AppointmentService.create` passa a calcular `scheduled_end` pela `products_services.duration_minutes` quando um `productServiceId` é passado (fecha o gap #1 da auditoria F0 — a coluna era escrita mas ignorada; "corte = 45min" agora chega ao slot); precedência scheduledEnd explícito > duração do serviço > slot da org (fallback legado — 0-regressão); multi-tenant duro (produto de outra org é ignorado); guardrails RN-BS-11 (0/negativo/NULL ignorados). Helper público `AppointmentService.serviceDurationMin`. **Parte 2:** tabela nova `professional_services(org, professional_id, service_id, is_primary, active, commission_percent)` aditiva por CREATE-then-ALTER (decisão D6 — tabela nova em vez de mapear como especialidade, evita ambiguidade "corte é especialidade ou serviço?" e libera comissão nativa); `ProfessionalServiceService` com `link` idempotente por UNIQUE, `unlink` soft-off, `listServicesFor`/`listProfessionalsFor` com filtro `activeOnly`, `isCapable` boolean, `setForProfessional` atômico com validação cedo. Só type='service' aceito. `commissionPercent` validado [0..100]. `test:beauty-service-duration` 25/25 + `test:beauty-professional-services` 52/52 (**77 checks totais**). Regressão: `test:clinic-agenda` 27/27, `test:comigo-agenda` 42/42, `test:clinic-episode-appointment` 34/34, `test:falatu-bridge-recon` 15/15, `test:beauty-registry` 42/42, `test:beauty-blueprint-piloto` 54/54, `test:beauty-profiles` 75/75, tsc limpo. `nextFreeSlots(serviceDurationMin)` fica pra fatia futura — F4 entrega o fundamental (create + N:N) sem inflar escopo. — perfis por-vertical Beleza semeados **CONDICIONALMENTE pelo blueprint** (não em `SYSTEM_PROFILES`, 0-regressão pras 8 verticais existentes). `PermissionService.seedBeautyProfiles(orgId)` cria 3 perfis CUSTOM (`is_system=0`) idempotentes por nome: **Recepção (Beleza)** — agenda/atendimento/contatos/vendas write, pagamentos read, financeiro **none** (RN-BS-08); **Cabeleireira (Beleza)** — atendimento write, agenda **read** (não remarca — recepção faz), sem campanhas/cadências/financeiro; **Gerente (Beleza)** — quase-full, cobranca read, configuracoes read, financeiro read (vê resumo, não modifica — §73). `VerticalBlueprintService.assignToOrganization` chama `seedBeautyProfiles` **síncrono, best-effort** quando `bp.baseVertical === "beleza"` (mesmo padrão de `LgpdService.seedConsentForVertical` em `ModuleService.applyVertical`). `test:beauty-profiles` 75/75 codifica: shape das 3 permissões coerentes com o papel, idempotência, preserva edição do admin (re-seed não sobrescreve), side-effect no assign, assign de outros blueprints NÃO semeia beleza, isolamento cross-tenant, integração completa com `assignToUser`+`levelFor`+`can`, `SYSTEM_PROFILES` intocado (6 templates canônicos), zero hardcoded Studio Márcia. Regressão: `test:rbac-granular` 27/27, `test:rbac-profiles-api` 28/28, `test:vertical-blueprint-service` 48/48, `test:beauty-blueprint-piloto` 54/54, `test:beauty-registry` 42/42, tsc limpo. Sidebar/UI reconhecendo a vertical fica DEFERIDA (opcional na tabela §5) — Sidebar hoje já consome `isModuleEnabled`/`canAccessModule` que naturalmente refletem o blueprint. — blueprint `beleza_salao_v1` publicado + piloto Studio de Beleza Márcia atribuído POR DADO (§17/§65). +1 entrada em `BlueprintSeeder.INITIAL_BLUEPRINTS` (baseVertical=beleza, min=start, default=growth, sem bundle, required=agenda+vendas+pagamentos, optional=campanhas/cadencias/assinaturas/estudio/areas/integracoes/diretor/rie/execucao, hidden=clinica/escola/retail/retail_floor/vms/prospect, quickStartPack=null — pack de beleza é fatia futura F17+); +1 branch em `inferBlueprintKeyFor` (`beleza` sempre mapeia pra `beleza_salao_v1` independente do plano, mesmo padrão da saúde). `test:beauty-blueprint-piloto` (54 checks) codifica: blueprint publicado + idempotente + shape completo, inferência em todos os planos + regressão nos outros, piloto atribuído com `assignToOrganization` (fixture com `randomUUID`, não constante), `EntitlementService.check` usa `hiddenModules` DO BLUEPRINT (não do FALLBACK) com `source.verticalBlueprint="beleza_salao_v1:v1"`, módulos do preset ficam active, `migrateExistingOrgs` migra tenants beleza legados, cross-tenant piloto↔clínica isolado, applyVertical + assign coexistem, zero hardcoded do Studio Márcia. Regressão em `test:blueprint-seeder` (78/78 — teste é dinâmico, reconhece o novo blueprint automaticamente), `test:vertical-blueprint-service` (48/48), `test:entitlement-hidden-via-blueprint` (28/28), `test:beauty-registry` (42/42), `tsc --noEmit` limpo. Achado F0: **~85% é COMPOR / REUTILIZAR / CONFIGURAR** — a operação essencial de salão (agenda com profissional/sala/especialidade, ciclos renováveis, ficha extensível, lembrete 24h, lista de espera, remarcação por WhatsApp) já existe canonicalizada como Clínica; o Simulador de Cabelo é o **Fashion Studio com prompt invertido** (preservar rosto/corpo, alterar cabelo — mesmo contrato `TryOnProvider`, mesma família de tabelas, mesma comparação lado a lado já pronta no frontend). O trabalho de F1+ é: (a) 1 chave em `verticals.ts` + 1 blueprint `beleza_salao_v1`; (b) 1 família `beauty_*` de tabelas espelhando `fashion_*` + 1 provider; (c) 3 conectores finos ao `products_services` (duração lida pela agenda, N:N profissional↔serviço, `maintenance_days` opcional); (d) 5 command handlers novos no MESMO registry; (e) endurecimento **transversal** de consent+quiet-hours+frequency-cap (aditivo, beneficia todas). Nenhum motor novo. Plano F0–F18. Análise em `docs/prd/ANALISE-PRD-BELEZA-SALOES-vs-CODEBASE.md`.

**Prioridade:** P0 — primeira vertical nova após a onda inicial (ADR-092 §60 já a previa como `💇 beleza`; ficou represada até o Bloco A do Autônomo, que já fechou).
**Acesso:** vertical + Beauty AI opt-in por flag; blueprint atribuído pelo Master Admin; dinheiro/margem role-gated; custo de IA restrito ao Master Admin.
**Tenant piloto:** **Studio de Beleza Márcia** — configurado exclusivamente por dado (`vertical_blueprints` + `organization_settings` + seeds), **nunca** por constante hardcoded (§17/§65 do PRD).
**Estratégia:** REUTILIZAR → CONFIGURAR → ESTENDER → **só então** CRIAR (§4 do PRD).
**Dependência dura:** ADRs 060 (Appointment), 145 (Clínica Jornada), 034–044/103–104 (Fashion Studio), 059/072/091/092/095/153 (planos/módulos/blueprint/RBAC), 073/074 (JobQueue/Scheduler), 075 (Storage), 130 (AI Governance), 136 (BusinessSignal), 152/158/159 (Runtime/espinha/Autonomy), 156 (External Intelligence), 163 (Invisible UX), 165 (Outcome Assurance), 166 (Enterprise Learning), 167/168 (Social/Growth) — **todos FECHADOS**.
**Não é:** novo CRM, nova agenda, novo Estúdio, novo motor de simulação, novo gateway de IA, novo executor, nova tabela de alerta, nova tela de credenciais, nem `if vertical === "beauty"` espalhado.

> **Regra de ouro (PRD 12):** *`SIMULAÇÃO ≠ AGENDAMENTO ≠ ATENDIMENTO ≠ RESULTADO`. Cada elo da jornada Descobrir→Experimentar→Decidir→Agendar→Atender→Recomprar tem sua própria confirmação e evidência.* Herda `DONE ≠ RESULTADO` (PRD 8) e `ENGAGEMENT ≠ BUSINESS VALUE` (PRD 11).

---

## 1. Contexto e objetivo

O PRD "Vertical Beleza & Salões" (v1.0, 14/08/2026) pede uma nova vertical para salões/barbearias/estética/nail designers, com o Studio de Beleza Márcia como piloto. A ordem é dura: **não desenvolver de forma hardcoded para o Studio** — tudo deve ser configurável para que um segundo salão (ex.: "Salão Teste B") receba a vertical sem alteração de código (§65 — o teste definitivo).

A vertical combina operação (agenda, clientes, profissionais, serviços, atendimento, produtos, comissão, campanhas, reputação, métricas) com um domínio novo específico do segmento: **Beauty AI** (Simulador de Cabelo, Análise de Harmonia Visual, Look→Serviços do catálogo real, Look→Agendamento). O PRD é enfático que Beauty AI **não pode ser entretenimento** — a simulação existe para virar agendamento e receita, sempre grounded no catálogo real do tenant, nunca inventando serviço/preço/promoção.

**Achado F0 (auditoria completa em 4 frentes — `docs/prd/ANALISE-PRD-BELEZA-SALOES-vs-CODEBASE.md`):** o ZapFlow já entrega ~85% do que a vertical exige. Os três motores de maior porte — try-on de imagem, agenda com profissional/sala/especialidade/ciclos, execução governada com aprendizado — já existem, provados em produção e generalizáveis por registro/registro/registry, não por reescrita. As lacunas reais são de borda (registrar a chave, criar o blueprint, +5 tabelas do domínio Beauty AI, 3 conectores finos ao catálogo) e um endurecimento transversal que a vertical traz à tona (consent `comunicacoes` transversal + quiet-hours + frequency-cap) mas que beneficia todas as verticais.

---

## 2. F0 — Auditoria do `main` (evidência `file:symbol`)

**Conclusão: ~85% é COMPOR/REUTILIZAR/CONFIGURAR.** (Auditoria completa em `docs/prd/ANALISE-PRD-BELEZA-SALOES-vs-CODEBASE.md`.)

| # | Superfície | `file:symbol` | Veredito | Papel / lacuna |
| --- | --- | --- | --- | --- |
| 1 | **Registro de vertical** | `verticals.ts:8-9` (`VerticalKey`), `:94-148` (`VERTICALS`), `:23-32` (`CONSENT_BY_VERTICAL`) | EXISTE (registry como dado) | +1 objeto (`beleza`) |
| 2 | **Blueprint por nicho** | `VerticalBlueprintService.ts`, `BlueprintSeeder.ts:49-168`, `vertical_blueprints`/`organization_blueprints` (`db.ts:7555-7593`) | EXISTE | +1 seed (`beleza_salao_v1`) |
| 3 | **Entitlements + gate server-side** | `EntitlementService.check:204`; `SocialEntitlementService.assertAllowed:49` (molde) | EXISTE | Reusar verbatim |
| 4 | **RBAC + role_profiles** | `PermissionService.ts:129-152`; `role_profiles`/`role_permissions` (`db.ts:5659-5675`); `ContextProjectionService.hasFullBusinessVisibility:141` (§73) | EXISTE | Perfis por-vertical: **opcional** F3 |
| 5 | **CRM canônico** | `contacts` (`db.ts:37-47` + `avg_ticket`, `memory_facts`, `tags`, `notes`); `CustomerProfileService`, `CustomerMemoryService` | EXISTE | Sem custom fields JSON (gap conhecido; usar `memory_facts`/`tags`) |
| 6 | **Agenda + profissional + sala + duração** | `appointments` (`db.ts:221-235` + `professional_id:3117`, `room_id:3119`, `expected_duration_minutes:3121`); `ClinicAgendaService` completo | EXISTE | Reusar; renomear em UI |
| 7 | **Anti-double-booking (AC-012)** | `ClinicScheduleSessionService.ts:322-374` (tx: COUNT dentro → INSERT); race-por-unique `ClinicAgendaService.scheduleFollowUp:507-528` | EXISTE (o molde canônico) | Reusar |
| 8 | **Disponibilidade prof+ausência+sala** | `ClinicScheduleSessionService.availability:567-648`; `ClinicProfessionalAbsenceService.overlaps:170-186` | EXISTE (único com todos os filtros) | Reusar |
| 9 | **Profissional desacoplado de login** | `clinic_professionals` (`db.ts:2576-2586`, `user_id` **opcional**) + PIN (`:3228-3230`) | EXISTE | Reusar (a manicure não precisa de conta) |
| 10 | **N:N profissional↔especialidade** | `clinic_professional_specialties` (`db.ts:2734-2747`); `ClinicSpecialtyService.listProfessionalsForSpecialty:218-248` | EXISTE | **N:N profissional↔serviço**: gap (F4) |
| 11 | **Sala/cabine com capacidade** | `clinic_rooms` (`db.ts:2589-2595` + `capacity:2943`) | EXISTE | Reusar |
| 12 | **Ciclo renovável (10 sessões, saldo RN-004)** | `ClinicTreatmentCycleService` (`usage:157`, `renew:426`); `clinic_treatment_cycles` (`db.ts:2850-2878`) | EXISTE | Reusar (pacote de 10 escovas) |
| 13 | **Janela de retorno / manutenção** | `clinical_encounters.follow_up_recommended_days` (`db.ts:3322`); `ClinicAgendaService.followUpQueue:542-580` | EXISTE | Reusar |
| 14 | **Ficha extensível por atendimento** | `clinical_encounters.form_data TEXT` (JSON livre, `db.ts:3252-3271`) | EXISTE | Reusar (fórmula de coloração) |
| 15 | **Lista de espera / vaga por cancelamento** | `ClinicVacancyService` (`tryOfferOnCancel:82`, `pickCandidate:172`, `handleReply:264`) | EXISTE | Reusar |
| 16 | **Remarcação por WhatsApp (3 horários)** | `ClinicRescheduleService` (`findSlots:79`, `createOffer:138`, `handleChoice:192`) | EXISTE | Janela hardcoded 8-18 (`:36-37`) — trocar por `agenda_open_hour` |
| 17 | **Lembrete 24h + opt-out `PARAR`** | `ClinicReminderService.dispatch:215`; `ClinicReminderReplyService.ts:45` (OPTOUT revoga consent `comunicacoes`) | EXISTE | Reusar |
| 18 | **Catálogo (product/service)** | `products_services` (`db.ts:196-209` + `duration_minutes:766`, `metadata_json:207`, `category:1326`); `inventory_items.avg_cost:763` | EXISTE | `duration_minutes` órfã (gap F4) |
| 19 | **Try-on virtual completo** | `FashionTryOnService.ts:41-46` (contrato); `FashionAvatarService` (consent+quarentena+EXIF+URL assinada); `fashion_*` tabelas | EXISTE (motor exato) | Espelhar como `beauty_*` |
| 20 | **Comparação lado a lado (UI)** | `src/storefront/FashionStudio.tsx:326-338, 758-775` | EXISTE | Reusar 1:1 |
| 21 | **Gateway de IA + custo** | `llm.ts` (`chat`, `generateImageB64`, `editImagesGoogleB64`, `editImagesB64`); `recordUsage:70-105` → `ai_usage_log`; `PlanService.studioAllowed` | EXISTE | Reusar |
| 22 | **Storage privado + URL assinada** | `fileSigning.ts` (`sha256(JWT_SECRET:{scope}_v1)`, TTL 15min); 6 escopos hoje | EXISTE (padrão canônico) | +1 escopo `beauty_private_media_v1` |
| 23 | **JobQueue + retry + idempotência** | `JobQueueService:55` (`enqueue`, `errorClass`, `sweepStale:114`); handlers `fashion_tryon`/`storefront_look_image` | EXISTE | Reusar |
| 24 | **Estúdio de Criação** | `StudioService.ts:55`; `BrandDnaService`, `Hook/Script/ChannelAdaptation/CreativeVariantService`; `GovernedPublishService.propose:40` | EXISTE | Reusar |
| 25 | **Execution Runtime + Autonomy** | `DecisionActionService.propose:40`; `ApprovalPolicyService.resolveContract:110`; `CommandExecutorService.execute:188` (21 handlers); `ConfirmationEngine.expect:89`/`confirm:166`/`sweepTimeouts:238` | EXISTE (completo) | Reusar; +5 handlers `beauty_*` |
| 26 | **BusinessSignal + dedupe + TTL** | `business_signals` (`db.ts:5927-5949`); `BusinessSignalService.publish:58` + `attention:109`; `SignalProcessRouterService.TRIGGER_MAP:55-63` | EXISTE | Reusar |
| 27 | **Fala Tu (choke-point `confirm`)** | `FalaTuService.interpret:192`/`confirm:439` (silo + porta I/O opt-in); paridade WhatsApp `:91` | EXISTE | Estender enum + switch |
| 28 | **Detector "cliente inativa"** | `ChurnRiskDetectorService.ts:55` (só olha `orders`); `Scheduler.reactivationPass:1521` | EXISTE (parcial) | Novo fator: dias desde último `appointment` (F11-F12) |
| 29 | **Consent `comunicacoes`** | 24 call-sites, **todos `Clinic*`** | ⚠️ **GAP LOAD-BEARING** | Mover para sink transversal (F5-transversal) |
| 30 | **Quiet-hours / frequency-cap outbound** | ❌ (só push interno ao dono) | AUSENTE | CRIAR mín. transversal (F5-transversal) |
| 31 | **CSAT 1-5 pós-`orders.paid_at`** | `satisfaction_surveys` (`db.ts:874-888`); `Scheduler.npsPass:1847` | EXISTE (gatilho: pedido) | Não há survey por atendimento; "pedir review Google" → F13 |
| 32 | **Reputação (ingerir + investigar + responder)** | 15 services `Reputation*`; `ReputationReplyService.ts:109` | EXISTE | Reusar |
| 33 | **LGPD (consent + retenção + esquecimento)** | `contact_consents` (`db.ts:5329-5344`); `LgpdService` completo; `maskIdentifier:17-22` | EXISTE | Reusar |
| 34 | **Outcome Assurance + Learning** | `OutcomeAssuranceService.assessAction:34`; `BusinessOutcomeResolver` registry; `PatternMemoryService` (único) | EXISTE | +1 resolver `BeautyOutcomeResolver` |
| 35 | **`AiGovernanceService.PEOPLE_AFFECTING`** | `AiGovernanceService.ts:19-40` | EXISTE | +1 entrada `estetica_appearance_advice` (F8) |

---

## 3. Decisões arquiteturais

- **D1 — `SIMULAÇÃO ≠ AGENDAMENTO ≠ ATENDIMENTO ≠ RESULTADO` é invariante.** Cada elo tem confirmação própria; nenhum coisa "está pronta" sem o próximo elo. `beauty_visual_consultations.status='selected'` NÃO cria appointment; `appointments.status='confirmed'` NÃO conta como atendimento; `completed` sem `clinical_encounters` fecha via `OutcomeReconcilerService` (herdado PRD 8).
- **D2 — Vertical = dado, não código.** Registrar `beleza` em `verticals.ts` (§1 auditoria) é 1 linha; blueprint `beleza_salao_v1` é seed via Master Admin (`POST /api/admin/blueprints` + `/publish` + `/organizations/:id/blueprint`). O segundo salão nasce sem deploy. `if vertical === "beauty"` é proibido — quem decide é `EntitlementService`, `NavigationManifestService`, `PermissionService`, `AiGovernanceService` (§7 do PRD).
- **D3 — Simulador de Cabelo = Fashion Studio com prompt invertido.** Mesmo contrato `TryOnProvider` (`FashionTryOnService.ts:41-46`), mesma família de tabelas (`beauty_*` espelha `fashion_*`), mesmo provider real (Gemini `gemini-2.0-flash-exp`), mesmo `input_hash` para idempotência, mesma fila `maxAttempts:1`, mesma URL assinada HMAC, mesma comparação lado a lado no frontend. O prompt inverte a regra: preservar rosto/corpo (herdado do `SAFETY_PROMPT:51-55`), **alterar cabelo**. NÃO cria segundo motor de simulação (§8 do PRD).
- **D4 — Recomendador Look→Serviços é DETERMINÍSTICO e GROUNDED no catálogo.** `LookServiceRecommendationService` cruza `beauty_reference_looks.suggested_services_json` × `products_services` do tenant; sem match → `insufficient_catalog` (nunca inventa serviço, preço, promoção — RN-BS-02). Sem serviço no catálogo → não sugere. Mesma barra do PRD 11 (`ENGAGEMENT ≠ BUSINESS VALUE`) aplicada ao domínio Beleza.
- **D5 — Agenda com profissional/sala/especialidade = Clínica.** A tabela `clinic_professionals` é agnóstica ao domínio (o nome é histórico; modela "quem atende, desacoplado de login, com PIN opcional"). A vertical Beleza a renomeia em UI para "Profissional" e reusa `ClinicAgendaService`, `ClinicScheduleSessionService.availability`, `ClinicVacancyService`, `ClinicRescheduleService`, `ClinicTreatmentCycleService`, `ClinicProfessionalAbsenceService`. NÃO cria segunda agenda (§8 do PRD). O blueprint `beleza_salao_v1` NÃO liga o módulo `clinica` (evita expor prontuário/TISS/etc. — só reusa os services).
- **D6 — N:N profissional↔serviço: decisão em F4.** Duas opções:
  - **(a) `professional_services` (tabela nova) **— mín. `(org, professional_id, service_id, active, is_primary, commission_percent)`. Mais claro semanticamente, permite comissão.
  - **(b) `clinic_professional_specialties` mapeando cada serviço como especialidade** — reuso puro, sem tabela nova; mas exige semear especialidade por serviço.
  - Recomendação: **(a)** — evita ambiguidade "corte é especialidade ou serviço?" e libera comissão por serviço nativamente.
- **D7 — Ficha técnica = `clinical_encounters.form_data`.** JSON livre por atendimento — o único campo extensível por atendimento em todo o sistema. Ficha de coloração (fórmula, produto, tempo, sensibilidade) vai aí. NÃO cria tabela nova.
- **D8 — Automação = comando GOVERNADO.** Manutenção detectada, retorno sugerido, vaga aberta, pedido de review → tudo via `DecisionAction → ApprovalPolicy(Autonomy Contract) → CommandExecutor → ConfirmationEngine` (D4 herdado PRD 10). +5 command handlers no MESMO registry (`beauty_maintenance_offer`, `beauty_review_invite`, `beauty_vacancy_offer`, `beauty_simulation_followup`, `look_service_recommendation`). NÃO cria segundo executor (§8).
- **D9 — Consent `comunicacoes` transversal, quiet-hours, frequency-cap.** A vertical Beleza torna explícito um gap load-bearing que hoje só o módulo Clínica endereça. F5-transversal move o check `LgpdService.hasConsent(orgId, contactId, 'comunicacoes')` para o sink (`MessageProviderService.sendMessage`), adiciona quiet-hours e frequency-cap. **Opt-in por flag global** — `outbound_consent_required`, `client_quiet_hours_enforced`, `client_frequency_cap_enforced` (default 0 para não quebrar tenants existentes; ligado no Studio Márcia no piloto).
- **D10 — Autopilot shadow-first (herdado RN-CG-10).** Beauty Autopilot propõe manutenção/retorno/review-invite/publicação mas NUNCA executa em `off` ou `auto` — sempre GOVERNADO. Aceitação da dona vira comando via `DecisionAction.propose`, nasce `awaiting_approval`, atravessa a autonomia.
- **D11 — Fotos = LGPD Art.5 II (dado pessoal sensível).** Consent tipado (`hair_simulation`) **separado** de consent `use_in_marketing` (§26 PRD — uma não implica a outra). Quarentena antes do processamento. EXIF strip. Storage privado. URL assinada HMAC (`beauty_private_media_v1`, TTL 15min). Retenção configurável (default 30d). Purga preguiçosa + Scheduler.
- **D12 — IA nunca julga aparência.** Herdado do Fashion Studio (`llm.ts:593` — *"NUNCA descreva, avalie ou comente corpo, peso, beleza ou aparência"*). Harmonia Visual é linguagem DESCRITIVA (contraste/equilíbrio/destaque), nunca ranking. `AiGovernanceService.PEOPLE_AFFECTING += "estetica_appearance_advice"` — sugestão estética passa a exigir ator humano + motivo, senão lança `human_decision_required`.
- **D13 — Dinheiro role-gated (§73 herdado).** `ContextProjectionService.hasFullBusinessVisibility:141` — recepção/cabeleireira nunca veem financeiro global; owner/admin sim. Vale para dashboard, Fala Tu, resultado de execução.
- **D14 — Isolamento cross-tenant duro.** Token do cliente do salão (portal) usa segredo próprio (padrão `FashionCustomerService.ts:28` — `sha256(JWT_SECRET:beauty_customer_v1)`). Toda query com `organization_id`. `beauty_avatar_assets`/`beauty_visual_consultations`/etc. **JAMAIS compartilhados entre tenants**.
- **D15 — Motor único de aprendizado.** Creative Learning e futuras aprendizagens Beleza escrevem em `PatternMemoryService` (§184 herdado — sem 2º Learning Engine). Só `assured` ensina forte (RN-EL-1).

---

## 4. Guardrails duros (RN-BS — no header dos services + testados)

- **RN-BS-01** — `SIMULAÇÃO ≠ AGENDAMENTO ≠ ATENDIMENTO ≠ RESULTADO`: cada elo com confirmação própria.
- **RN-BS-02** — IA NUNCA sugere serviço/produto/preço/promoção fora do catálogo do tenant (`products_services`). Sem match → `insufficient_catalog`.
- **RN-BS-03** — IA NUNCA julga aparência: Harmonia Visual descritiva, nunca ranking; `AiGovernanceService.PEOPLE_AFFECTING += "estetica_appearance_advice"`.
- **RN-BS-04** — Fotos são dado pessoal sensível: consent tipado antes do processamento; simulação e marketing são consents SEPARADOS; quarentena; EXIF strip; storage privado; URL assinada TTL 15min; retenção configurável + purga.
- **RN-BS-05** — Logs jamais contêm foto/base64/prompt com PII; `safety_report_json` só flags; `ux_telemetry_events` sanitiza via `safeId`.
- **RN-BS-06** — Idempotência real na simulação: `input_hash = sha256(avatarKey:params:provider.key)` — retry nunca cobra 2x.
- **RN-BS-07** — Isolamento cross-tenant duro: token do portal do cliente do salão tem segredo próprio; `beauty_*` sempre filtra `organization_id`.
- **RN-BS-08** — Dinheiro role-gated: recepção/cabeleireira nunca veem financeiro global; owner/admin sim (§73).
- **RN-BS-09** — Consent `comunicacoes` transversal (F5): nenhum outbound (campanha/cadência/oportunidade/review-invite) envia sem `LgpdService.hasConsent(..., 'comunicacoes')` quando flag ativa.
- **RN-BS-10** — Quiet-hours + frequency cap: default 8h-21h São Paulo, 3 msg/dia/contato (F5-transversal).
- **RN-BS-11** — Nunca inventa dado ausente: sem foto → sem simulação; sem serviço no catálogo → não sugere; sem histórico → não infere preferência.
- **RN-BS-12** — Shadow-first para autopilot: propõe mas NUNCA executa em `off`/`auto` — sempre GOVERNADO (RN-CG-10 herdada).

---

## 5. Plano de fatias — status

Cada fatia = 1 PR draft → CI verde → merge → next. Fluxo padrão do repo (CLAUDE.md).

| Fatia | Escopo | Status |
| --- | --- | --- |
| **F0** | doc-only: `docs/prd/ANALISE-PRD-BELEZA-SALOES-vs-CODEBASE.md` + este ADR-169; Reuse Matrix, Impact/Dependency/Data Model/Migration/Regression/Flag/Rollback/Security/AI-Cost/E2E, RN-BS-01..12, plano F1–F18 | **MERGED (PR #1121)** |
| **F1** | Registro da vertical: +1 chave em `VerticalKey`/`VERTICALS`/`CONSENT_BY_VERTICAL` + `EntitlementService.FALLBACK_HIDDEN_BY_VERTICAL`; `test:beauty-registry` 42/42 | **MERGED (PR #1122)** |
| **F2** | Blueprint `beleza_salao_v1` publicado + inferência + piloto por dado; `test:beauty-blueprint-piloto` 54/54 | **MERGED (PR #1123)** |
| **F3** | 3 perfis CUSTOM por-vertical (Recepção/Cabeleireira/Gerente Beleza) semeados condicionalmente pelo blueprint; `test:beauty-profiles` 75/75 | **MERGED (PR #1124)** |
| **F4** | Duração do serviço lida pela agenda + N:N professional_services com comissão nativa; `test:beauty-service-duration` 25/25 + `test:beauty-professional-services` 52/52 | **MERGED (PR #1125)** |
| **F5** | Fundação da Beauty AI: consent tipado + upload+quarentena+EXIF strip + URL assinada TTL 15min via `fileSigning` canônico + retenção configurável + LGPD Art.18; `test:beauty-visual-consultation` 71/71 | **MERGED (PR #1126)** |
| **F6** | Simulador de Cabelo — provider plugável (Stub + Gemini) + fila + idempotência + prompt invertido + vocab fechado; `test:beauty-hair-simulation` 61/61 | **MERGED (PR #1127)** |
| **F7** | Backend HTTP completo (10 endpoints admin + rota pública media assinada); `test:beauty-routes` 40/40 E2E | **MERGED (PR #1128)** |
| **F8** | `BeautyHarmonyAnalysisService` (365L, DETERMINÍSTICO — 0 LLM) + nova tabela `beauty_visual_analyses` + entrada `estetica_appearance_advice` em `PEOPLE_AFFECTING` (RN-BS-03 codificado como código — sugestão estética exige actor+reason via `guardApplied`, senão `human_decision_required`). 5 dimensões em vocab fechado (contraste/equilíbrio/destaque/volume/intensidade), narrativa por template com VERBOS de efeito (nunca adjetivos da pessoa), validador hard rejeita 20+ palavras proibidas (bonito/feio/atraente/nota/rank/melhor/pior/envelhec/rejuvenesc/embel...), disclaimer sempre embutido. Cada análise gera 1 linha em `ai_decisions` (auditoria ADR-130). 3 rotas HTTP: `/vocabulary/harmony` + `POST /consultations/:id/analysis` + `GET /analyses`. Determinismo comprovado: 2 análises mesmos inputs = mesma narrativa. `test:beauty-harmony-analysis` 61/61. Regressão: `test:ai-governance` 33/33, 7 beauty verdes, tsc limpo | **MERGEADA (PR #1129)** |
| **F9** | `LookServiceRecommendationService` (~330L, DETERMINÍSTICO — 0 LLM): elo comercial da Beauty AI (sim/consulta → serviços do catálogo REAL). Grounded em `products_services` (type='service', active=1) + `beauty_reference_looks.suggested_services_json`. 3 níveis de relevância (`primary` curado pelo salão · `matched` LIKE em vocab fechado · `generic` reservado). 2 vocabs FECHADOS exportados (`KEYWORDS_COLOR` 16 · `KEYWORDS_CUT` 7). Primary vem PRIMEIRO na ordem de curadoria; dedupe por id. Read-only puro (0 INSERT). Cross-tenant DURO (refLook de outra org ignorado silenciosamente). Determinismo comprovado. Catálogo vazio → `insufficient_catalog` (nunca inventa serviço — RN-BS-11). 3 rotas: `/vocabulary/recommendations`, `/simulations/:id/recommendations`, `/consultations/:id/recommendations`. `test:beauty-look-to-services` 51/51. Regressão: 6 testes beauty verdes, tsc limpo | **MERGEADA (PR #1130)** |
| **F10** | `BeautyLookToAppointmentService` (~430L, DETERMINÍSTICO — 0 LLM): FECHA O CICLO da Beauty AI (§7/§46). Composição PURA (nenhum motor novo): `ProfessionalServiceService.listProfessionalsFor` (F4) + `ClinicScheduleSessionService.availability` (ADR-145 F41, mesma tabela `clinic_professionals`) + `AppointmentService.create` (F4/ADR-160 F6) + `beauty_visual_consultations.scheduled_appointment_id` (F5, coluna já existente). Snapshot visual DERIVÁVEL via FK (RN-004 — quando a foto é purgada por retenção, snapshot some naturalmente — LGPD). 3 métodos: `availability` (retorna `[{professional, slots[]}]` com primary primeiro), `book` (cria appointment + amarra ao snapshot, idempotente), `consultationForAppointment` (lookup derivado). 8 erros tipados (nunca 500 opaco). Guard explícito de per-professional conflict (409). Populava `professional_id` + `professional_name_snapshot` + `product_service_id`. 2 rotas HTTP: `GET /consultations/:id/availability`, `POST /consultations/:id/book`. RN-BS-07/11/12/03 codificados. `test:beauty-look-to-appointment` 46/46. Regressão: 9 testes beauty verdes + `test:clinic-agenda` 27/27 + `test:clinic-episode-appointment` 34/34 + `test:falatu-bridge-recon` 15/15, tsc limpo | **MERGEADA (PR #1131)** |
| **F5-transversal-A** | `OutboundConsentGuardService` (~180L) — gate LGPD `comunicacoes` transversal no SINK canônico `MessageProviderService.sendMessage`. 1 flag opt-in `outbound_consent_required` (default 0 = 0-regressão). Beneficia TODOS os ~30 callers de sendMessage (Cadence/Playbook/Reminder/Radar/etc — não só beleza). `OutboundBlockedError` com code tipado. RN-BS-04/07/11 codificados. `test:beauty-outbound-consent-transversal` 25/25. Regressão: 6 beauty verdes + 5 callers verdes (clinic-followup/reminder/vacancy/followup-notice/radar-send), tsc limpo | **MERGEADA (PR #1132)** |
| **F5-transversal-B** | `ClientQuietHoursGuardService` (~190L) — janela silenciosa por org (default 22h→8h SP, custom pelo dono, cross-midnight aware). 3 colunas aditivas em `organization_settings` (`client_quiet_hours_enforced` flag opt-in + `client_quiet_hours_start_hour/end_hour` opcionais). Segundo gate no MESMO sink canônico, depois do consent guard (F5-A). `evaluate(orgId, now?)` determinístico pra teste. `start==end` = 24h silêncio (pausa total). `OutboundQuietHoursError` com code tipado + hourSP + janela. NÃO confunde com `proactive_awake_*` do UxPreferences (aquele é do DONO; este é do CLIENTE). RN-BS-11/12/07 codificados. `test:beauty-quiet-hours-transversal` 48/48. Regressão: 6 beauty verdes + 5 callers verdes, tsc limpo | **MERGEADA (PR #1133)** |
| **F5-transversal-C** | `ClientFrequencyCapGuardService` (~230L) — cap de N mensagens/janela por contato (default 3/24h). 3 colunas aditivas em `organization_settings` + 1 tabela nova `outbound_send_log` (store isolado dos `messages`). Terceiro gate no MESMO sink, depois de consent + quiet-hours. Two-phase: `evaluate` decide; sink fetches; `record` grava só quando allow + sucesso do provider (best-effort). `evaluate(orgId, id, now?)` determinístico. `OutboundFrequencyCapError` com used/cap/windowHours. Unknown contact permite + não-registra. RN-BS-11/12/07 codificados. `test:beauty-frequency-cap-transversal` 35/35. Regressão: 3 guards F5-transversal + 5 callers verdes + look-to-appointment, tsc limpo | **MERGEADA (PR #1134) — FECHA F5-transversal completa (3/3 fatias)** |
| **F11** | `AbandonedBeautySimulationDetector` (~200L, DETERMINÍSTICO — 0 LLM): detecta consultas 'ready' com sim SUCCEEDED sem 'selected' há >X horas (default 24h) e publica na espinha canônica (`business_signals` dedupe `beauty:abandoned_simulation:{consultationId}` — D6, sem tabela paralela). 2 colunas aditivas em `organization_settings` (`beauty_abandoned_detector_enabled` flag opt-in + `beauty_abandoned_after_hours`). Registrado no Scheduler.tick. Consent hair_simulation revogado → NÃO publica (leitura live, RN-BS-11). contactName só com consent comunicacoes (RN-BS-04). NÃO envia mensagem — só sinaliza (RN-BS-12). Sinais têm severity=attention, basis=fact, subjectType=contact. RN-BS-07 codificado. Fatia futura F11-B: Autopilot em SHADOW propondo follow-up via DecisionAction+ApprovalPolicy freado pelos 3 gates da F5-transversal. `test:beauty-abandoned-simulation` 44/44. Regressão: 3 guards + 4 beauty verdes, tsc limpo | **MERGEADA (PR #1135)** |
| **F12** | `BeautyMaintenanceDetector` (~200L, DETERMINÍSTICO — 0 LLM): publica `business_signal` pra par (contato, serviço) com `products_services.maintenance_days` vencido sem próximo appt. +1 coluna aditiva em `products_services` (`maintenance_days`, opt-in por serviço, NULL=sem manutenção) + 1 flag opt-in por org. Query única com NOT EXISTS filtra só o mais recente por par. Ignora cancelled/no_show. Dedupe `beauty:maintenance_due:{contactId}:{serviceId}` no `business_signals`. Registrado no Scheduler.tick. RN-BS-04/07/11/12 codificados. Handler `beauty_maintenance_offer` fica pra fatia futura F11-B (Autopilot). `test:beauty-maintenance-detector` 29/29. Regressão: F11 + 3 guards + 5 outros beauty + clinic verdes, tsc limpo | **em andamento (esta PR)** |
| **F13** | Handler `beauty_review_invite` (pedir avaliação governada; grounded no atendimento assured; nunca sem consent `comunicacoes`); `test:beauty-review-invite` | pendente |
| **F14** | Detector "horário ocioso + cliente elegível" (composição `ClinicVacancyService` + `followUpQueue`) publica `beauty_vacancy_opportunity`; handler `beauty_vacancy_offer`; `test:beauty-vacancy-opportunity` | pendente |
| **F15** | Fala Tu intents de beleza (extende enum + switch conforme padrão FalaTuService §1.8 da auditoria); `test:beauty-falatu-intents` | pendente |
| **F16** | Métricas Beauty AI em `ux_telemetry_events` (whitelist +N event_types); dashboard master de custo/uso (reusa `AiUsageDashboardService`); `test:beauty-metrics` | pendente |
| **F17** | `test:beauty-golden-paths` (Studio Márcia fixture, fluxo E2E do §7 da análise); `test:beauty-hardening` codifica RN-BS-01..12 como regressão + runbook `docs/runbook/beleza-operacao.md`; **FECHA o ADR-169** | pendente |
| **F18** | **Prova §65**: cria "Salão Teste B" via blueprint, configura profissional/serviços/horários próprios — **sem alterar código** — e roda `test:beauty-tenant-b-generalization`. Se precisar tocar código, F18 volta a in-progress | pendente |

---

## 6. Rollout

**OFF (default) → shadow → single-tenant (Studio Márcia) → cohort → GA.** Todas as flags nascem `default 0`. Studio Márcia recebe ativação manual pelo Master Admin no piloto. Autopilot **nunca vai direto a GA** (RN-BS-12/RN-CG-10 herdada). F18 é o gate para GA: se a generalização "Salão Teste B" precisar tocar código, a arquitetura não está pronta.

---

## 7. Testes por fatia

Padrão do repo (CLAUDE.md fluxo de fatia): cada fatia entrega 1 script `scripts/test-beauty-*.ts` (idempotente, `tmpDir` isolado, helper `check(name, ok)`, cobre happy path + edge + audit + multi-tenant). Adicionado ao `package.json` + matrix do `.github/workflows/ci.yml`. F17 (`test:beauty-hardening`) tem dupla função: (a) codifica RN-BS-01..12 como regressão tocando os serviços REAIS de F1–F16; (b) verifica fiação de produção (services importáveis, rotas montadas, passes no Scheduler, handlers no registry canônico, runbook presente).

Regressão em cada fatia: rodar toda a suíte anterior + suíte das outras 8 verticais (`test:falatu*`, `test:clinic-*`, `test:reputation-*`, `test:social-*`, `test:growth-*`, `test:comigo-*`, `test:retail-*`, `test:escola-*`). Flag OFF preserva comportamento atual de tenants existentes (matriz A do PRD §58).

---

## 8. Consequências

**Positivas:**
- Segundo salão nasce por blueprint, sem deploy — cumpre §17/§65 do PRD.
- Reuso máximo: Clínica libera agenda profissional/sala/especialidade/ciclo; Fashion libera try-on; Runtime libera automação governada; Learning libera aprendizado — nada de motor duplicado.
- Endurecimento transversal (F5-transversal) beneficia todas as verticais existentes (consent `comunicacoes`, quiet-hours, frequency-cap) — aditivo, opt-in, reversível.
- Studio Márcia como fixture real pra `test:beauty-golden-paths` — a "dona de baixa afinidade com tecnologia" (§11 PRD) é aferida no teste E2E, não apenas em produção.

**Riscos e mitigações:**
- **Custo de IA** (simulador de cabelo): créditos por dia + idempotência por `input_hash` + master-only observability. Escalar quota com plano.
- **Guardrail "IA não julga aparência"**: entrada em `PEOPLE_AFFECTING` + prompt herdado + narrativa determinística. `test:beauty-harmony-analysis` codifica a regra.
- **Consent transversal (F5)**: opt-in por flag global; default 0 não quebra tenants; audit de bloqueio em cada sink.
- **Generalização (F18)**: teste explícito. Se falhar, é a arquitetura que ainda não está pronta — F18 volta a in-progress e itera até passar.

**Neutras / trade-offs:**
- Reusar `clinic_*` tables e services para uma vertical "beleza" pode parecer estranho semanticamente, mas o custo de renomear é maior que o benefício (breaking changes em 15+ ADRs, retrocompatibilidade). O nome da tabela é histórico; o modelo é agnóstico. F17 documenta claramente no runbook a decisão.
- Não abrir módulo-corpo `beleza` no `PLAN_GRADE` inicialmente — a vertical usa módulos já existentes (`agenda`, `catalogo`, `vendas`, `campanhas`, `estudio`, `diretor`, `rie`, `execucao`, `copiloto`) + `beauty_hair_simulator_enabled` como flag sub-feature. Se um bundle "Beleza + Simulador" for necessário comercialmente, entra em `PLAN_BUNDLES` como aditivo (molde: `growth_clinica` em `plansGrade.ts:106-124`).

---

## 9. Referências

- **PRD:** "Vertical Beleza & Salões — ZapFlow", v1.0, 14/08/2026 (documento origem)
- **Análise F0:** `docs/prd/ANALISE-PRD-BELEZA-SALOES-vs-CODEBASE.md` (Reuse Matrix + 30 perguntas do PRD §6 + Plans + E2E)
- **ADRs pai:**
  - ADR-092 (distribuição por vertical — §60 previa `💇 beleza`)
  - ADR-145 (Clínica Jornada — molde da agenda profissional/sala/especialidade/ciclo)
  - ADRs 034–044, 103–104 (Fashion Studio — molde do try-on)
  - ADR-153 (Vertical Entitlements / Blueprint)
  - ADR-060 (Appointment), ADR-072 (Module), ADR-091 (Planos), ADR-095 (RBAC granular)
  - ADR-136 (BusinessSignal), ADR-152/158/159 (Runtime + espinha + Autonomy)
  - ADR-163 (Invisible UX — molde do "Hoje" por exceção)
  - ADR-165 (Outcome Assurance), ADR-166 (Enterprise Learning)
  - ADR-167/168 (Social + Growth — molde da publicação governada e da conversão)
- **CLAUDE.md convenções críticas:** nº 1 (isolamento), nº 2 (CREATE-then-ALTER), nº 4 (HMAC), nº 6 (LGPD), nº 10 (feature flags), nº 12 (BusinessSignal)
