# Checklist acumulado — PRD WhatsApp Unificado / Fala Tu

> Mantido no repo conforme §22 do PRD. Atualizar a CADA entrega (mesmo parcial /
> diagnóstico / correção). Nunca escrever só "feito"/"100%". Estados permitidos:
> `NÃO INICIADO` · `EM ANDAMENTO` · `IMPLEMENTADO` · `VALIDADO` · `BLOQUEADO` ·
> `NÃO APLICÁVEL — JUSTIFICADO`. `IMPLEMENTADO` exige código; `VALIDADO` exige
> evidência de teste/gate. Marcar `[x]` só quando VALIDADO.

## Quadro por fase

| Fase | Itens | Estado | Evidência | Pendência para o gate |
|---|---|---|---|---|
| F0 | F0.1–F0.4 | **EM ANDAMENTO** | `docs/whatsapp-unificado/ANALISE-F0-auditoria-baseline.md` | Contrato Evolution real (Swagger) + fixtures + execução da suíte |
| F1 | F1.1–F1.4 | **IMPLEMENTADO** (escopo enxuto) | F1.1 (`evolution-credential` 6/6) · F1.2a (`meta-webhook-dedup` 10/10) · F1.2b (`whatsapp-jid` 16/16) · F1.2c (`evolution-channel-status` 10/10) · F1.3 (`evolution-reset` 14/14) · F1.4 (`evolution-connect-subscribe` 5/5) | **G1 atendido** no escopo enxuto; difere pra F1.2d a máquina de estados RF-02 + webhook assinado (A10) |
| F2 | F2.1–F2.4 | **IMPLEMENTADO** (escopo enxuto) | F2.1 + F2.2 + F2.3 (`channel-binding-migration` 18/18) + F2.4 (`channel-binding-gate` 11/11) | **G2 atendido** no escopo enxuto; ativação por-produtor do gate difere pra Fase 6 |
| F3 | F3.1–F3.4 | **IMPLEMENTADO** (escopo enxuto) | F3.0 + F3.1a (24/24) + F3.1b (30/30) + F3.2 (20/20) + F3.3a (22/22) + F3.3b (12/12) + F3.4 desambiguação durável (`numbered-list-context` 18/18) | **G3 atendido** no escopo enxuto (CA-04/05/06). Ativação do modo misto por `mixed_mode_enabled` é rollout do piloto (F7) |
| F4 | F4.1–F4.4 | **IMPLEMENTADO** | F4.0 + F4.1 (23/23) + F4.2 (21/21) + F4.3 convergência (`falatu-bridge-convergence` 15/15) + F4.4 prova CA-09 (`falatu-consolidation-ca09` 18/18) | **G4 (CA-09) atendido** |
| F5 | F5.1–F5.4 | **IMPLEMENTADO** | F5.0 + F5.1 (18/18) + F5.2 DOCX (15/15) + F5.3 entrega (14/14) + F5.4 prova CA-07 (`falatu-file-ca07` 13/13) | **G5 (CA-07) atendido** |
| F6 | F6.1–F6.4 | NÃO INICIADO | — | G6 |
| F7 | F7.1–F7.4 | NÃO INICIADO | — | G7 |

## Itens individuais (32)

### Fase 0 — Revalidar e congelar a linha de base
- [ ] **F0.1** — HEAD e achados revalidados; correções já existentes reconhecidas. — **IMPLEMENTADO** (matriz 13/13 + 7 extras, HEAD `40508f1` × auditado `8362838`; evidência na análise F0). Falta só o carimbo de execução da suíte para virar VALIDADO.
- [ ] **F0.2** — Produtores, consumidores, identidades e vínculos mapeados. — **IMPLEMENTADO** (U01–U36 existem; 7 padrões de canal; sem resolvedor único; sem `channel_feature_bindings`; 5 atalhos `wa.me`).
- [ ] **F0.3** — Contrato Evolution e fixtures sanitizadas registrados. — **EM ANDAMENTO / BLOQUEADO** (contrato derivado do código + docs públicas; Swagger da instalação real e fixtures **pendentes** por falta de acesso — Gate G0 permite seguir por contrato).
- [ ] **F0.4** — Baseline, simulação de migração e plano de reversão registrados. — **IMPLEMENTADO** (25/25 testes existem; schema/flags/rollback registrados; simulação numérica de migração fica para F4.1 em modo sem efeitos).

### Fase 1 — Corrigir fundação da conexão
- [ ] **F1.1** — Configuração, credenciais e normalização centralizadas. — **PARCIAL/IMPLEMENTADO** (credencial Evolution centralizada em `MessageProviderService.resolveEvolutionSend`, com prioridade corrigida — token do CANAL primeiro, env fallback; achado A6/RF-02/INV-01. `test:evolution-credential` 6/6; regressões `instagram-send`/`falatu-solo-whatsapp` 72/72/`delivery-receipts`/`channel-health` verdes). **Falta ainda:** normalização de ID/eventos do webhook (movida para junto de F1.2, onde vive a identidade); criptografia real do token de canal — achado X1 — fica como fatia irmã com migração compatível.
- [ ] **F1.2** — Empresa, webhook protegido e estados corretos. — **EM ANDAMENTO** (sub-fatias). **F1.2a IMPLEMENTADO**: dedup do inbound Meta/Cloud + Instagram (achado X2/INV-02) — o handler `/api/webhooks/meta` chama `claimWebhookEvent(provider, wamid|mid)` antes de despachar; retry da Meta não reprocessa (sem duplicar contato/ticket/resposta). `test:meta-webhook-dedup` 10/10; regressões `security-webhook` 12/12, `security-tenant` 7/7, `channel-health`, `delivery-receipts` verdes. **F1.2b IMPLEMENTADO**: `classifyWhatsappJid` (módulo puro `whatsappJid.ts`) valida o JID antes de virar telefone — grupo/status/newsletter/lid/inválido não viram contato de atendimento (achado X3/INV-01); só individual com telefone plausível segue; wired no inbound Evolution do `server.ts`. `test:whatsapp-jid` 16/16. **F1.2c IMPLEMENTADO**: fim do `default_org` — `markEvolutionChannelStatusByIdentifier` (módulo puro `evolutionChannelStatus.ts`) resolve o canal pelo `identifier` ÚNICO e SÓ atualiza status; nunca inventa org nem confia no header spoofável x-organization-id (achados A8/A9/SEC-F4); wired nos 2 pontos do `server.ts` (connect legado + connection.update). `test:evolution-channel-status` 10/10; regressões `security-tenant` 7/7, `falatu-whatsapp` 21/21, `channel-health` 8/8, `falatu-solo-whatsapp` 72/72 verdes. **Faltam (F1.2d futura, fora do escopo enxuto):** máquina de estados sessão/webhook da RF-02 + webhook assinado/URL protegida (A10).
- [ ] **F1.3** — Provisionamento idempotente; sessão protegida contra reset automático. — **IMPLEMENTADO** (removido o delete+recreate silencioso de `connectAndGetQr`; capacidade preservada em `resetInstance` EXPLÍCITO/operador; retorno honesto `needsReset`. `test:evolution-reset` 14/14; regressão `test:falatu-solo-whatsapp` 72/72. VALIDADO vira `[x]` quando a suíte rodar no CI + a rota/UI de reset da Fase 2 expor a operação com confirmação).
- [ ] **F1.4** — Rotas legadas e Solo compatíveis com a fundação comum. — **IMPLEMENTADO**: a rota legada `/api/evolution/instance/connect` deixou de reimplementar create+connect+QR inline (com o subscribe MINÚSCULO do achado A7 que o Evolution GO descarta) e passou a DELEGAR ao `EvolutionService.provision` (subscribe MAIÚSCULO + múltiplos endpoints de QR + campo `data.qrcode` + reset não-destrutivo da F1.3). Contrato de resposta preservado pro ChannelsPanel (`{base64}`/`{state:'open'}`/400). O Solo já usava o serviço consolidado. `test:evolution-connect-subscribe` 5/5; regressões `evolution-reset` 14/14, `falatu-solo-whatsapp` 72/72, `channel-health` 8/8, `falatu-whatsapp` 21/21 verdes. **Corrige na prática o fluxo real do ChannelsPanel** (era este endpoint que assinava eventos em minúsculo).

### Fase 2 — Conexão autônoma e configuração dos usos
- [ ] **F2.1** — Importação/criação/QR/retomada disponíveis na UI existente. — **F2.1a (backend) IMPLEMENTADO**: `ChannelProvisioningService` + rotas autenticadas `POST /api/channels/whatsapp/provision` (mode `new`|`existing`) e `GET /api/channels/whatsapp/status`. Org SEMPRE da sessão (RF-01 §7.2, nunca corpo/header); reusa `EvolutionService.provision`; idempotente por `(org,identifier)`; import §8 (nega instância de outra org; importa só a que EXISTE no provedor e está livre; nunca inventa); `EvolutionService.instanceExists` (verificação não-destrutiva); segredos nunca voltam. `test:channel-provision` 15/15; regressões `falatu-solo-whatsapp` 72/72, `security-tenant` 7/7, `channel-health` verdes. **F2.1b (UI) IMPLEMENTADO**: `ChannelsPanel` religado ao endpoint AUTENTICADO (`apiFetch('/api/channels/whatsapp/provision')`) no lugar do `fetch` legado não-autenticado. Toggle de modo "Usar/importar existente" × "Adicionar número novo" (os dois que o dono escolheu); QR/estado vindos do endpoint; retomada ao abrir a tela (`GET /whatsapp/status`); erros tipados tratados (409 outra empresa · 404 não existe · 502/`needsReset` provedor). UI-only, tsc+build verdes. **Fim do F2.1.**
- [ ] **F2.2** — Usos por funcionalidade e resolvedor aplicados no backend. — **IMPLEMENTADO (backend + escrita + UI)**: tabela `channel_feature_bindings` (aditiva/opt-in, UNIQUE por org+finalidade+unidade+canal) + `ChannelBindingService.resolve()` — resolvedor ÚNICO com precedência unidade→org→nenhum, priority DESC, gate por direção (inbound/outbound), fallback só quando configurado, revalidação de canal (org + não-desabilitado), motivo sempre no retorno. Isolamento (INV-01). **Sem religar os ~20 produtores ainda** (Fase 6) — nasce testado, 0-regressão. **Controles de ESCRITA IMPLEMENTADOS**: `ChannelBindingService.upsert/remove` (valida finalidade conhecida via `KNOWN_FEATURES`, canal/fallback da org, concorrência otimista por `policy_version` → `version_conflict`, auditado) + rotas autenticadas owner/admin `GET /api/channels/bindings`, `POST /api/channels/bindings`, `DELETE /api/channels/bindings/:id` (códigos 400/409/404 tipados; segredos nunca trafegam). `test:channel-binding` 21/21; regressões `channel-provision` 15/15, `security-tenant` 7/7, `channel-health` 8/8; tsc verde. **UI IMPLEMENTADA**: seção "Usos por finalidade (opcional)" no `ChannelsPanel` — lista os usos configurados (finalidade → número, com direção), remove, e adiciona (select de finalidade × número), consumindo `GET/POST/DELETE /api/channels/bindings`; texto deixa claro "sem configurar, tudo segue como está". UI-only, tsc+build verdes. **F2.2 completo.**
- [ ] **F2.3** — Preferências migradas sem habilitação indiscriminada. — **IMPLEMENTADO**: `ChannelBindingMigrationService.migrate(dryRun default)` deriva bindings SÓ dos sinais existentes (canal `kind='internal'`→`gestao`; atendimento→`atendimento`), `origin='migration'`, NÃO habilita finalidade nova (§9); dryRun é o default seguro (só relata); idempotente (2ª vez→skip_existing); NÃO sobrescreve binding manual (finalidade já em outro canal→conflict); ignora canal desabilitado; isolado por org. Rota `POST /api/channels/bindings/migrate` (dryRun default; `{dryRun:false}` aplica). `test:channel-binding-migration` 18/18; regressões `channel-binding` 21/21, `channel-provision` 15/15, `security-tenant` 7/7; tsc verde. 0-regressão (produtores só leem o resolvedor na Fase 6).
- [x] **F2.4** — Fluxo móvel e bloqueio de pendências ao desligar validados. — **IMPLEMENTADO (gate no SINK, opt-in)**: `OutboundFeatureDisabledError` + `ChannelBindingService.assertOutboundAllowed(orgId, feature?, {unitId?})` — só LANÇA quando a finalidade tem binding com saída DESLIGADA (`feature_disabled`); passa em `no_binding`/finalidade ligada/sem-feature (0-regressão). Wired como gate OPCIONAL no sink de saída `MessageProviderService.sendMessage`/`sendDocument` (quando o chamador informa `opts.feature`): desligar a finalidade BLOQUEIA o envio ANTES de tocar o provedor (fetch=0), sem afetar as outras finalidades do mesmo canal (CA-03). A ativação POR PRODUTOR (passar `feature` em cada ponto de envio) fica pra Fase 6 — hoje nasce testado e 0-regressão porque nenhum produtor passa `feature` ainda. `test:channel-binding-gate` 11/11; regressões `channel-binding` 21/21, `channel-binding-migration` 18/18, `evolution-credential` 6/6, `security-tenant` 7/7, `delivery-receipts` 10/10; tsc verde. **Fecha o G2 no escopo enxuto.**

### Fase 3 — Identidade e conversa interna unificadas
- [ ] **F3.0** — Auditoria de identidade/autorização/modo misto (RF-04/05/06). — **IMPLEMENTADO (doc-only)**: `docs/whatsapp-unificado/ANALISE-F3-identidade-modo-misto.md`. 8 perguntas respondidas com `arquivo:linha`. Achado nº1 (bloqueador G3): Diretor `ExecutiveAdvisorService.ask(orgId,question)` sem projeção por usuário + gate de dinheiro contornado em pergunta aberta (INV-11/X5, CA-04). Recorte F3.0→F3.1a→F3.1b→F3.2→F3.3→F3.4 proposto.
- [ ] **F3.1** — Identidade e permissão unificadas, incluindo perguntas abertas. — **EM ANDAMENTO**. **F3.1a IMPLEMENTADO (projeção por usuário no Diretor — CA-04/INV-11)**: `ExecutiveAdvisorService.ask(orgId, question, { canSeeMoney })` e `buildPanorama(orgId, { canSeeMoney })` passam a REDIGIR o financeiro quando o usuário não pode ver dinheiro (§73) — some pilar Financeiro, indicador em R$, meta em R$, comissão, recomendações de plano e o impacto R$ dos sinais; metas de contagem e fatos não-monetários seguem. Fecha o bypass do gate em PERGUNTA ABERTA (antes `needsMoney:false` pulava `canSeeMoney`). Chamadores religados: `FalaTuAskService` (pergunta aberta passa `canSeeMoney(user)`), rota web `POST /api/executive/ask` (sem `requireRole` — projeta pela MESMA régua, permitindo gerente), `webhookProcessor` (ramo `pergunta_negocio` é manager-only → `canSeeMoney:true` explícito). Default = mostra tudo (0-regressão: 5 testes de bloco + rotas owner/admin intactos). `test:executive-money-projection` 24/24; regressões `executive-briefing-block` 10/10, `executive-retail-commission-block` 8/8, `executive-plan-recommendations-block` 23/23, `executive-effectiveness` 10/10, `business-snapshot`, `falatu-ask` 33/33, `falatu-ask-whatsapp` 13/13, `security-money-gating` 12/12; tsc verde. **F3.1b IMPLEMENTADO (fachada de identidade)**: `SenderIdentityService.resolve(orgId, fromNumber)` — resolvedor ÚNICO read-only que concilia `users.phone` × `authorized_managers` (reusa `phoneMatch`, mesma régua/SQL do Gestor/Coordenador). NÃO eleva privilégio (RN §10): papel/perfil vêm SÓ do `users` casado — presença em `authorized_managers` nunca vira admin; manager sem usuário é `legacyManagerOnly` com `role:null`. Vínculo AMBÍGUO (2+ usuários casam o número) fica PENDENTE (`user:null`, `ambiguous:true` — não "chuta" identidade). Registra `confidence` (exact→high / tolerant→medium) + `matchType`. Isolado por org. **Nasce testado, sem religar consumidores ainda** (Gestor/Coordenador/Orchestrator na F3.3) → 0-regressão. `test:sender-identity` 30/30; regressões `gestor-command`, `falatu-whatsapp` 21/21, `ai-orchestrator` 59/59, `security-tenant` 7/7; tsc verde. **F3.1 (a+b) completo.**
- [ ] **F3.2** — Entrada comum web/WhatsApp com aliases preservados. — **PARCIAL/IMPLEMENTADO (prova de posse)**: `PhonePossessionService` + tabela `user_phone_bindings` (aditiva) vinculam telefone ao usuário AUTENTICADO por código de 6 dígitos (molde PIN Fase 28 reusado: `sha256`+`timingSafeEqual`+TTL 10min+cap 5 — não é 2º mecanismo). Guarda normalização/verificação/org/status (`pending→verified→revoked`, nunca DELETE). Reverificação de número NOVO REVOGA o vínculo anterior do usuário E o mesmo número verificado a outro usuário (1 dono verificado/número, §10). Confiança `high` no verificado (complementa o `medium` tolerante da F3.1b). NÃO escreve `users.phone` (telefone é endereço do canal — o resolvedor prefere o vínculo verificado na F3.3). Entrega do código é injetável (transporte é do chamador — evita acoplar canal). `verifiedBinding(orgId,phone)` exposto pro resolvedor. Isolado por org; `list` redigido (sem hash). **Nasce testado, sem religar** → 0-regressão. `test:phone-possession` 20/20; regressões `sender-identity` 30/30, `falatu-protocols` 42/42, `security-tenant` 7/7, `falatu-solo-whatsapp` 72/72; tsc verde. **Falta:** entrada comum web/WhatsApp + preservação explícita de aliases entram com o modo misto (F3.3).
- [ ] **F3.3** — Modo misto sem vazamento de conversa interna para CRM. — **EM ANDAMENTO**. **F3.3a IMPLEMENTADO (roteador puro)**: `MixedModeRouterService.route(orgId, senderId, input)` decide a FAIXA pela ordem do §10 — `skip` (própria/§10.3) · `attendance` (cliente; ÚNICO que autoriza CRM, §10.8) · `internal` (gestão) · `ask_which` (papel duplo sem contexto claro, §10.7 — NUNCA infere por texto) · `reject_unknown_internal` (desconhecido em canal interno) · `ambiguous_identity` (pessoa conhecida mas ambígua num caminho que exige QUEM). Compõe `SenderIdentityService` (F3.1b) + `PhonePossessionService.verifiedBinding` (F3.2, precedência — desfaz ambiguidade) + `channels.kind`; papel só do usuário resolvido (F3.1a/§73), manager legado não eleva. Função PURA (lê identidade org-scoped, não escreve/cria CRM/responde); contexto de conversa (§10.5) é INPUT do chamador (não lê estado). **Ninguém consome ainda** — a fiação no `webhookProcessor` atrás da flag `mixed_mode_enabled` é a F3.3b → 0-regressão. `test:mixed-mode-router` 22/22; regressões `sender-identity` 30/30, `phone-possession` 20/20, `gestor-command`, `falatu-whatsapp` 21/21, `security-tenant` 7/7; tsc verde. **F3.3b IMPLEMENTADO (fiação atrás de flag)**: flag `mixed_mode_enabled` (default 0 → caminho inbound IDÊNTICO ao de hoje, 0-regressão). No `webhookProcessor`, ANTES de criar contato/ticket num canal de atendimento, `MixedModeInboundService.decide` consulta o roteador (F3.3a) com o contexto de conversa (ticket ABERTO do número = `hasActiveCustomerContext`): `internal` → `runInternalInbound` (bloco interno EXTRAÍDO e reusado — FalaTu/Controller/Diretor/Coordenador) SEM criar CRM (fecha "gestor vira ticket", CA-04/§10.8); `ask_which` → grava pendente DURÁVEL (`mixed_mode_pending_choices`, TTL 30min) + pergunta "atendimento ou gestão?" (§10.7, escolha por sinal EXPLÍCITO 1/2, nunca NL); resposta resolve a faixa e limpa o pendente; `attendance`/desconhecido → fluxo de hoje. Gestor COM atendimento em curso → segue no atendimento. Fail-open pro atendimento (erro na decisão cai no fluxo atual). Bloco interno extraído em `runInternalInbound` (canal interno chama o mesmo helper — sem duplicar). `test:mixed-mode-inbound` 12/12 (dirige o `processIncomingMessage` real); regressões `falatu-whatsapp` 21/21, `falatu-solo-whatsapp` 72/72, `falatu-ask-whatsapp` 13/13, `gestor-command`, `falatu-trigger-only` 23/23, `security-webhook` 12/12, `meta-webhook-dedup` 10/10, `whatsapp-jid` 16/16, `mixed-mode-router` 22/22, `security-tenant` 7/7; tsc verde. **F3.3 completo.** Pendente durável do modo misto é MÍNIMO — a desambiguação numerada robusta (Gestor/Coordenador `lastActions`/`lastList` in-memory) é a F3.4.
- [x] **F3.4** — Confirmações, expiração e papéis ambíguos tratados. — **IMPLEMENTADO (desambiguação durável — CA-06)**: `NumberedListContextService` + tabela `numbered_list_contexts` (aditiva) guardam a ORDEM EXATA da lista numerada MOSTRADA por (org, usuário, escopo), com TTL. `GestorCommandService` ("aprovar N", escopo `gestor_approvals`) e `CoordenadorService` ("concluir/iniciar/ajuda N", escopo `coordenador_tasks`) trocaram os Maps IN-MEMORY (`lastActions`/`lastList`, perdidos no restart) por esse contexto durável: "N" resolve contra a lista que o usuário VIU — não contra uma lista RECOMPUTADA que, após restart, poderia estar reordenada e confirmar a AÇÃO ERRADA (risco exato do CA-06). Recompute só permanece como fallback quando o usuário NUNCA viu uma lista (sem ordem "errada" a preservar) → 0-regressão da conveniência. Isolado por org/usuário/escopo; TTL 30min evita "N" resolver lista velha. `test:numbered-list-context` 18/18; regressões `gestor-command`, `falatu-whatsapp` 21/21, `falatu-solo-whatsapp` 72/72, `falatu-approve-whatsapp` 12/12, `mixed-mode-inbound` 12/12, `security-tenant` 7/7; tsc verde. **Fecha o Gate G3 no escopo enxuto.**

### Fase 4 — Consolidar registros operacionais do Fala Tu
- [ ] **F4.0** — Auditoria de consolidação (RF-09/CA-09). — **IMPLEMENTADO (doc-only)**: `docs/whatsapp-unificado/ANALISE-F4-consolidar-registros-falatu.md`. Mapeia o silo (`falatu_inbox_items`/`falatu_tasks`/`falatu_events`/`falatu_lists`), o dual-write `FalaTuService.confirm` (3 flags), a recon `FalaTuBridgeReconService.report`/`backfillTasks` (ADR-160 F10), e os GAPS: F4.1 relatório por registro + taxonomia de conflito (só há contagem agregada); F4.2 dry-run + checkpoint + backfill events/lists; F4.3 virada de leitura; **F4.4 achado nº1 — conclusão NÃO converge** (`toggleTask` mexe só no silo). Recorte F4.0→F4.1→F4.2→F4.3(convergência)→F4.4(prova CA-09).
- [ ] **F4.1** — Relatório de vínculos e conflitos por registro disponível. — **IMPLEMENTADO (relatório por registro + taxonomia de conflito, §15.1)**: `FalaTuBridgeReconService.records(orgId, {limit,offset})` — read-only, derivado por query (RN-004), isolado por org. Classifica CADA registro do silo (task/event/list) na política de migração: `linked_ok` · `linked_state_divergent` (conclusão diverge silo×canônico — surfaça o achado nº1 da F4.4) · `broken_link` (vínculo→canônico inexistente) · `unlinked_migratable` (tarefa/lista shopping sem espelho) · `personal_only` (nota/evento pessoal, lista não-shopping — preservar). NÃO infere "dois objetos possíveis" (§15.1 proíbe dedupe por título/valor/data). `counts` cobrem TODA a população; `records` é paginado (offset/limit) + `truncated`. Rota `GET /api/falatu/bridge/records` (owner/admin). `test:falatu-bridge-records` 23/23; regressões `falatu-bridge-recon` 15/15, `falatu-porta`/`-events`/`-lists` verdes; tsc verde. **Falta F4.2** (backfill dry-run + checkpoint + events/lists).
- [ ] **F4.2** — Migração idempotente retomável sem efeitos externos. — **IMPLEMENTADO (dry-run + checkpoint + listas)**: `FalaTuBridgeReconService.backfillTasks/backfillLists({limit,dryRun})` — **dry-run** relata `wouldBackfill` SEM criar canônico, carimbar ou gravar checkpoint (zero efeito externo, §15.1); execução **real** cria via o MESMO caminho da porta viva (tasks→`TaskService.create`; listas shopping→`matchItemsToProducts`+`addManualItems` draft, só itens que casam o catálogo — RN-151), carimba `bridged_*` e grava **checkpoint durável** (`falatu_bridge_backfill_state`: `migrated_total`/`runs`/`last_run_remaining` por org+tipo → retomável/observável, §15.2). Idempotente (só `bridged_*_id IS NULL`; reexecutar não recria — RN-004). `backfillEvents` → `not_applicable` (contact-gated: appointment exige contato real, o silo não guarda → não fabrica). Rotas `POST /api/falatu/bridge/backfill-tasks|backfill-lists` (dryRun/limit) + `GET /bridge/backfill-state` (owner/admin). `test:falatu-bridge-backfill` 21/21; regressões `falatu-bridge-recon` 15/15, `falatu-bridge-records` 23/23, `falatu-porta-lists`; tsc verde. **Falta F4.3** (convergência de conclusão — achado nº1).
- [ ] **F4.3** — Registros operacionais principais preservados; notas pessoais mantidas. — **IMPLEMENTADO (convergência de conclusão/reabertura — achado nº1, CA-09)**: concluir/reabrir converge nos DOIS sentidos para pares bridgeados (§15.2). `FalaTuService.toggleTask` propaga o BIT de conclusão pro canônico via `TaskService.move` (concluir→'feito'; reabrir só desfaz 'feito', não pisa em 'fazendo'/'cancelada' — que o silo não conhece); `TaskService.move`/`recordResult` refletem de volta no espelho (`falatu_tasks WHERE bridged_task_id`) via helper guardado `reflectFalatuMirror` (UPDATE idempotente, sem chamar o FalaTuService → sem loop). Só pares bridgeados; tarefa não-bridgeada / sem espelho = 0-regressão. Divergência PRÉ-EXISTENTE NÃO é auto-resolvida (continua sinalizada pela F4.1 — não reabre/fecha tarefa velha em silêncio). Best-effort na propagação Fala Tu→canônico (falha não derruba a ação; drift fica visível no relatório). `test:falatu-bridge-convergence` 15/15; regressões `task-result`, `task-recurrence` 19/19, `task-recurrence-edit` 13/13, `falatu-porta`, `falatu-bridge-recon`/`-records`/`-backfill`; tsc verde. **Falta F4.4** (prova CA-09 ponta-a-ponta).
- [x] **F4.4** — Conclusão/reabertura e reversão coerentes entre interfaces. — **IMPLEMENTADO (prova CA-09 ponta-a-ponta — compõe F4.1/F4.2/F4.3, sem código novo de produção)**: `test:falatu-consolidation-ca09` (18/18) semeia dataset misto (vínculo válido · divergente silo×canônico · ausente/migratable · quebrado · evento pessoal · evento válido · lista shopping migratable · lista general pessoal) e prova o CA-09: (2) migra **2×** → 1ª migra 1 tarefa + 1 lista, 2ª não migra nada (idempotente), canônicos NÃO crescem (**sem duplicação**) e silo intacto (**sem perda** — discard é UPDATE, nunca DELETE); (3) **sem promoção de acesso** — evento pessoal nunca vira appointment, lista general nunca vira requisição (só a shopping migra); (4) vínculo **quebrado segue `broken_link`** e conflito de estado segue `linked_state_divergent` (não auto-resolvidos); (5) **estados coerentes entre interfaces** — concluir no Fala Tu → canônico 'feito', reabrir no quadro → silo não-concluído, tarefa migrada também converge; (6) **reversibilidade/compat** — flag off deixa o silo legível, preserva os vínculos, backfill recusa; re-habilitar não duplica; (7) isolamento entre orgs. Regressões `falatu-bridge-recon` 15/15, `-records` 23/23, `-backfill` 21/21, `-convergence` 15/15, `task-result`; tsc verde. **Fase 4 fechada — Gate G4 (CA-09) atendido.**

### Fase 5 — Solicitar e receber arquivos pela conversa
- [x] **F5.0** — Auditoria de arquivos pela conversa (RF-07/CA-07). — **IMPLEMENTADO (doc-only)**: `docs/whatsapp-unificado/ANALISE-F5-arquivos-conversacionais.md`. Mapeia o que JÁ existe (PDF `ReportPdfService`/pdfkit; XLSX `buildXlsx` OOXML sem dependência; `FalaTuReportService.executiveSummary` PDF/XLSX com projeção por papel → artefato privado + link assinado via `POST /api/falatu/reports/summary`; `ArtifactService` disco privado+sha256+RBAC por classificação+download público `routes/artifacts.ts:43`; `fileSigning` HMAC 15min; `JobQueueService`+precedente `generate_manager_pdf`) e os 3 GAPS reais: **DOCX inexistente** (só o mapa MIME→ext em `ArtifactService.ts:37`); **`sendDocument` fixa `application/pdf`** (`MessageProviderService.ts:259`, Evolution); **`signedUrl` relativo** (`ArtifactService.ts:125`, WhatsApp busca server-to-server → precisa URL absoluta `APP_URL`). Sem roteador de intenção de arquivo (hoje regex `\bpdf\b` `AIOrchestratorService.ts:437`), sem referência ao "isso"/último resultado. Recorte F5.0→F5.1(catálogo+recorte)→F5.2(DOCX real)→F5.3(entrega tipada+URL absoluta+job)→F5.4(prova CA-07).
- [x] **F5.1** — Catálogo, contexto e autorização de arquivos implementados. — **IMPLEMENTADO (catálogo + autorização + referência "isso", §13.1/§13.2/§13.3)**: `FileRequestCatalogService` — a etapa de INTERPRETAÇÃO+AUTORIZAÇÃO do pedido de arquivo, separada da geração (F5.2)/entrega (F5.3). **Catálogo FECHADO** (`executive_summary`→`ContextEngine` e `existing_artifact`→`ArtifactService` com consulta PRONTA; `sales_by_period`/`accounts_finance`/`tasks_by_assignee` declaram a dependência de domínio com `queryStatus:'pending_domain_query'` — consulta implementada NO DOMÍNIO depois, §13.2, nunca no webhook); pedido fora do catálogo → devolve as opções, NUNCA inventa (RN-151). **Autorização** revalidada na resolução via projeção por papel (`ContextProjectionService.canSeeDomain`/`projectSnapshot`), fail-closed, recusa GENÉRICA sem vazar título/estrutura (§13.4). **Resultado estruturado** data-only com fonte+instante+ausência (§13.1 passo 3), NÃO gera arquivo. **Referência "isso"** durável (tabela aditiva `falatu_last_result`, 1 linha por org+usuário+conversa, upsert; padrão durável herdado da F3.4) — reexporta o MESMO snapshot congelado em outro formato ("me manda isso em Excel") sem reconsultar; **revogação** entre consulta e reexport REDUZ o servido (reaplica a autorização atual sobre o snapshot). `formatGeneratorReady` honesto (pdf/xlsx sim, docx só na F5.2 — nunca renomeia). Rotas `GET /api/falatu/files/catalog` + `POST /files/resolve`. `test:falatu-file-catalog` 18/18; regressões `falatu-report`/`xlsx`/`artifacts`/`artifact-rbac` verdes; tsc verde. **Falta F5.2** (DOCX real).
- [x] **F5.2** — PDF/XLSX reutilizados e DOCX real gerado. — **IMPLEMENTADO (DOCX real, §13.3)**: `DocxService.buildDocx` — gerador de `.docx` REAL (OOXML WordprocessingML editável) SEM dependência nova, empacotado pelo helper compartilhado `ooxmlZip` (`zipStored`/`xmlEscape`, STORED/determinístico — extraído do `XlsxService`, que passou a reusá-lo, sem duplicar). Monta `[Content_Types].xml` + `_rels/.rels` + `word/document.xml` com parágrafos reais (título/subtítulo/seções heading+linhas/rodapé), runs com negrito e tamanho (half-points), `sectPr` — abre e edita no Word/LibreOffice/Docs. NUNCA renomeia PDF/HTML pra `.docx`. Integrado ao `FalaTuReportService` como 3º formato (`ReportFormat += "docx"`, mesmas seções do PDF, herda a projeção por papel) + `DOCX_MIME`. Catálogo (F5.1): `formatGeneratorReady.docx` agora `true`. `test:falatu-docx` 15/15 (valida o container com `unzip` DE VERDADE — partes WordprocessingML, texto real, XML escapado, negrito/tamanho, não-é-PDF-renomeado, projeção por papel, isolamento); regressões `xlsx` 13/13 (refactor de zip byte-safe), `falatu-report`, `artifacts`, `artifact-rbac`, `falatu-file-catalog` verdes; tsc verde. **Falta F5.3** (entrega tipada por MIME + URL absoluta + job durável).
- [x] **F5.3** — MIME, fila, artefato e entrega segura integrados. — **IMPLEMENTADO (entrega tipada + URL absoluta + job durável, §13.3/§13.4)**: (1) `MessageProviderService.sendDocument` deixou de FIXAR `application/pdf` — recebe `opts.mimeType` e propaga no `bodyA` da Evolution (default PDF → 0-regressão nos callers existentes; XLSX/DOCX agora saem com o MIME certo). (2) `ArtifactService.absoluteSignedUrl`/`absoluteSignedUrlForUser` — resolve a URL assinada RELATIVA (`/api/public/artifacts/...`) em ABSOLUTA com `APP_URL` (o provedor baixa server-to-server); sem `APP_URL` → null honesto (não manda link quebrado); revalida acesso na entrega. (3) `FileDeliveryService` — conecta geração/localização à ENTREGA: `deliverNow` gera o do catálogo (`executive_summary` via `FalaTuReportService`, herda projeção por papel) OU localiza artefato existente, monta URL absoluta assinada (REVALIDA autorização — revogação bloqueia sem vazar conteúdo nem link), envia com MIME tipado + **fallback DECLARADO** pro link (nunca finge "anexado"); `enqueue` roda como JOB durável na `JobQueueService` (handler `deliver_file`, mesmo mecanismo do `generate_manager_pdf`, sem 2ª fila). `test:falatu-file-delivery` 14/14 (MIME por formato · URL absoluta · gera×localiza · revogação não envia · fallback de link · sem-APP_URL não inventa · pending honesto · job enfileirado · isolamento); regressões `falatu-docx`/`xlsx`/`falatu-report`/`artifacts`/`artifact-rbac`/`falatu-file-catalog` verdes; tsc verde. **Falta F5.4** (prova CA-07 ponta-a-ponta → Gate G5).
- [x] **F5.4** — Arquivos abertos e dados/permissões/fallback verificados. — **IMPLEMENTADO (prova CA-07 ponta-a-ponta — compõe F5.1/F5.2/F5.3, sem código novo de produção)**: `test:falatu-file-ca07` (13/13) prova o critério de aceite: (1) pede o relatório → resposta ESTRUTURADA autorizada com o dado de vendas; (2) solicita os **3 formatos** → cada um ABRE de verdade (PDF `%PDF`; XLSX/DOCX via `unzip` com OOXML/WordprocessingML reais) e MANTÉM o mesmo valor (120); (3) **DOCX comprovado por CONTEÚDO** real (`word/document.xml` com `<w:document>` + valor), não só extensão (Gate G5); (4) cada entrega usa URL **absoluta** + MIME **tipado** por formato; (5) usuário SEM permissão **não recebe conteúdo nem link** (revogação); (6) **projeção por papel na entrega** — vendedor recebe vendas mas não o valor sensível de finance (9000); (7) **fallback DECLARADO** — anexo falhou → link identificado; (8) tamanho honesto; (9) isolamento multi-tenant. Regressões `falatu-file-delivery`/`falatu-docx`/`xlsx`/`falatu-file-catalog`/`falatu-report` verdes; tsc verde. **Fase 5 fechada — Gate G5 (CA-07) atendido.**

### Fase 6 — Integrar os demais usos e validar falhas
- [ ] **F6.1** — Consumidores remanescentes usam seleção por finalidade. — NÃO INICIADO
- [ ] **F6.2** — Falhas parciais, deduplicação e políticas de fila validadas. — NÃO INICIADO
- [ ] **F6.3** — Guardas das verticais e automações preservadas. — NÃO INICIADO
- [ ] **F6.4** — Saúde, métricas, outros canais e capacidade do piloto verificados. — NÃO INICIADO

### Fase 7 — Piloto, observação e descontinuação controlada
- [ ] **F7.1** — Rollback exercitado e piloto autorizado preparado. — NÃO INICIADO
- [ ] **F7.2** — Ciclo representativo do piloto executado e observado. — NÃO INICIADO
- [ ] **F7.3** — Regressões corrigidas e reconciliação concluída antes de ampliar. — NÃO INICIADO
- [ ] **F7.4** — Redundâncias elegíveis retiradas; compatibilidade e histórico preservados. — NÃO INICIADO

## Prestação de contas — entrega Fase 0

```
ENTREGA: Fase 0 — auditoria + baseline (doc-only)
HEAD de entrada: 40508f13f74598549e1bd47cf1d33904fc6d76fc
Commit/PR entregue: ver PR desta branch

Resultado para o usuário:
Auditoria do HEAD conforme a instrução principal do PRD (revalidar antes de mexer).
13/13 achados da §4.1 revalidados + 7 achados extras, com evidência arquivo:linha.
Nenhum código de produção alterado.

Reuso e redundância:
- Nenhuma remoção nesta fase (proibido antes de migração+validação — §15/§19).
- Confirmado que NÃO existe resolvedor único de canal nem tabela de uso por
  finalidade; ~20 cópias do mesmo SQL "primeiro canal" — alvo da RF-03.

Dados e migração:
- Nenhuma migração executada. Simulação numérica adiada para F4.1 (modo sem efeitos).
- Efeitos externos disparados durante migração: zero (nenhuma migração rodou).

Validação:
| Comando/cenário | Ambiente | Resultado real | Evidência |
| grep de existência dos 25 testes de baseline | coding env | 25/25 presentes | F0.4 |
| npm run lint / build (docs-only) | coding env | n/a (sem mudança de código) | — |
Falhas preexistentes: execução real da suíte pendente (início da Fase 1).
Regressões novas: nenhuma (doc-only).

Gates/invariantes: Gate G0 — inventário e baseline auditáveis; Evolution real pendente.

Ativação:
- Flags/configurações alteradas: nenhuma.
- Piloto/produção: não iniciado.
- Reversão: trivial (doc-only).

Riscos e bloqueios restantes: acesso à Evolution real (Swagger + fixtures).
Próxima entrega prioritária: Fase 1 (F1.3 reset destrutivo; F1.1/F1.2 credencial+identidade).
```
</content>
